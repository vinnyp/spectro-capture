# Capture Mode PRD — round 1 fixes

Resume point for round 1 of the peer-review gate. One box per finding from every review that ran (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`). The fence file `prd-capture-mode-fences.md` (F1–F13) is the owner's written authorization for every change below. Tick each box as the fix lands in `prd-capture-mode.md`. Line numbers refer to the document at commit `e5c43d1`.

Source key: PM / Staff (S#) / Test (T#) / Arch (A#) = Claude lenses; agy-PM / agy-Staff / agy-Arch = cross-model lenses. R1-F# = consolidated finding in the log.

## Blockers and Majors (owner-adjudicated)

- [x] **FX1** (R1-F1 · PM Blocker, T1, S3, T17, agy-PM minor "skip invocation") — **fence F6.** Rewrite UJ3.1 step 3 and its Retry/Skip/Flag bullets, UJ3 step 9's disagreement branch, and the session-core flowchart: a per-scan failure HOLDS the current row; the next trigger press is the retry; Skip and Flag are deliberate queue-management keys (bindings not specified); after K_FAILED_ATTEMPTS (candidate 3, OQ) the row auto-defers with a distinct "moved on" cue and the queue advances; Skip after a failure advances exactly once; state explicitly which row is current after a failure and what the next press scans. A failed retry increments the consecutive-failure counter (T17). Re-take pressed while a scan is in flight is rejected like a second trigger (T17). Update the session-lifecycle diagram edge "sample failed … row deferred, queue advances" to "K_FAILED_ATTEMPTS reached or Skip/Flag: row deferred".
- [x] **FX2** (R1-F2 · S1 Blocker, T2, T3) — **fence F7.** Per-scan set = ambient light leakage, out-of-range temperature, set disagreement, operator flag. Remove "calibration drift" from the UJ3.1 intro and "no reading arrives within SCAN_TIMEOUT" from UJ3.1 step 1; add both to UJ3.6 step 1 as device-PRD halts (scan-delta drift per device PRD §3; no reading per §5 liveness). Remove SCAN_TIMEOUT from UJ3 step 6's branch, the constants bullet, and the surfaced OQ list. Update UJ3.3 step 1's cause list.
- [x] **FX3** (R1-F3 · S2 Blocker, A2, A10) — Rewrite UJ3 step 10, the row-state diagram edge "N samples agree, averaged, durably written", UJ3.8 step 4, and UJ4 step 8: the row's canonical measurement is the set of N raw per-mode sample payloads, durably written before anything else; the averaged value is derived from them and carries a derivation version; QC ΔE and display use the derived mean; the averaging space (spectral vs colorimetric) is a Data Foundation constraint. Add a `### Inherited obligations for Data Foundation` subsection at the end of the User Journeys section listing: the canonical set / derived mean; sample-level raw payloads; attempt records with cause and outcome (retained good samples on a deferred row, failed attempts, failed re-scan attempts); adjudication decisions with notes (leave deferred, accept average with spread); the durability guarantee (FX12); the recorded measurement condition per reading and derivation inputs per derived value (FX30); all immutable.
- [x] **FX4** (R1-F4 · A1, agy-Arch Blocker, T9, agy-Staff Major, PM minor `:355`) — **fence F12.** Persisted session cursor: update UJ3.5 step 3, UJ3.6 step 3, UJ3.4 step 4 ("next session opens at the first pending row" → "at the cursor, or the next pending row if the cursor row is no longer pending"), UJ2.1 step 9, and the session-lifecycle diagram note. State that the device PRD §5 "current item" definition applies to the un-jumped case and record the cursor refinement as an inherited note for the device PRD in the surfaced list.
- [x] **FX5** (R1-F5 · PM Major, S6, T11 part) — **fence F9.** Flag row semantics per F9: on the current pending row (cause "flagged: missing or damaged") or on the most recent captured row on the recents strip (Captured → Deferred, reading demoted to history). Add the `Captured --> Deferred` edge to the row-state diagram. Rewrite the Flag bullet in UJ3.1 step 3; amend UJ3.8 step 4's "a good reading is never demoted by a bad retry" to add "only the operator's Flag demotes a captured reading".
- [x] **FX6** (R1-F6 · PM Major, T11, A7, S11, agy-Staff minor) — Wrap-exhausted exit: in UJ3.7 step 4 and UJ3 step 13, when a wrap reaches only rows that were skipped without an attempt in this pass, the session offers "Flag remaining as missing" (they become deferred) or "End session"; flagging enters the review. Update the session-lifecycle diagram: `Capturing --> Review: queue exhausted with deferred rows, or wrap reaches only skipped rows`.
- [x] **FX7** (R1-F7 · PM Major) — UJ3 step 2: when the collection has zero pending rows and one or more deferred rows, "Start capture session" runs the pre-flight gate and opens directly into UJ3.3; the nothing-to-capture state lists "Review N deferred rows" alongside import, add item, re-scan.
- [x] **FX8** (R1-F8 · PM Major, T5 N=1) — **fence F10.** Add "Accept average" as the third choice at UJ3 step 9's disagreement branch and in UJ3.3 (review); the spread is recorded on the reading (Collection Mode obligation, add to FX3's list). State that N = 1 skips the agreement check (UJ1 step 3 and UJ3 step 9).
- [x] **FX9** (R1-F9 · PM Major, S8, T10) — **fence F11.** State the normalisation rule once in UJ2.2 step 2 (trim, collapse internal whitespace, case-insensitive, display value preserved; applies to collection names too — UJ1 step 2). Cite it from UJ2 step 8, UJ2.1 step 6, UJ3.7 step 2, UJ4 step 4, UJ4.1 step 2. Keep UJ2.1's ambiguous-match branch.
- [x] **FX10** (R1-F10 · A3, S7, T18) — UJ4 and out-of-session UJ3.8 are one-row sessions: pre-flight, device binding, end themselves when the row is captured or when the operator ends from a halt. Rewrite UJ4 step 9 ("the session ends itself") and UJ3.8 step 1 (the no-session entry runs the pre-flight gate). State that while a bulk session on the same collection is paused or interrupted, an ad-hoc capture or re-scan attaches to that session (one in-flight session per collection).
- [x] **FX11** (R1-F11 · S5 Major, A6, T15, S13) — **fence F12.** Define the session entity in the vocabulary paragraph at the top of User Journeys (collection-owned; device identity, released at quit; started/ended; status active | interrupted | ended | complete; elapsed capture time; cursor). At most one active per app, one interrupted per collection; two collections may each hold an interrupted session. Rewrite UJ3.5 step 4: "Resume capture" after relaunch is a new session start against the persisted cursor — full pre-flight gate including authorization — binding whichever device is connected; replace the "original device required" branch with "if a different device is connected, it is bound for the new session; the session summary records the device change". An interrupted session can be ended from the collection (abandonment). Update UJ1.2's delete guard wording to "an active or interrupted session".
- [x] **FX12** (R1-F12 · A4) — Add to UJ3 step 11 and the UJ3.5 intro the stated guarantee: a reading whose row-success confirmation reached the operator survives power loss and drive detachment; the confirmation is gated on the durable write, not on the transaction returning; the mechanism is Data Foundation's (ADR-0003). Add to FX3's obligations list.
- [x] **FX13** (R1-F13 · S9, T7) — Replace PERCEPTUAL_FUSION_WINDOW in UJ3 step 7 and the constants bullet with TRIGGER_ACK_WINDOW (candidate 100 ms from any host-visible trigger press) and RESULT_CUE_LATENCY (candidate TBD, from reading arrival); add a write-to-advance latency budget for the row-success confirm (ROW_CONFIRM_BUDGET, candidate TBD, OQ). Note that both cues and the Demo Device's latency run on the injectable clock (FX18).
- [x] **FX14** (R1-F14 · agy-Staff Blocker, S4, T6b, A9, PM minor `:278`) — **fence F8.** Remove N_CONSEC_DRIFT from UJ3.1 step 4, the constants bullet, and the OQ list; add one sentence that drift detection relies on the device PRD's calibration-due signal and scan-delta halt plus the within-item spread. Define "hard failure" (an instrument-reported per-scan failure: light leak, temperature) vs "deferred" for the two remaining counters (S4).
- [x] **FX15** (R1-F15 · T4) — Delete "reading out of range" / "out-of-range" from UJ3 step 6, UJ3.1 step 1, UJ3.3 step 1, and the session-core flowchart.
- [x] **FX16** (R1-F16 · T5, S16) — UJ3 step 9: SAMPLE_TOLERANCE statistic = the maximum ΔE2000 of any sample from the set mean, computed under the collection's display illuminant/observer defaults (candidate statistic and basis — OQ); N = 1 skips the check.
- [x] **FX17** (R1-F17 · T6a, S12) — UJ3.1 step 4: the guard mode is an explicit setting (enabled / log-only), default an OQ; remove "logged but does not pause during the first ~10 real sessions" from the journey and move the phasing to the OQ bullet's closer ("log across the first ~10 dogfood sessions with the guard in log-only mode; whether a dogfood build counts as a release for the no-unresolved-constant rule is the owner's call").
- [x] **FX18** (R1-F18 · T8, T15, T7, T20) — Add `### Simulated-layer obligations this PRD adds` (a short list after the surfaced questions): a measurement-command log (start, outcome, initiator) so "exactly one measurement per accepted trigger" is assertable; a declared collection-and-session persisted-state fixture (rows in each state, an interrupted session with cursor, an open halt record); Demo latency and cue timestamps on the injectable clock; simulated readings carry the same per-mode set as live. Mark each "(obligation on the device PRD's §6 layer; inherited)".
- [x] **FX19** (R1-F19 · T12, A8, S16) — One partial-set rule stated in the vocabulary paragraph: an in-progress sample set survives only reorder and queue-list viewing (no other scan intervenes); Pause (UJ3.4 step 2), jump (UJ3.7 step 3), entering the review (UJ3.3 step 1), and Add item (UJ4.1 step 1 and 5) discard the held set and return the row to sample 0. Update each of those steps to say so.
- [x] **FX20** (R1-F20 · agy-Staff Major, PM minor `:241`, PM nit `:243`) — UJ3 step 5 trigger-agnostic: "triggers a scan — the instrument's own button where the SDK surfaces it, otherwise a one-hand keyboard or on-screen trigger, which is designed as the primary path until the hardware spike says otherwise". Rename "keyboard advance" in the accessibility bullet to "the trigger and the queue-management keys".
- [x] **FX21** (R1-F21 · agy-Arch Major, A5, S15) — UJ3.10 steps 1 and 4: queue order is a per-row collection attribute distinct from any view's sort; only UJ3.10's explicit reorder actions change it. Add a row to the UJ5 differences table: "Mid-session reorder and queue list — Reading A: a second list inside the takeover; Reading B: the collection's own list" and name it as input to the ADR-0004 prototype.
- [x] **FX22** (R1-F24 · agy-PM Major → Minor) — **fence F13.** Keep UJ4.1; add one sentence to its intro recording the decision and the cross-model concern (creeping UI on the capture surface) as input to UJ5.

## Minors and nits

- [x] **FX23** (PM minor `:355`) — resume position after skips: covered by FX4; tick when FX4 lands.
- [x] **FX24** (PM minor `:155-156`) — UJ2 step 8 and UJ2.2 step 2's branch: duplicate codes in the file are listed and excluded (both offending rows), proceed or fix the file — the same treatment as blank codes; update the import flowchart node G2.
- [x] **FX25** (PM minor `:178`, S14) — UJ2.1: rows in the collection absent from the file are left untouched, never deleted; a fourth preview line "N rows in the collection are not in this file — left untouched"; an import commits all rows or none (also UJ2).
- [x] **FX26** (PM minor `:144-147`, T13) — UJ2 step 3: rows with the wrong column count are listed by row number and excluded; detected encoding/delimiter is shown only when detection failed or on request; the file-changed check runs at commit; a file missing at commit returns to the unreadable-file state.
- [x] **FX27** (PM minor `:186`) — UJ2 step 6 and UJ2.2 step 3: Swatch Name is optional; only Swatch Code is required.
- [x] **FX28** (PM minor `:109`) — UJ1 step 3: samples-per-row defaults to 3 and stays required-but-defaulted.
- [x] **FX29** (PM minor `:505-552`, agy-PM minor "prefer Reading A") — UJ5: add three lines — the discriminating observable (time and steps to reach the just-captured row at pause and end; mode-slip count with capture shortcuts off-surface; where the deferred review and recents strip live), the research default (Reading B, the only reading with shipped precedent) for the owner to confirm or overrule, and what closes it (a Demo Device prototype of each). Record the cross-model product lens's preference for Reading A (distraction-free, lower mode-slip) as input.
- [x] **FX30** (S10) — UJ3 step 6: the measurement condition(s) (M0/M1/M2) are recorded with the reading; illuminant/observer are recorded with each derived value (fence F2); note that acq v2 §6 Q6.4 asks for both on the raw row and that this is a Data Foundation constraint (derivation inputs recorded with every derived value).
- [x] **FX31** (S13) — relaunch resume vs runs-to-completion: covered by FX11 (new session start, full gate); tick when FX11 lands.
- [x] **FX32** (S17) — State the v1 design scale in the Cluster 3 intro as provisional constants: ROWS_TARGET (candidate 200–1,200 per collection and per import), ROWS_CEILING (candidate 10,000, the ADR-0001 spike's tested bound), a session up to one working day. Add to the OQ list.
- [x] **FX33** (S18) — UJ3 step 4: the two-redundant-indicators sentence becomes conditional ("under Reading B; under Reading A the takeover itself is the indicator — see UJ5").
- [x] **FX34** (S nit, A11) — Replace "collection file" with "the store file" (or "the file holding the collection") throughout.
- [x] **FX35** (S nit `:400`) — UJ3.7 step 2: the find narrows across all rows, pending first, then deferred, then captured, so the three bullets are reachable.
- [x] **FX36** (S nit `:172`) — UJ2.1 step 6: "unchanged rows are untouched" in place of "already found … are not re-imported".
- [x] **FX37** (S nit) — UJ2.2 step 7: the header signature is the set of column names, order-insensitive.
- [x] **FX38** (T14) — UJ3.9 step 5: a disconnect walks UJ3.6; a process kill and relaunch walks UJ3.5; state the resume assertion (counts identical, current item per the cursor, no duplicate rows).
- [x] **FX39** (T16) — UJ3.7 step 2: find matches by prefix on code and substring on name, normalised per F11; UJ3.10 step 3: sorts are stable, locale-aware, numeric-aware for codes, and toggle direction.
- [x] **FX40** (T19) — UJ3.5 sleep branch: state that with no sleep seam the test reduces to "inject disconnect, change a readiness input, Resume".
- [x] **FX41** (A12) — UJ3 step 4: define "row r of R" — r is the row's position among pending rows in the current queue order; R is the pending count, which grows on insert.
- [x] **FX42** (agy-Staff nit) — UJ2.1 step 6: the per-row keep/overwrite choice is a toggle per row in the preview with an all-rows default, not a blocking dialog series.
- [x] **FX43** (agy-Arch minor) — INSERT_POSITION OQ bullet: add "append to the end and jump to it (UJ3.7)" as the simpler alternative.
- [x] **FX44** (agy-PM minor) — UJ3.7 Skip bullet: Skip is a dedicated queue-management key (binding not specified here). Covered by FX1's wording; tick with FX1.
- [x] **FX45** (PM nit `:150`) — UJ2 step 5: reference UJ1 step 2's duplicate-name branch.
- [x] **FX46** (PM "missing for the requirements pass") — Add a `### Notes for the requirements pass` bullet list at the end: success metrics to instrument the thesis (UI overhead per row above the instrument's scan cycle; deferred and rework rate per session; zero mis-attributed readings on a Demo Device stress run; items per hour shown in the session summary). Not a journey change.
- [x] **FX47** (Staff clarifying questions 1–20) — Verification aid, no edit: Q1→FX1/F6 · Q2→FX11/F12 · Q3→FX11 (yes, one interrupted each) · Q4→FX11 (new start, full gate) · Q5→FX3 · Q6→FX5/F9 · Q7→FX10 · Q8→FX2/F7 · Q9→FX14/F8 · Q10→FX9/F11 · Q11→FX13 · Q12→FX19 (yes, Pause discards) · Q13→FX19 (yes) · Q14→FX25 · Q15→FX21 · Q16→FX32 · Q17→FX6 · Q18→FX17 (owner's call, recorded in the OQ) · Q19→FX33 · Q20→FX30 (Data Foundation constraint; OQ stands).
- [x] **FX48** (housekeeping) — Update the surfaced-questions list (remove SCAN_TIMEOUT and N_CONSEC_DRIFT; add K_FAILED_ATTEMPTS, guard-mode default, ROW_CONFIRM_BUDGET, ROWS_TARGET/CEILING, the SAMPLE_TOLERANCE statistic, the device-PRD cursor note), the constants bullet, and the assumptions list (note fences F6–F13 with a link to the fence file). Re-run the mermaid parser on every block and the anchor check; every changed anchor updated.

## Verification (item-for-item)

Every finding in the log maps to a box above: PM (B→FX1; M→FX5, FX8, FX6, FX7, FX9; minors→FX14, FX23, FX24, FX25, FX26, FX27, FX28, FX29, FX20; nits→FX45, FX20; missing→FX46). Staff (S1→FX2; S2→FX3; S3→FX1; S4→FX14; S5→FX11; S6→FX5; S7→FX10; S8→FX9; S9→FX13; S10→FX30; S11→FX6; S12→FX17; S13→FX31; S14→FX25; S15→FX21; S16→FX15/FX16/FX19; S17→FX32; S18→FX33; nits→FX34–FX37; questions→FX47). Test (T1→FX1; T2, T3→FX2; T4→FX15; T5→FX16; T6→FX17/FX14; T7→FX13; T8→FX18; T9→FX4; T10→FX9; T11→FX5/FX6; T12→FX19; T13→FX26; T14→FX38; T15→FX11/FX18; T16→FX39; T17→FX1; T18→FX10; T19→FX40; T20→FX18). Arch (A1→FX4; A2→FX3; A3→FX10; A4→FX12; A5→FX21; A6→FX11; A7→FX6; A8→FX19; A9→FX14; A10→FX3; A11→FX34; A12→FX41). agy-PM (Blocker→rejected R1-F22; UJ4.1→FX22; UJ3.10→rejected R1-F23; Reading A→FX29; Skip→FX44). agy-Staff (Blocker→FX14; trigger→FX20; resume→FX4; complete→FX6; nit→FX42). agy-Arch (Blocker→FX4; Reading A reorder→FX21; insert→FX43). agy-Test: rejected R1-F25.
