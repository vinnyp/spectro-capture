# Capture Mode PRD — round 22 fixes

Resume point for round 22 (log: "Round 22"). Fences F1–F48 are the authorization; F45/F46 bind every edit. Tick each box with a one-line note. Statuses are bare Legend values.

- [x] **FX22-0** — Rows every round-22 lens ALIGNed and no fix below edits flip to 🤝 Aligned: E29, E30, E31, E33. Rows a fix below edits stay or go to "⌛️ Ready for Alignment": R5.9, R8.4, R11.8, R11.12. Report final counts. — E29/E30/E31/E33 → 🤝; R11.8 → ⌛️ (edited by FX22-2); R5.9, R8.4, R11.12 held at ⌛️; nothing else touched.
- [x] **FX22-1** (T22-1, T22-2, A22-2) — §11's script, step 4 becomes: "still enabled, in the review: N_CONSEC_HARD rows moved on from by each of [R5.9]'s three routes pause it, and rows it excludes — a "Skip" with no attempt, a row left set aside with no attempt — do not ([R8.4])". One sentence; the constant, not "two"; nothing else in the step changes. — step 4 replaced verbatim, both cites carrying their §-anchors; the step's trailing semicolon and every other step unchanged.
- [x] **FX22-2** (T22-3) — R11.8: the first sentence ends "…behave identically, and a test can set a generated set's spread against SAMPLE_TOLERANCE ([R4.9])" so an agreeing, a boundary, and a disagreeing set are each producible on the Demo Device. The row stays at its sentence count; the inherited-obligation parenthetical stays. — clause appended inside the existing sentence with `[R4.9](#4-the-scan-loop)`; row still one rule sentence plus the inherited-obligation parenthetical.
- [x] **FX22-3** (A22-1, I22-1, T22-4) — Fence file, F47's map: "the copy file's header, its state-level marks (E29–E34), and its action-level marks (E2, E18, E28, E36, E37, E42)" in place of "the copy file's header and E29–E34". — replaced in the F47 bullet of the fence → row map; the copy-file link kept, the E ids plain as their neighbours are.
- [x] **FX22-4** (I22-2) — Fence file, F48's Decision: "counting consecutive rows the instrument set aside by any route:" → "counting consecutive rows set aside by any of these routes:", followed by "(gloss amended round 22 to match R5.9)". The Why paragraph is history and stays. — gloss amended and the parenthetical placed on it, before the colon that introduces the three routes; the routes, the rest of the Decision, and the Why are untouched.
- [x] **FX22-5** (I22-3) — Copy file preamble: "Every promise made below is backed by a requirement row in the PRD" → "Every promise made below is backed by a requirement row in the PRD, except a state this PRD hands over whole ([E29](#error--state-copy))". No new sentence. — the existing sentence gained the exception clause; sentence count unchanged.
- [x] **FX22-6** (T22-5, A22 nit) — Surfaces table, the three cells that list "QC or correction" (Collection, Re-scan, Capture surfaces): "QC or correction ‹QC & Comparison PRD›", matching the copy file's name cell. — the mark added inside the three existing cells; no column, no row, no other cell changed.
- [x] **FX22-7** (I22 nit, A22 nit) — R11.12: "([E18] and [E42]'s re-scan, [E33])" → "([E18]'s and [E42]'s re-scan, [E33])". Legend priority paragraph: "so [R11.12]'s states, [R11.15]'s surface lists and [R7.19]'s entry points are asserted per phase". — both applied; R11.12 gained one apostrophe and no words, so its sentence count is unchanged.
- [x] **FX22-8** (checks) — citations and anchors resolve; no `&amp;`; both mermaid files render; every requirement row at most two sentences; counts reported. — all 180 R/E/M cites resolve to a live row and every in-repo `#anchor` to a GitHub-slug heading, the only misses being the two links into the device PRD's pre-existing `&amp;` §7 heading (out of scope); no `&amp;` in the three capture files; both mermaid-bearing files render (6 blocks); 150 requirement rows all ≤ 2 sentences.

Map: T22-1/T22-2/A22-2→FX22-1 · T22-3→FX22-2 · A22-1/I22-1/T22-4→FX22-3 · I22-2→FX22-4 · I22-3→FX22-5 · T22-5/A22 Surfaces nit→FX22-6 · I22 nit/A22 Legend nit→FX22-7 · T22 post-lock (R4.9 boundary rides with FX22-2; the review's "Leave it set aside" with no attempt as a test-matrix item for the engineering plan; E29 adoption by the QC & Comparison PRD)→post-lock list.

**After the pass:** PRD 19,614 words; 157 🤝, 4 ⌛️ (R5.9, R8.4, R11.8, R11.12) over 161 R and M rows; copy 44 🤝, 0 ⌛️.
