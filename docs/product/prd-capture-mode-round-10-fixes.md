# Capture Mode PRD — round 10 fixes

Resume point for round 10 (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 10"). One box per new finding from every lens. Fences F1–F43 (F43 and the F42 clarification recorded this round) plus the earlier amendments (F6, F31, F36, F38, F40) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected. Line numbers refer to commit `701ec4e`. WHAT not HOW; the operator's vocabulary; no technical jargon. Read E2, E23, E24, E27, E39 together before editing any one of them: they tell the set-aside story in five places and must agree on what is reversible.

## Status flips (authorized by the round-10 dispositions in the log)

- [x] **FX10-0** — Flip to 🤝 Aligned the 11 rows the log's "Round 10 — Row flips" paragraph lists; return E24, R3.6, R8.3, R3.11 to ⌛️ (re-opened); the 7 held rows stay ⌛️ with "edited in round 10, re-review" where a fix edits them. Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 10, re-review". Report final counts. — **applied.** 10 of the 11 listed rows flipped to 🤝 (R1.7, R4.22, R5.9, R7.5, R7.13, R8.5, R11.9, E25, E27, E43); E2 was on the list but FX10-9 edits it, so it reads "edited in round 10, re-review". R3.6, R8.3, R3.11, and E24 re-opened. Of the 7 held rows, the 5 a fix edits now read "edited in round 10, re-review" (R7.1, R8.1, R11.12, E23, E39); R8.2 and R8.15 were held but untouched by any item, so they keep the round-9 wording they carried in. R7.15 was 🤝 and FX10-7 edits it, so it returns to ⌛️ as the log expected. No new rows. Final: 186 🤝, 13 ⌛️ of 199 (144 R, 44 E, 11 M).

## Blocker

- [x] **FX10-1** (Test R10-T1, Staff R10-F2, Arch A10-1; **F42 clarification (1)**) — R8.1 (`:775`): a scan from the browse-opened list is a re-scan by choice and leaves the collection's remembered row where it was, so the next bulk session still opens where the operator stopped (R3.11); the browse's full non-cost is stated — no gate, no session, no change to the remembered row. R3.6 (`:655`) and R8.3 (`:777`): the remembered-row update and the review-selected mark apply to a row selected in a review entered inside a session, not to a browse. R3.11 (`:660`): cite R8.1 so the two read as one rule. — **done.** R8.1 now says the browse costs nothing at all: no gate, no session, and the collection's remembered row stays where it was, so the next bulk session still opens where the operator stopped. A scan from it is "a re-scan the operator chose", and the row closes with the rule stated from that side too — the remembered row does not move either way, so scanning a swatch again from the list never changes where the next bulk session opens. R3.6 scopes its review-selected update to "a review the operator entered inside a session" and says the browse is not one of those. R8.3 splits the two: selected inside a session it becomes the current item and the remembered row, marked review-selected; selected in the browse-opened list it goes under the instrument without becoming the remembered row. R3.11's closing sentence names the browse-opened scan as governed by the same rule. All three cite fence F42 as clarified.

## Majors

- [x] **FX10-2** (Staff R10-F1; **F42 clarification (2)**) — R8.1 (`:775`): mirror R8.7's carve-out — with no session open on the collection the scan runs as a one-row session with the pre-flight gate (R3.10, R3.11); where the collection's bulk session is paused it is a row inside that session (R9.6): the pause lifts for that row and the queue returns to the row it left, still paused. Say that the browse itself has nothing to discard (no session, no partial set), so the entry rule of R7.1 / fence F19 is about the in-session review only. — **done.** R8.1's scan clause now follows the session the collection already has, the same way a re-scan reached from the collection does (R8.7): with no session open it is a one-row session and the pre-flight gate runs (R3.10, R3.11); where the collection's bulk session is paused it is a row inside that session rather than one of its own (R9.6) — the pause lifts for that row and the queue is back where it left, still paused. The row also says a browse has nothing to discard (no session behind it, no part-finished swatch), so the discard-on-entry rule further down is about the review opened inside a session (R7.1, fence F19).
- [x] **FX10-3** (PM PM10-2, Test R10-T2, Arch risk note) — R11.12 (`:860`): the set-aside-list clause lists, for each row, whether it is settled and the decision and note it was given (R8.2, fence F42), and which of the leave actions the list is offering — none on a settled row, "Leave them all set aside" only while something is outstanding (R8.5, R8.15) — so F42 is listed rather than reasoned about. — **done.** R11.12's set-aside-list clause now lists, per row, its cause, attempt count, samples kept, whether it is settled, and where it is the decision and the note it was given (R8.2, fence F42) — and which of the two leave actions the list is offering: neither on a settled row, and "Leave them all set aside" only while some row is still outstanding (R8.5, R8.15). F42's conditions are listed rather than reasoned about.
- [x] **FX10-4** (PM PM10-1, PMM PMM10-1 Blocker, Test R10-T3, Staff R10-F4) — E39 (`:913`) body: "They stay in your collection and in this list, marked, with everything the instrument did read. Add one note for all of them if you like. That settles everything still outstanding — there's nothing here waiting on you any more — and if you're in a session, it finishes it." — **done.** E39's body is the fix file's wording verbatim, so "clears the list" is gone: the rows stay in the collection *and in this list*, and what the action does is settle everything still outstanding rather than empty anything. R8.2's promise that the list shows every set-aside row settled or not, and E27's corrected body, now agree with it.
- [x] **FX10-5** (Staff R10-F3) — R7.1 (`:750`): ending a session early (R7.5, E23) is named beside Pause as the second deliberate exception to the discard reason — nothing goes under the instrument, but the swatch is put down and the room is left — so "the one deliberate exception" becomes the two. — **done.** R7.1's exception sentence names two: "Pause", and ending the session early (R7.5, E23, UJ3.4). Neither puts anything under the instrument, but each is where the swatch is put down and the room is left, so a part-finished set is not held across either — "Resume" starts the row from its first sample, and so does the next session after an early end.
- [x] **FX10-6** (PMM PMM10-2, Test R10-T4) — E23 (`:897`): the set-aside sentence reads "⟨deferred⟩ swatches are set aside. You can deal with them now, another day, or just look back over them whenever you like." so it is true when every one of them is already settled. — **done.** E23's set-aside sentence is the fix file's wording, so it stays true when every set-aside row is already settled: they can be dealt with now, another day, or just looked back over. Its action cell's condition is untouched.
- [x] **FX10-7** (PMM PMM10-3, Arch A10-3; **fence F43**) — E24 (`:898`) body: "⟨deferred⟩ swatches are set aside for good — still there, still marked, and you can scan any of them again whenever you like." No new action. R7.15 (`:764`): one sentence — a state shown on the collection surface never hides that surface's entry points, so the door of R8.1 is there while the summary shows. — **done.** E24's body says the swatches set aside for good are "still there, still marked, and you can scan any of them again whenever you like", and its action cell is unchanged (fence F43 — no second home for the door). R7.15 states the rule once: a state the app shows on the collection surface never hides that surface's own entry points, so the way into the set-aside swatches is there while the summary is up, and its test clause now lists those entry points beside the summary either way.

## Minors

- [x] **FX10-8** (Arch A10-2) — R8.1 (`:775`): "governs only the ways in that happen on their own — the queue running out, and a session opening straight into the list —" loses the enumeration (a resume onto a review-selected row, R3.8, is a third and is governed too): "governs only the ways in that happen on their own, and never whether the offer is there". — **done.** R8.1's enumeration is gone: "governs only the ways in that happen on their own, and never whether the offer is there". The resume onto a review-selected row (R3.8) is no longer left out of a list that claimed to be complete.
- [x] **FX10-9** (agy PMM10-1, operator residue) — E2 (`:876`) and E23 (`:897`) word the review action's condition the same way: "Review the set-aside swatches — only while any swatch is set aside". R1.7 (`:604`) quotes it if it quotes the condition. — **done.** E2's finished-variant action now reads "Review the set-aside swatches — only while any swatch is set aside", word for word as E23's does. R1.7 was left alone: it states the condition as prose ("wherever the collection holds a set-aside row at all, on the one condition that offer carries everywhere") rather than quoting the action cell, which is what the item conditioned its edit on — so R1.7 keeps its flip to 🤝.
- [x] **FX10-10** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph still lists every control named by a row; Traceability paragraph (`:572`) names F43 and the F42 clarification; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Rows: 144 R, 44 E, 11 M (199), no duplicate or renumbered ID. Statuses: 186 🤝, 13 ⌛️ (4 re-opened in round 10, 7 edited in round 10, plus R8.2 and R8.15 held untouched at their round-9 wording); every row carries exactly one. §12's Labels paragraph still writes every control a row names — no item added an action, and E24 gained none. The Traceability paragraph reads F1–F43, names F43 as this round's new fence, and lists fence F42 as clarified beside F6, F36, F38, and F40. Six mermaid blocks, byte-identical to HEAD. No `&amp;`. No broken same-file anchors.

## Item-for-item map (verified against each review)

PM: PM10-1→FX10-4 · PM10-2→FX10-3 · R8.1/E33 third-place risk→log only. Staff: R10-F1→FX10-2 · R10-F2→FX10-1 · R10-F3→FX10-5 · R10-F4→FX10-4 · Q1–Q2→FX10-2 · Q3→FX10-1 · Q4→FX10-5 · Q5→FX10-4. Test: R10-T1→FX10-1 · R10-T2→FX10-3 · R10-T3→FX10-4 · R10-T4→FX10-6 · E24 "Open the collection" residue→log only. Arch: A10-1→FX10-1 · A10-2→FX10-8 · A10-3→FX10-7 · remembered-row single-writer note→FX10-1. PMM: PMM10-1→FX10-4 · PMM10-2→FX10-6 · PMM10-3→FX10-7. agy: staff's two Majors→FX10-2, FX10-4 · PMM10-1→FX10-9 · PMM10-2→rejected R10-X1 · PMM10-3→overruled F43.
