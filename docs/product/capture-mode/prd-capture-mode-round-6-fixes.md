# Capture Mode PRD — round 6 fixes

Resume point for round 6, the delta verification of round 5 (log: `../../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 6"). Every round-5 finding verified RESOLVED or RESOLVED-BY-FENCE by its own lens on both routes, with three PARTIALs closed below. One box per new finding from every lens. Fences F1–F38 (F36–F38 and the F31 clarification are this round's) are the authorization. Tick each box as it lands. Row IDs never renumber; new rows take the next number. Line numbers refer to commit `425038e`.

## Status flips (authorized by the round-6 dispositions in the log)

- [x] **FX6-0** — Flip to 🤝 Aligned every R, E, and M row that no Claude lens OBJECTed to in round 6 and at least one ALIGNed (the log's "Round 6" section lists the 35 rows that stay ⌛️; every other row flips). Rows this round's fixes touch (below) stay ⌛️ even if they were in the flip set — a row edited after alignment must be re-reviewed; note "edited in round 6, re-review" in its Status cell. Report the final counts per status.
  - **Done.** 144 rows flipped to 🤝 Aligned; 36 stay ⌛️ per the log's enumerated list (the log's prose says 35 — the list carries 36 names, and the list was followed); 15 rows the fixes touched return to ⌛️ with "edited in round 6, re-review" (E19, E32, E39, E41, R1.5, R4.4, R4.9, R4.14, R4.17, R5.7, R5.10, R6.2, R7.11, R8.5, R11.1); new row E42 is ⌛️, never reviewed. Rows now 196 (was 195).

## Blockers

- [x] **FX6-1** (R6-F1 · Test T1 Blocker) — R11.12 (`:856`): widen to every capture-owned surface — the capture surface, the queue list and find field, the set-aside list, the re-scan surface, the add-item form, the session-summary state — and, for any §12 state, which conditional lines and actions are present; keep "without matching wording". R8.3, R8.7, R7.15 keep their citations. Add the import preview's three-way count and the set-aside list's contents (R2.9, R2.13, R2.14, R8.2) to what a test can enumerate.
  - **Done.** R11.12 rewritten: every capture-owned surface enumerated (capture surface incl. the dropped-samples line, queue list and find field, set-aside list with each row's cause/attempts/samples and its header line, re-scan surface, add-a-swatch form, session-summary state), plus which conditional lines and actions of whichever §12 state is up; import preview's three-way count, untouched-rows line and issue list added (R2.9, R2.13, R2.14, R8.2 cited). "without matching any wording" kept; R8.3, R8.7, R7.15 citations intact. No new IDs.
- [x] **FX6-2** (R6-F2 · Staff S6-F1 Blocker; **fence F36**) — R4.12 (`:680`): the fallback basis without the spectral entitlement (mean across colour values under the F28 reference; basis recorded; reading marked non-spectral); R1.5 and R4.9 cite it; the Data Foundation hand-off carries the non-spectral mark.
  - **Done.** R4.12 carries the colour-value fallback under the F28 reference, the recorded basis (curves or colour values) and the non-spectral mark, citing device PRD §2; R1.5 and R4.9 cite it; the closing hand-off paragraph carries the mark to Data Foundation. No new IDs.
- [x] **FX6-3** (R6-F3 · Staff S6-F2 Blocker) — R11.9 (`:853`): the interleaved case "must not pause the session while accepted samples intervene, and still auto-defers once its own failed attempts reach K_FAILED_ATTEMPTS"; R5.4 (`:704`): drop "consecutive" in favour of "that many failed attempts on one row, counted across any accepted samples in between". Fold in fence F37's withdrawal of the N_CONSEC_HARD < K invariant (R11.9's order reason becomes: the guard is walked with a run of rows, not one row).
  - **Done.** R11.9's interleaved case now reads "must not pause the session while accepted samples intervene and must still auto-defer once its own failed attempts reach K_FAILED_ATTEMPTS"; its order reason is now "one swatch for a row's own count, a run of swatches for the guard" and the N_CONSEC_HARD < K invariant sentence is gone (F37). R5.4 drops "consecutive" for "that many failed attempts on one row — counted across any accepted samples that fell in between". No new IDs.

## Majors

- [x] **FX6-4** (R6-F4 · Arch A-N1, PMM-2; **F31 clarification**) — R8.5, R8.15: a deliberately-left row is settled. R1.7 (`:601`), R3.9 (`:655`), R3.7 (`:653`), UJ3 step 1: routing and completion treat settled rows as adjudicated — a collection with nothing pending and every set-aside row settled is finished; starting capture there shows E2's finished variant ("Every swatch here is scanned or set aside for good") and does not reopen the review; unsettled set-aside rows still route to the review. E24 (`:894`): the deferred line reads "⟨deferred⟩ are set aside for good" and its action is offered only when unsettled rows remain. R8.13 / R11.11: the settled mark is kept and readable.
  - **Done.** R8.5 and R8.15 state that a left row is settled (adjudicated, never reopens the review, still re-scannable). R1.7, R3.9, R3.7 and UJ3 step 1 route on "unsettled"; E2's finished variant reads "Every swatch here is scanned or set aside for good" (headline "You're done with ⟨collection⟩"). E24's line reads "⟨deferred⟩ swatches are set aside for good" with the review action only while one is unsettled. R8.13 and R11.11 keep and read back whether a decision settled the row. Also carried into R7.11, UJ3.5's two bullets and two state-diagram edges for consistency (R7.11 returns to ⌛️). No new IDs.
- [x] **FX6-5** (R6-F5 · Arch A-N3, Staff S6-F3, Test T4) — R11.3 (`:847`): remove the false closed-list claim; restate as inherited notes for the device PRD §6 — its exception list gains the trigger source (gated on OQ 2) and its configurable-latency row moves to the first build phase — and add both to the closing hand-off paragraph (`:914`). R11.1 stays true ("within the closed list, as amended by this note").
  - **Done.** R11.3 no longer claims the trigger source is on the device PRD's closed list; it hands two inherited notes for device PRD §6 (trigger source added to the list, gated on OQ 2; configurable-latency row pulled into the first build phase), both repeated in the closing hand-off paragraph. R11.1 now reads "within the device PRD's closed list of exceptions as amended by the notes this PRD hands it". No new IDs.
- [x] **FX6-6** (R6-F6 · Arch A-N4; **fence F37**) — R5.9 (`:709`): N_CONSEC_HARD counts consecutive rows that ended in an instrument-caused deferral; repeated failures on one row auto-defer it without pausing; R5.4's auto-defer path is reachable with the guard enabled; E19's ⟨n⟩ is rows for both counters; R11.9's walk order updated; OQ 3 notes the counters may merge.
  - **Done.** R5.9 rewritten: both counters count consecutive rows the instrument set aside, repeated failures on one swatch auto-defer it without pausing, R5.4's auto-defer path stated reachable with the guard enabled, the merge possibility recorded against OQ 3. E19's body now counts swatches for both counters; the UJ3.1 flowchart's guard node moved after the row's own K_FAILED_ATTEMPTS check and reworded to rows. No new IDs.
- [x] **FX6-7** (R6-F7 · Arch A-N5; **fence F38**) — R10.7 (`:833`): PROTOTYPE_RUNS (candidate 3, OQ 8), alternating order; fewer slips across all runs wins; tie → fewer steps-to-row; PROTOTYPE_PARTICIPANTS = 1 stands (F35). OQ 8's closer matches.
  - **Done.** R10.7 carries PROTOTYPE_RUNS (new named provisional constant, candidate 3, OQ 8), alternating order, fewer slips across all runs wins, tie to fewer steps-to-row, no single slip decides; PROTOTYPE_PARTICIPANTS = 1 stands (F35). OQ 8's closer matches. New constant: PROTOTYPE_RUNS. No new row IDs.
- [x] **FX6-8** (R6-F8 · PM6-1) — E20 (`:890`): two variants under one id — set-aside ("⟨code⟩ is already set aside") and still-pending ("⟨code⟩ is still waiting in the queue — you'll come back to it. There's nothing here to flag."); R5.7 names both; R11.12 exposes the variant.
  - **Done.** E20 written in two variants under one id — already-set-aside and still-pending ("⟨code⟩ is still waiting in the queue" / "You'll come back to it when the queue comes round. There's nothing here to flag."). R5.7 names both and ties the second to a Skip before any attempt (R5.5, R6.4); R11.12 lists the variant. No new IDs.
- [x] **FX6-9** (R6-F9 · PM6-2) — R4.21 (`:689`): until the trigger is pressed on the new current row, "Re-take sample" and "Restart item" target the row that just landed and answer with E36 naming that row (the F16 boundary); after that press they act on the current set.
  - **Done.** R4.21 states that until the trigger is pressed on the new current row both actions target the row that just landed and answer with E36 naming it (the F16 boundary, citing R5.6), and act on the current set after that press. No new IDs.
- [x] **FX6-10** (R6-F10 · PM6-3, PMM-6) — R7.1 (`:746`) and R11.12: the capture surface (and the set-aside list header on review entry) carries a "⟨dropped⟩ samples on ⟨code⟩ let go" line after any discard, listed among the enumerated fields; a jump, review entry, and a completed Add item therefore disclose the count; R8.1, R6.2, R9.10 cite it. Add the line's wording to §12 as a state row (E42) so copy honesty holds.
  - **Done.** New state row **E42** ("Samples let go", headline "⟨dropped⟩ samples on ⟨code⟩ let go"). R7.1 carries the line on the capture surface and in the set-aside list header; R8.1, R6.2 and R9.10 cite it; R11.12 lists it among the capture surface's enumerated fields; added to the Surfaces table for the capture surface, the set-aside list and the add-a-swatch form. **New ID: E42.**
- [x] **FX6-11** (R6-F11 · PMM-1) — R6.3 (`:725`), R9.10 (`:813`): "Add a swatch"; R8.15 (`:777`): "Leave them all set aside"; sweep every row-quoted label against §12's Labels rule (`:864`); add "Start capture session" to the labels list.
  - **Done.** "Add item" is gone from the document (R6.3, R9.10, R4.4, R4.17, R4.18, R7.1, UJ3.7, UJ4.1, the UJ4 flowchart, E32's name, the Surfaces table) in favour of "Add a swatch"; R8.15 and UJ3.3 use "Leave them all set aside". §12's Labels rule now also fixes the wording of the controls that belong to no state: "Start capture session", "Add a swatch", "Re-take sample", "Restart item", "Skip", "Flag", "Pause", "Resume", "End session", "Review the set-aside swatches". No new IDs.
- [x] **FX6-12** (R6-F12 · PMM-3) — E18 (`:888`): a re-scan variant under the same id — body "Your existing reading stays as it is unless you accept a new one", actions "Take it again; Accept the average; Abandon the re-scan"; R8.10 (`:788`) cites the variant; R11.12 exposes it.
  - **Done.** E18 written in two variants under one id; the re-scan variant's body is "Your existing reading stays as it is unless you accept a new one" with actions "Take it again; Accept the average; Abandon the re-scan". R8.10 cites the variant and quotes the three labels; R11.12 lists it. No new IDs.
- [x] **FX6-13** (R6-F13 · PMM-4) — §12 Placeholders (`:866`): scope the ⟨code⟩ rule — in a state that says where the operator will resume (E22, E23, E25) it is the row they will land on; elsewhere it is the swatch the sentence is about (E17, E20 name the row just left).
  - **Done.** The §12 ⟨code⟩ rule is scoped: in E22, E23 and E25 it is the row the operator will land on (the R3.7 fallback); elsewhere it is the swatch the sentence names, which in E17, E20, E36 and E42 is the row just left. No new IDs.
- [x] **FX6-14** (R6-F14 · Test T2) — R11.13 (`:857`): the app records every capture shortcut and control interaction, whoever delivered it, with the surface that had focus and the accepted / rejected / ignored outcome, readable after the run (R11.11); R10.7 and M11 cite that record.
  - **Done.** R11.13 now records every capture shortcut and control interaction, whoever delivered it, with the surface that had focus and the accepted/rejected/ignored outcome, readable after the run (R11.11), so a human prototype run leaves the same evidence a test does; R10.7 cites that record. M11 already cited R11.13 and was left unedited so its flip status is not disturbed. No new IDs.
- [x] **FX6-15** (R6-F15 · Test T3) — R8.13 (`:791`) and R11.11 (`:855`): every sample, attempt, guard pause, discard, and review decision records the session it belongs to and its time, so M4, M5, and M9 are computable from the read-back; M4/M5/M9 cite it.
  - **Done.** R8.13 records the session and the time on every sample, attempt, guard pause, discard and review decision, naming M4/M5/M9; R11.11 reads them back per session and per chain; M4, M5 and M9's Method columns cite it. No new IDs.

## Minors and nits

- [x] **FX6-16** (Arch A-N2) — R3.7 (`:653`): the fallback ends "…or into the set-aside list per R3.9 when unsettled set-aside rows remain, or the nothing-to-capture state".
  - **Done.** R3.7's fallback now ends into the set-aside list per R3.9 while any set-aside row is unsettled, or the nothing-to-capture state (R1.7) when every one has been left. No new IDs.
- [x] **FX6-17** (Arch A-N6) — R4.18 (`:686`): drop "Re-take sample" and "Restart item" from the in-flight action list and cite R4.21.
  - **Done.** R4.18's in-flight action list is Flag, Skip, Pause, a jump, Add a swatch; "Re-take sample" and "Restart item" are named as rejected outright in flight, citing R4.21. No new IDs.
- [x] **FX6-18** (Arch A-N7, Test T5) — R3.12 (`:658`): bind SESSION_LENGTH — a session of SESSION_LENGTH at ROWS_TARGET, paced at DEMO_SCAN_CYCLE, ends with tallies, elapsed capture time, and queue order intact and TRIGGER_ACK_WINDOW still met on its last row.
  - **Done.** R3.12 binds SESSION_LENGTH: a session of that length at ROWS_TARGET, paced at DEMO_SCAN_CYCLE (R11.3), ends with tallies, elapsed capture time and queue order intact and TRIGGER_ACK_WINDOW (R4.8) still met on its last row. No new IDs.
- [x] **FX6-19** (Staff S6-F4) — R9.9 (`:812`): scope "no flag-after-landing window" to a one-row session; a capture inside a paused bulk session keeps R5.6's window.
  - **Done.** R9.9 scopes the missing window to an item captured in a one-row session and states that a capture inside a paused bulk session is a row of that session (R9.6) and keeps R5.6's window. No new IDs.
- [x] **FX6-20** (Staff S6-F5) — Capture-workflow diagram (`:44`): "Collection ready (queue in queue order; pending rows to capture)".
  - **Done.** The capture-workflow diagram's Ready state reads "Collection ready (queue in queue order; pending rows to capture)". No new IDs.
- [x] **FX6-21** (PMM-5, log note) — Closing hand-off paragraph: an inherited glossary note for the device PRD — this PRD's copy says "the instrument" where the device PRD's halt copy on the same surface says "the device"; one noun should win when the device PRD is next amended.
  - **Done.** The closing hand-off paragraph carries the glossary note: this PRD says "the instrument" where the device PRD's halt copy on the same surface says "the device", and one noun should win when that document is next amended. No new IDs.
- [x] **FX6-22** (nits) — R5.10's recipe adds "with the guard enabled"; R4.14 "caution tone" (drop "or error"); E41 body "code or name"; E39 headline "the remaining ⟨n⟩ swatches"; UJ3.3 step 1 label "Review the set-aside swatches"; Staff Q19 answered in the log (amending 🤝 device-PRD rows goes through inherited notes and OQ 16, owner-approved).
  - **Done.** R5.10's recipe adds "with the guard enabled"; R4.14 reads "A caution tone always pre-empts"; E41's body reads "own code or name"; E39's headline reads "the remaining ⟨n⟩ swatches"; UJ3.3 step 1 uses "Review the set-aside swatches". Staff Q19 needs no document change — the log answers it and OQ 16 already carries the inherited-note route. No new IDs.
- [x] **FX6-23** (housekeeping) — Traceability pointer F1–F38; OQ table: OQ 3 notes F37; OQ 8 notes F38 and PROTOTYPE_RUNS; the non-spectral mark in OQ 21's Feeds. Re-run the mermaid parser, anchors, row counts (report old → new), the all-status check (⌛️ + 🤝 only), and the jargon sweep.
  - **Done.** Traceability now reads F1–F38; OQ 3 records F37's withdrawal of the invariant and the possible merge; OQ 8 records F38, PROTOTYPE_RUNS and the tie-break; OQ 21's Feeds gains R4.12 (the non-spectral mark) and its Details the F36 fallback. Verification: 6 mermaid blocks parse; 596 links, all anchors resolve; rows 195 → 196 (R 143, E 41 → 42, M 11); every row status is ⌛️ Ready for Alignment (with or without the re-review note) or 🤝 Aligned; the "Add item / Leave all set aside / closed list of simulated-versus-live exceptions" grep is empty; the jargon sweep is empty.

## Verification (item-for-item)

PM: PM6-1→FX6-8 · PM6-2→FX6-9 · PM6-3→FX6-10 · UJ3.3 label→FX6-22. Staff: S6-F1→FX6-2 · S6-F2→FX6-3 · S6-F3→FX6-5 · S6-F4→FX6-19 · S6-F5→FX6-20 · Q19→FX6-22 · m7 PARTIAL→FX6-5. Test: T1→FX6-1 · T2→FX6-14 · T3→FX6-15 · T4→FX6-5 · T5→FX6-18 · B2 PARTIAL→FX6-1 · M13 PARTIAL→FX6-18 · nits→FX6-22. Arch: A-N1→FX6-4 · A-N2→FX6-16 · A-N3→FX6-5 · A-N4→FX6-6 · A-N5→FX6-7 · A-N6→FX6-17 · A-N7→FX6-18. PMM: PMM-1→FX6-11 · PMM-2→FX6-4 · PMM-3→FX6-12 · PMM-4→FX6-13 · PMM-5→FX6-21 · PMM-6→FX6-10 · PMM2 PARTIAL→FX6-4 · PMM8 PARTIAL→FX6-11 · nits→FX6-22. agy (four lenses): all round-5 items RESOLVED, no new findings.
