# Capture Mode PRD — round 9 fixes

Resume point for round 9 (log: `../../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 9"). One box per new finding from every lens. Fences F1–F42 (F42 and the F40 clarification recorded this round) plus the earlier amendments (F6, F31, F36, F38) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected this round. Line numbers refer to commit `9aa4b0f`. WHAT not HOW; the operator's vocabulary; no technical jargon.

## Status flips (authorized by the round-9 dispositions in the log)

- [x] **FX9-0** — Flip to 🤝 Aligned the 8 rows the log's "Round 9 — Row flips" paragraph lists; return R1.7, E2, R8.2, E27 to ⌛️ (re-opened); the 9 held rows stay ⌛️ with "edited in round 9, re-review" where a fix edits them. Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 9, re-review". Report final counts. — **applied.** 7 of the 8 listed rows flipped to 🤝 (R4.21, R5.7, R9.10, R11.13, E20, E32, E44); R7.1 was on the list but FX9-9 edits it, so it returns to ⌛️ "edited in round 9, re-review". R1.7, E2, R8.2, E27 re-opened. All 9 held rows were edited and now read "edited in round 9, re-review". Five rows a fix edited carry "edited in round 9, re-review" rather than 🤝: R7.1 (from the flip list) plus R8.5, R8.15, E25, E39, which were 🤝 before. No new rows were needed. Final: 181 🤝, 18 ⌛️ of 199 (144 R, 44 E, 11 M).

## Majors

- [x] **FX9-1** (Staff R9-F1, Test R9-T2, Arch risk note; fence F39) — E2 (`:876`) finished variant gains a conditional action: "Review the set-aside swatches — only while the collection holds a set-aside row". R1.7 (`:604`): "the three actions E2 writes for that variant" becomes those three plus the conditional review action, pointing at R8.1. R11.12 (`:860`): the surface enumeration adds the collection surface with its entry points, so the offer's presence and absence there are things a test lists; R8.1 (`:775`) says both are observable on the collection surface. Surfaces table Collection row (`:581`): "review deferred rows" → "review the set-aside swatches". — **done.** E2's finished variant gains the conditional action "Review the set-aside swatches — only while the collection holds a set-aside row", and its body a count sentence — "⟨deferred⟩ swatches are set aside — you can look back over them whenever you like" — that drops out at zero by the Placeholders rule, so the action never appears without copy behind it. R1.7 names that fourth action, cites R8.1 and fence F39 for the condition, and says a finished collection is where the door matters most. R11.12's surface enumeration adds the collection surface with its entry points, so the offer being there and the offer being gone are each listed on a finished collection like any other. R8.1 says the same from its side. The Surfaces table's Collection row now reads "review the set-aside swatches".
- [x] **FX9-2** (Staff R9-F2, Staff Q2–Q4; **fence F42**) — R8.2 (`:776`): the list shows every set-aside row and, where a row was settled, its decision and note. R8.5 (`:779`) and R8.15 (`:781`): the leave actions are offered only on rows not yet settled. R8.1 (`:775`): choosing "Review the set-aside swatches" from the collection opens the list as a browse — no pre-flight gate, no session — and scanning a row from it runs as a one-row session (R3.11) with the gate; a session that opens into the review on its own (R3.9) is unchanged. E39 (`:913`) "This finishes the session" stays true inside a session; add the words that make it true from a browse (it finishes the review), in the operator's register. — **done.** R8.2 now lists every set-aside row settled or not, and where one was left for good shows the decision and its note, so a dealt-with row reads as dealt with; a test lists the rows with each one's settled state, decision, and note. R8.5 and R8.15 are each offered only on rows not yet settled — "Leave them all set aside" is not there at all with nothing outstanding, and neither action touches a settled row. R8.1 says choosing the review from the collection opens the list to look through and not to scan from: no gate, no session (fence F42); scanning a row from there is the one-row session with the gate (R3.10, R3.11), and a session that opened into the list already ran the gate (R3.9). E39's "This finishes the session" becomes "That clears the list, and if you're in a session it finishes it". The Surfaces table's set-aside-list row carries the decision and note too.
- [x] **FX9-3** (Test R9-T1; **F40 clarification**) — R5.9 (`:713`): the floor is the guard's, not one counter's — no guard counter is ever below 2, because one auto-deferred row counts toward both; keep "That floor is what makes the next promise hold" true. OQ 3 (`:950`): record the floor for both counters. R11.9 (`:857`): cite the guard floor rather than N_CONSEC_HARD's alone. — **done.** R5.9: neither counter is ever below 2, whatever OQ 3 tunes them to (fence F40 as clarified) — the floor belongs to the guard rather than to one counter, because an auto-deferred row counts toward both, so a floor on one alone would leave the other free to pause on that single row; "That floor is what makes the next promise hold" is left standing and is now true of both. OQ 3 records the bound the same way. R11.9's interleaved step rests on the guard's floor rather than N_CONSEC_HARD's.
- [x] **FX9-4** (PM PM9-1) — E23 (`:897`) is a prompt the operator can back out of ("Keep scanning"), and under fence F41 the samples are still there while it is up: the dropped-samples sentence goes to the future — "the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ will be let go" — and R7.5 (`:754`) says the summary names how many samples will be let go on confirming, and were let go once it ends. E22 (`:896`) stays in the past tense: Pause discards at once. — **done.** E23's dropped-samples sentence goes to the future: "the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ will be let go when you stop." R7.5 says the summary names how many and off which swatch in the future while it is still a question the operator can answer with "Keep scanning" — under fence F41 the samples are still there until the session ends — and in the past once it has. E22 is untouched: Pause discards at once.
- [x] **FX9-5** (PMM PMM9-1) — E23 (`:897`) and E25 (`:899`): a second variant for the case where the next session opens into the set-aside list rather than at a row (nothing pending, an unsettled set-aside row waiting — R3.7, R3.9): "Your next session opens straight into the swatches you set aside." / "You'll start again in the swatches you set aside." R7.5 (`:754`) and R7.13 (`:762`) name that case. Placeholders paragraph (`:870`): where the fallback rule picks the set-aside list rather than a row, the state shows that variant and no ⟨code⟩. — **done.** E23 and E25 each gain a second variant of their closing sentence for the session that opens into the set-aside list rather than at a row: "Your next session opens straight into the swatches you set aside." and "You'll start again in the swatches you set aside." R7.5 names that case in its resume clause; R7.13 says the resume state says so and names no row because there is none to name. The Placeholders paragraph adds the rule: where the fallback has no pending row to pick, E23 and E25 show that variant and carry no ⟨code⟩. R11.12's variant enumeration names both.
- [x] **FX9-6** (PMM PMM9-2, Staff R9-F3; fence F39) — E27 (`:901`) body: "Add a note if you like — missing, or damaged. It stays in your collection, marked, with everything the instrument did read. The app won't send you back to it and it won't hold up finishing — you can scan it again whenever you like, from your collection or from the set-aside swatches." — **done.** E27's body is the fix file's wording verbatim: the app won't send you back to it and it won't hold up finishing — you can scan it again whenever you like, from your collection or from the set-aside swatches. The false "It won't come back in the review" is gone.
- [x] **FX9-7** (Staff R9-F4) — E43 (`:917`) shown on a Demo Device session started without spectral data (R4.22) speaks of a license the contributor never has: add a simulated variant whose body says the demo run has no spectral data and what that means for the readings, with no license sentence; R4.22 (`:693`) and R11.12 (`:860`) name the variant. — **done.** E43 is written in two variants, License and Demo Device. The Demo Device variant says the run is set up without spectral data, that the app keeps the colour values it generates rather than the full curve behind them, that these swatches can't be worked out later under a different light, and that each is marked so they can be told apart and scanned again on a run that has it — with no license sentence, because a contributor was never asked for one. R4.22 names the variant; R11.12's variant enumeration lists E43's two.

## Minors

- [x] **FX9-8** (Arch A9-1) — R4.22 (`:693`): replace "that test has nothing to set today, because selecting the Demo Device never goes near licensing at all and the absent-spectral-data outcome sits inside the licensing layer it skips" with OQ 16's wording (`:963`) — the outcome exists in the simulated licensing layer but selecting the Demo Device never invokes that layer, so the Demo Device needs its own switch. — **done.** R4.22 now says what OQ 16 says: the absent-spectral-data outcome does exist in the simulated licensing layer, but selecting the Demo Device never invokes that layer at all, so nothing reaches the outcome by that route — which is why the Demo Device's settable state surface needs a switch of its own.
- [x] **FX9-9** (PM PM9-2) — R7.1 (`:750`): "Any action that does put one there first — the operator's 'Pause', …" — Pause puts nothing under the instrument yet discards; name it as the deliberate exception (R7.3: Resume returns to the row at sample 0) rather than folding it into the one reason. — **done.** R7.1's discard list drops "Pause" and gains it back as the named exception: the operator's "Pause" discards the partial set as well, and is the one deliberate exception to that reason rather than a case of it — nothing goes under the instrument when a session is paused, but a pause is where the swatch is put down and the room is left, so "Resume" starts that row again from its first sample (R7.3, E22).
- [x] **FX9-10** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph (`:868`) still lists every control named by a row; Traceability paragraph (`:572`) names F42 and the F40 clarification; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Rows: 144 R, 44 E, 11 M (199). Statuses: 181 🤝, 18 ⌛️ (4 re-opened in round 9, 14 edited in round 9); every row carries exactly one, and no round-8 status wording remains. The seven status-only flips were checked cell by cell against the previous commit — no body touched. §12 Labels still writes every control a row names; the Traceability paragraph now reads F1–F42, names F42 as this round's new fence, and lists fence F40 as clarified beside F6, F36, and F38. Six mermaid blocks, byte-identical to HEAD. No `&amp;`. No broken same-file anchors.

## Item-for-item map (verified against each review)

PM: PM9-1→FX9-4 · PM9-2→FX9-9 · D50/2° watch→log only. Staff: R9-F1→FX9-1 · R9-F2→FX9-2 · R9-F3→FX9-6 · R9-F4→FX9-7 · Q1→FX9-1 · Q2–Q4→FX9-2 · Q5→FX9-7. Test: R9-T1→FX9-3 · R9-T2→FX9-1. Arch: A9-1→FX9-8 · R1.7/E2 risk→FX9-1 · E20/E44 headline risk→log only. PMM: PMM9-1→FX9-5 · PMM9-2→FX9-6 · not-raised residue→log only. agy: see log.
