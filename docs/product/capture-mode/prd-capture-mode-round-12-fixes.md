# Capture Mode PRD — round 12 fixes

Resume point for round 12 (log: `../../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 12"). One box per new finding from every lens. Fences F1–F43 plus the amendments (F6, F31, F36, F38, F40, F42 (1)–(4)) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected. Line numbers refer to commit `46e9afb`. WHAT not HOW; the operator's vocabulary; no technical jargon. Read R3.1, R3.6, R3.11, R7.1, R7.5, R8.1, R8.3, R8.15, R9.4, R9.6 and E23, E39 together before editing any of them.

## Status flips (authorized by the round-12 dispositions in the log)

- [x] **FX12-0** — Flip to 🤝 Aligned R7.15 and R11.12; return R7.5 to ⌛️ (re-opened in round 12); the 9 held rows stay ⌛️ with "edited in round 12, re-review" where a fix edits them (held rows a fix does not touch keep their current note). Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 12, re-review". Report final counts. — **applied.** R7.15 and R11.12 flipped to 🤝 Aligned; neither is edited by a fix below, so both stay aligned. R7.5 returned to ⌛️ "re-opened in round 12" (FX12-2 also edits it). All 9 held rows carry a round-12 edit, so each now reads "edited in round 12, re-review" (R3.6, R3.11, R7.1, R8.1, R8.3, R8.15, E23, E24, E39). Nothing else moved and no new rows. Final: 189 🤝, 10 ⌛️ of 199 (144 R, 44 E, 11 M).

## Blockers

- [x] **FX12-1** (Test R12-T1, Arch A12-1 — Blocker; PM PM12-1, Staff R12-2, PMM PMM12-3; **F42 clarification (4)**) — Vocabulary (§ Vocabulary, near the **Session** entry `:131`): define **live bulk session** once — a bulk session on the collection that is active and neither paused nor interrupted, its open set-aside review included; distinct from R3.1's elapsed-capture-time accounting, which leaves time in the set-aside list out for its own reason. R8.1 (`:775`): the key becomes "whether a live bulk session is on the collection"; the fourth state is "a live bulk session"; add the one-row-session case to the look-through side — a one-row session already under way (R9.4, R8.7, or a scan started from the list) is still a look-through, the remembered row does not move, and selecting another row ends or abandons that one-row session (R3.11) with its own partial set let go under R7.1 with the cue. R3.6 (`:655`), R8.3 (`:777`): "while a live bulk session is on the collection" / "while none is". R3.11 (`:660`): the closing clause says only that a scan from the set-aside list with no live bulk session never moves the remembered row — whether it runs as a one-row session or, on a paused bulk session, as a row inside that session (R8.1, R9.6). R7.1 (`:750`): both sentences use the new term ("entering the set-aside review while a live bulk session is on the collection"; "opening the set-aside list while a live bulk session is on the collection discards…"). — **done.** The Vocabulary gains one entry beside **Session**: a live bulk session is a bulk session on the collection that is under way and neither paused nor interrupted, the time its set-aside review is open counted as part of that run; a one-row session is never one, however far along it is; it is the one thing that decides how the set-aside list opens; and it is a different idea from §3's elapsed capture time, which leaves time in the set-aside list out for a reason of its own — that number says how much work was done, not which session is running. R8.1's key sentence now turns on "whether a live bulk session is on the collection", its fourth state is titled **A live bulk session**, and one sentence added after the three look-through states says a one-row session already under way changes none of it — with an ad-hoc add, a re-scan, or a scan started from the list still going (R9.4, R8.7) the list is still a look through, the remembered row still does not move, and selecting another row there ends or abandons that one-row session (R3.11) with its own part-finished set let go, cue and all (R7.1). R3.6 reads "while a live bulk session is on the collection" / "while there is none"; R8.3 the same, and its closing clause turns on the new term rather than on the surface. R3.11's closing clause no longer says the scan is governed by the one-row rule: it says only that a scan from the set-aside list while no live bulk session is on the collection leaves the remembered row alone, whether it runs as a one-row session or, on a paused bulk session, as a row inside that session (R8.1, R9.6). R7.1 uses the term in both places — "entering the set-aside review while a live bulk session is on the collection" and "opening the set-aside list while a live bulk session is on the collection discards" — which also closes R12-F4's retired-key sentence. Every citation of the fence now reads "as clarified four times", the Traceability paragraph included. Sentences were replaced rather than added except for the one new R8.1 sentence and the Vocabulary entry, and the four states are still stated only in R8.1.
- [x] **FX12-2** (PMM PMM12-1 — Blocker; Staff R12-1, Test R12-T3) — E23 (`:897`): "…the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ will be let go when you stop, or if you go to the set-aside swatches now." R7.5 (`:754`): the samples are still there until the operator either ends the session or takes the review from the summary, which enters the in-session review and lets them go with the discard cue and the list's header line (R7.1, R8.1, E42, fence F19). — **done.** E23's sample line now reads "…the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ will be let go when you stop, or if you go to the set-aside swatches now." R7.5 no longer says the samples last "until the session actually ends": it says they are still there until the operator either ends the session or takes the review from the summary, and that taking it now opens the review inside the still-running session, which lets them go there and then with the discard cue and, in the list's header, the count and the swatch they came off (R7.1, R8.1, E42, fences F19, F41). The future/past split on the same line is unchanged. R7.5 carries "re-opened in round 12" per FX12-0.

## Majors

- [x] **FX12-3** (Test R12-T2, Arch A12-2; PM PM12-2) — R8.15 (`:781`): where a live bulk session is on the collection and nothing is left pending, "Leave them all set aside" completes that session (R8.6); with rows still pending the queue goes on; opened as a look-through it settles the rows and the collection reads finished (R1.7, E2's finished variant), leaving an ended or unresumed interrupted session as it is. E39 (`:913`): "…and where that was the last thing waiting in a session you're running, it finishes it." — **done.** R8.15 now splits the three cases: where a live bulk session is on the collection and nothing is left pending, "Leave them all set aside" satisfies R8.6's completion condition in a single keypress and that session completes; where one is on the collection with rows still pending the queue simply goes on, the set-aside rows now settled; and where the list was opened to look through it settles those rows and nothing more — no session is finished by it, one already ended or one interrupted and not yet resumed is left exactly as it was, and the collection reads finished once nothing is pending and no set-aside row is unsettled (R1.7, E2's finished variant). E39 now reads "…and where that was the last thing waiting in a session you're running, it finishes it."

## Minors

- [x] **FX12-4** (PMM PMM12-4) — E24 (`:898`) headline: "That's the lot — ⟨captured⟩ swatches in ⟨elapsed⟩"; action stays "Done". — **done.** E24's headline is "That's the lot — ⟨captured⟩ swatches in ⟨elapsed⟩" and its action is still "Done". The body is untouched, so it still opens "That's ⟨rate⟩ an hour" directly under "That's the lot" — flagged for the owner as a copy call, not fixed, because the body is outside this item.
- [x] **FX12-5** (PMM PMM12-5, Staff R12-3, Arch note) — Labels paragraph (`:868`) opening: "A few labels are fixed here as well as, or instead of, in a state below — because the control belongs to no state of its own, or because it appears across several:" — list unchanged. — **done.** The Labels paragraph now opens the list with "A few labels are fixed here as well as, or instead of, in a state below — because the control belongs to no state of its own, or because it appears across several:". The list itself is unchanged, so "Add a swatch", "Review the set-aside swatches" and "Done" — which all also appear in a state's copy — are no longer described by a framing that says they belong to no state.
- [x] **FX12-6** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph still lists every control named by a row; Traceability paragraph (`:572`) names F42 as clarified four times; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Every row a fix touched carries the right status (see FX12-0); the 10 open rows are R3.6, R3.11, R7.1, R7.5, R8.1, R8.3, R8.15, E23, E24, E39. The §12 Labels paragraph still lists every control a row names, and every action a row names still appears in that state's copy — no fix added or renamed a control, and E24's action is still "Done". No state gained a variant or a conditional line, so R11.12's enumeration needed no change. The Traceability paragraph names fence F42 as clarified four times and says what the fourth settles; no "clarified three times" is left anywhere. All six mermaid diagrams are untouched and still fenced. No literal ampersand-escape anywhere in the file. Counts: 144 R, 44 E, 11 M — 199 rows, 189 🤝 and 10 ⌛️.

## Item-for-item map (verified against each review)

PM: PM12-1→FX12-1 · PM12-2→FX12-3 · residue judgments→log only. Staff: R12-1→FX12-2 · R12-2→FX12-1 · R12-3→FX12-5 · Q1–Q3→FX12-1, FX12-2. Test: R12-T1→FX12-1 · R12-T2→FX12-3 · R12-T3→FX12-2. Arch: A12-1→FX12-1 · A12-2→FX12-3 · interrupted-session closure question→log only · Labels note→FX12-5. PMM: PMM12-1→FX12-2 · PMM12-2→FX12-2 · PMM12-3→FX12-1 · PMM12-4→FX12-4 · PMM12-5→FX12-5. agy: advisory; see log.
