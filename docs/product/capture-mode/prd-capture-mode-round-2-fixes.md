# Capture Mode PRD — round 2 fixes

Resume point for round 2. These are the regressions and gaps the round-1 delta verification found in the round-1 fix pass (log: `../../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 1 — delta verification"). One box per new finding from every lens that ran the delta pass. The fence file `prd-capture-mode-fences.md` (F1–F16) is the owner's written authorization. Tick each box as the fix lands in `prd-capture-mode.md`. Line numbers refer to commit `f09c90e`.

Every round-1 finding was verified RESOLVED or RESOLVED-BY-FENCE by its own lens; the one PARTIAL (test T12, the partial-set rule) is closed by FX2-4 and FX2-5 below.

Source key: PM / Staff / Test / Arch = Claude delta-verification lenses; agy-* = cross-model delta lenses.

## Regressions and gaps (owner-adjudicated where a fence is named)

- [x] **FX2-1** (NEW sleep · PM Blocker NEW-1, Test N3, Arch N1, Staff NEW-1) — **fence F14, "not a session interruption".** Sleep is a device-PRD halt, never an interruption. Remove "or the Mac slept" from the session vocabulary (`:105`), "the Mac sleeps" from UJ3 step 12's branch (`:271`) and UJ3.5 step 1 (`:377`), and "sleep" from the interruption flowchart edge (`:412`) and the lifecycle note (`:77`) — keep "sleep loses nothing" as a promise where it reads naturally. UJ3.5 step 3's sleep bullet (`:383`) is the one that stays; move it to UJ3.6 (device halt) with a cross-reference from UJ3.5. Interrupted = quit, crash, force-quit, power loss.
- [x] **FX2-2** (NEW resume entity · Staff NEW-2) — **fence F14.** UJ3.5 step 4 (`:384`): "Resume capture" starts a new session against the remembered row; the interrupted session is closed as "interrupted — resumed by the next session"; the new session inherits tallies and elapsed capture time; device change noted in the summary. Update the vocabulary (`:105`) status list to say an interrupted session is closed by the session that resumes it, and that "one interrupted per collection" means one unresumed one. Update the lifecycle diagram if it shows resume returning to the same session.
- [x] **FX2-3** (NEW ended cursor · Staff NEW-3; Arch N3 review selection) — **fence F15.** The remembered row belongs to the collection: rewrite UJ3 step 2 (`:244`) "a fresh session opens at the first pending row" → "opens at the collection's remembered row, or the next pending row if that row is no longer pending (fence F15)"; keep UJ3.4 step 4 (`:367`) and flowchart (`:418`) as they are; UJ3.3 step 2 (`:352`) selecting a deferred row updates the remembered row; a resume with zero pending rows opens into the review (UJ3.5 step 4 → UJ3.3). Amend the vocabulary sentence that ties the current row to the session.
- [x] **FX2-4** (NEW partial set vs save-failure · Test N1) — Vocabulary (`:107`): "a completed set awaiting its save is not a partial set; it is held across a save-failure halt for 'Try saving again' (UJ3.6 step 4)". Check UJ3 step 10 (`:265`) and UJ3.6 (`:398`) still agree.
- [x] **FX2-5** (NEW partial set vs deferred samples · Test N2) — Vocabulary (`:107`): "samples on a row that becomes deferred — by Skip, Flag, or K_FAILED_ATTEMPTS — are kept as that row's record (UJ3.1 step 3), never discarded; the discard rule is only for a row that stays pending". State what Skip does mid-set with good samples and no failure (UJ3.1 step 3, `:287`): the row is deferred with those samples kept, cause "skipped mid-set".
- [x] **FX2-6** (NEW Flag target · PM NEW-3) — **fence F16.** UJ3.1 step 3 (`:288-289`): until the next trigger press on the new current row, Flag demotes the row that just landed; after that press, Flag targets the current row. Say it once, in the operator's words; the recents strip stays as the place the just-landed row is visible.
- [x] **FX2-7** (NEW guard pause vs partial set · PM NEW-4, Test N6, Staff NEW-6) — UJ3.1 step 4 (`:291-294`) and vocabulary (`:107`): the consecutive-failure pause happens with the held row still under the instrument, so the held samples survive it; only the operator's explicit Pause (UJ3.4) discards. Force-resume resets the consecutive-failure counters but not the held row's K_FAILED_ATTEMPTS count (so the row can still auto-defer). Split the two pauses in the lifecycle diagram (`:60`): "Paused by operator" and "Paused by the guard". Note the reachability consequence (with N_CONSEC_HARD = 2 < K_FAILED_ATTEMPTS = 3 the guard pauses first when enabled) as a sentence in UJ3.9 step 4 (`:453`): the contributor walks the K path with the guard in record-only mode.
- [x] **FX2-8** (NEW review re-scan failure vs F6 · PM NEW-5, Test N4, Staff NEW-5) — UJ3.3 step 2 bullet (`:353`): a failed sample in the review holds the row and the next press retries, exactly as F6; the review moves to the next row only after K_FAILED_ATTEMPTS, Skip, or "Leave deferred". Same in UJ3.8 step 4 (`:444`): a failed sample on a re-scan holds for the retry; after K_FAILED_ATTEMPTS the re-scan attempt is abandoned, the row stays captured with its existing value, and the attempt is kept in history.
- [x] **FX2-9** (NEW re-scan accept-average · PM NEW-2) — **fence F10 applied to UJ3.8.** UJ3.8 step 4 (`:444`): when a re-scan's samples disagree, the same three choices as UJ3 step 9 — re-take, Accept average (the new set supersedes the prior reading, spread recorded), or abandon (row stays captured with its existing value; attempt kept in history). "A good reading is never demoted by a bad retry" stays true: abandoning never changes the canonical value.
- [x] **FX2-10** (NEW one-row session exit · Test N5, PM NEW-6) — UJ3.8 step 1 (`:440`), UJ4 intro (`:481`), UJ4 step 9 (`:495`): the one-row session ends itself when its row is captured, deferred (UJ4 step 7), abandoned after K_FAILED_ATTEMPTS (re-scan), or when the operator ends it from a halt or otherwise — never left open to block the next bulk session.
- [x] **FX2-11** (NEW attach rules · Arch N2, Staff NEW-4) — UJ3.8 step 1 (`:440`) and UJ4 intro (`:481`): (a) an ad-hoc capture or re-scan while another collection's session is active — paused included — is not allowed until that session is ended or complete; the app says which session is holding the instrument; (b) attaching to a paused bulk session lifts the pause for that one row and returns the queue to the row it left (UJ3.8 step 5 already says so; say it in UJ4.1 too); (c) attaching to an interrupted session is a resume first (UJ3.5 step 4, fence F14: a new session starts, full gate, device bound), then the row is scanned inside it; when the one-row capture completes the bulk session stays active, not ended.
- [x] **FX2-12** (NEW UJ1 step 6 · Arch N4) — UJ1 step 6 (`:119`): "Start capture session" is available once the queue has at least one pending or deferred row.
- [x] **FX2-13** (NEW "pass" bounds · Test N7) — UJ3 step 13 (`:272`) and UJ3.7 step 4 (`:430`): a "pass" is one traversal from the remembered row around to it again; a row jumped away from counts as skipped for the wrap-exhausted check.
- [x] **FX2-14** (NEW row-state diagram · Staff NEW-7) — Row-state diagram (`:92`): add "Skip after a failed attempt or mid-set" to the Pending → Deferred causes.
- [x] **FX2-15** (housekeeping) — Update the surfaced-questions list, the assumptions list (fences F14–F16 with a link), and the constants bullet; re-run the mermaid parser on every block and the anchor check; run the jargon sweep again (`durabl|persist|cursor|payload|schema|sqlite|inject|fixture|thread|async|transaction|row identity|blob`) and clear or justify every hit outside the two hand-off lists.
- [x] **FX2-16** (agy architecture delta, corrected to Nit) — UJ4.1 step 5 and UJ3.8 step 5: say what happens after the held row is captured. In the Cataloger's words: "once the inserted item is captured, the queue returns to the row that was held; when that row is captured, the queue continues with the next pending row after the inserted item, so nothing behind or ahead is skipped." Same shape for a mid-session re-scan: "the queue returns to the row it left, then continues in queue order." No mechanism; this is the promise the app keeps, the fence F12/F15 remembered row already makes it possible.

## Verification (item-for-item)

PM delta: NEW-1→FX2-1 · NEW-2→FX2-9 · NEW-3→FX2-6 · NEW-4→FX2-7 · NEW-5→FX2-8 · NEW-6→FX2-8/FX2-10. Test delta: N1→FX2-4 · N2→FX2-5 · N3→FX2-1 · N4→FX2-8 · N5→FX2-10 · N6→FX2-7 · N7→FX2-13. Arch delta: N1→FX2-1 · N2→FX2-11 · N3→FX2-3 · N4→FX2-12. Staff delta: NEW-1→FX2-1 · NEW-2→FX2-2 · NEW-3→FX2-3 · NEW-4→FX2-11 · NEW-5→FX2-8 · NEW-6→FX2-7 · NEW-7→FX2-14. agy-Arch delta → FX2-16. agy delta lenses: appended below when they report.
