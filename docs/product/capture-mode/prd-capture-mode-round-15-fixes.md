# Capture Mode PRD — round 15 fixes

Resume point for round 15 (log: `../../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 15"). One box per new finding from every lens. Fences F1–F44 plus the amendments (F6, F31 (1)–(3), F36, F38, F40, F42 (1)–(5), F44 (1)–(2)) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected. Line numbers refer to the round-14 commit. WHAT not HOW; the operator's vocabulary; no technical jargon. One rule, one row: every discard is enumerated in R7.1, every exit in R8.6, every entry state in R8.1, every leave-all effect in R8.15 — the others cite.

## Status flips (authorized by the round-15 dispositions in the log)

- [x] **FX15-0** — Flip to 🤝 Aligned R3.6, R7.11, R8.1, R8.3; the 7 held rows stay ⌛️ with "edited in round 15, re-review" where a fix edits them (held rows a fix does not touch keep their current note). Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 15, re-review". Report final counts. — **applied.** Flipped to 🤝 Aligned and left there: R3.6, R7.11 and R8.3 (no fix edits any of them). R8.1 flipped too and was then edited by FX15-1 and FX15-2, so it ends "⌛️ Ready for Alignment — edited in round 15, re-review", as rule 3 directs. All 7 held rows carry a round-15 edit, so each now reads "edited in round 15, re-review" (R3.8, R7.1, R8.6, R8.15, R11.12, E39, E42 — E42's "re-opened in round 14" note is replaced, the row having been edited this round). No 🤝 row other than R8.1 was edited, nothing else moved, and no new rows. Final: 191 🤝, 8 ⌛️ of 199 (144 R, 44 E, 11 M).

## Majors

- [x] **FX15-1** (Arch R15-A1, Test R15-F2; **F31 clarification (3)**) — R7.1 (`:753`): the one-row clause names both triggers — choosing another swatch in the list, and settling the swatch the one-row session is on, one row at a time or all at once (R8.5, R8.15) — and the closing count of the operator's own discarding actions reads "Three" with that settle named beside Pause and ending early; the line shown is E42's set-aside variant. R8.15 (`:784`) cites R7.1 for the discard rather than stating it. R8.1 (`:778`): its one-row sentence cites R7.1 rather than re-enumerating the trigger (if that edit is made, R8.1 returns to ⌛️ per FX15-0). — **done.** R7.1 now closes its list of the operator's own discarding actions at **Three**: "Pause", ending the session early, and a decision that settles the very swatch under the instrument, taken one row at a time or all at once (R8.5, R8.15, fence F31 as clarified three times) — with the reason given in the operator's terms (they are done with that swatch, so there is nothing left for its samples to be kept for) and E42's set-aside variant named as the line. R7.1's one-row clause now names both triggers — choosing another swatch, and settling the very swatch that session is on, which ends it. R8.15 cites R7.1 for the discard in both its exceptions rather than restating the cue and the line. R8.1's one-row sentence names the settle alongside selecting another row and sends the part-finished set to R7.1 rather than re-enumerating it; R8.1 was flipped by FX15-0 and returns to ⌛️.
- [x] **FX15-2** (PM PM15-1, PMM PMM15-2; **F44 clarification (2)**) — R8.6 (`:783`): the end-of-run review has three ways in — the queue exhausted, the end-early summary's "now", and a resume that opens into the review at a review-selected row (R3.8) with the queue already exhausted; a resume that opens into it with rows still pending is the detour (the operator pressed "Resume capture", not stop), so leaving returns to the row they were on and the session goes on. Lead with the scope so the early-end sentence never reads unconditional. R3.8 (`:660`): points at R8.6 for which exit that review has. R8.1's fourth way in cites it. — **done.** R8.6 now leads with the scope: "For the review at the end of the run, leaving it before that ends the session early" — the early-end sentence can no longer be read as unconditional. Three ways into that review: the queue exhausted; the end-early summary's "now" however many swatches are still waiting; or "Resume capture" opening straight into it at a swatch selected there before, with nothing left waiting. Every other way in is the detour — the collection-surface review taken mid-run, and a resume that opens into the review with swatches still waiting, because pressing "Resume capture" asks to carry on rather than to stop — and where the detour was a resume there was no swatch under the instrument before it, so leaving puts the operator in the queue at the swatch the session would otherwise have opened at (R3.7, R3.8). R3.8 now points at R8.6 for which exit that review has. R8.1's fourth way in says the resume opens the review at that swatch and takes its exit from R8.6 like every other way in.
- [x] **FX15-3** (Staff S15-1) — R7.1 (`:753`): "the row returns to sample 0 and stays pending" holds for a row that was pending; a captured row whose re-scan (R8.7) is cut short by a halt or a jump keeps the reading it had (R8.8, R8.10) and the samples taken so far are let go. E42 (`:919`): a third variant — "You'd started ⟨code⟩ again before this. It keeps the reading it already had." R11.12 (`:863`): three variants. — **done.** R7.1's outcome is split by what the row was: a row that was pending returns to sample 0 and stays pending; a captured row the operator had started scanning again (R8.7) keeps the reading it already had, with the cut-short attempt kept in its history (R8.8, R8.10). E42 gains a third variant, "Re-scan" — "You'd started ⟨code⟩ again before this. It keeps the reading it already had." — and R11.12 now names three variants (in-the-queue, set-aside, re-scan).
- [x] **FX15-4** (PMM PMM15-1) — E39 (`:916`): "…marked, with everything already kept on them." plus a conditional line shown only where the swatch under the instrument had a part-finished set: "The ⟨dropped⟩ samples you'd just taken on ⟨code⟩ are let go; it goes back to its first sample." R11.12 (`:863`) enumerates the line. Leave E27's phrase (`:904`) as is; note it for the owner. — **done.** E39 now reads "marked, with everything already kept on them", and carries the conditional line "The ⟨dropped⟩ samples you'd just taken on ⟨code⟩ are let go; it goes back to its first sample." R11.12 enumerates that line beside E39's line about the run the decision finished. §12's Placeholders paragraph is kept true: ⟨dropped⟩ is the count let go by the action that led here, or — where the state is the confirmation shown before that action, which is E39 alone — by the action it is asking about; the existing "a sentence carrying one count drops whole when that count is zero" rule is what makes the line conditional. E27's "everything the instrument did read" is left as it is (see the report).
- [x] **FX15-5** (Test R15-F1; **F31 clarification (3)**) — R8.15 (`:784`): the one-row exception belongs to the whole look-through branch ("The look-through branch has one exception under the instrument…"), not to the interrupted sub-case; the "No other row under the instrument is touched by either of them" sentence becomes the rule for a review-selected row holding a part-finished set — "Leave them all set aside" settles it too, and its samples go under R7.1 with the cue and E42's set-aside line (fence F31 as clarified three times). Staff Q4: the rows-pending branch says the session ends on leaving the review (R8.6), not on the keypress. — **done.** The exception is now the whole look-through branch's ("The look-through branch has one exception under the instrument"), not the interrupted sub-case's. "No other row under the instrument is touched by either of them" is replaced by the rule it was hiding: the review has the same exception — a swatch the operator had started after selecting it there (R8.3), at the end of the run or in a detour alike, is one of the rows being settled, and the part-finished set it was holding goes the same way under R7.1 (fence F31 as clarified three times) — followed by the guard that survives: neither leave action touches the swatch under the instrument in any other case. Staff Q4: the rows-pending branch now says settling leaves nothing outstanding but does not itself end the session — it ends early when the operator leaves the review, which is R8.6's rule.

## Minors

- [x] **FX15-6** (PM, Test diagram note) — Session state diagram (`:82-90`): add the detour edges — `Capturing --> Review: opened from the collection surface mid-run (detour, fence F44)` and `Review --> Capturing: leave the detour, back on the swatch left` — and re-label `Review --> Ready` as the end-of-run review's early exit. Validate the mermaid renders. — **done.** Two edges added to the session state diagram, `Capturing --> Review: opened from the collection surface mid-run (detour, fence F44)` and `Review --> Capturing: leave the detour, back on the swatch left`, and `Review --> Ready` re-labelled "leave the end-of-run review early, rows stay deferred". Every existing edge is still there (38 edges, up from 36); labels are unquoted exactly as every neighbouring edge is; both states are already declared; all six mermaid blocks parse with balanced quotes and no unrecognised lines.
- [x] **FX15-7** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph still lists every control named by a row; Traceability names F44 as clarified twice and F31 as clarified three times; mermaid renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Statuses: the 4 authorized flips landed and R8.1 returned to ⌛️ after FX15-1/FX15-2 edited it; all 7 held rows carry a round-15 edit and read "edited in round 15, re-review"; no other row moved. §12 Labels needed no change — no fix added or renamed a control, and every action the edited rows name ("Pause", "Resume", "Resume capture", "Leave it set aside", "Leave them all set aside", "Review the set-aside swatches") is written in the Labels paragraph or in a state's copy. Placeholders is kept true by the ⟨dropped⟩ edit above. Traceability now names fence F31 as clarified three times and fence F44 as clarified twice, each with what the clarification settles; still seven fences carrying an amendment or clarification. All six mermaid blocks parse. No literal ampersand-escape anywhere in the file (0 occurrences). Counts: 144 R, 44 E, 11 M — 199 rows, 191 🤝 and 8 ⌛️.

## Item-for-item map (verified against each review)

PM: PM15-1→FX15-2 · diagram note→FX15-6 · residue→log only. Staff: S15-1→FX15-3 · Q1→FX15-3 · Q2→FX15-1 (cite) · Q3→FX15-2 (wording) · Q4→FX15-5. Test: R15-F1→FX15-5 · R15-F2→FX15-1 · diagram risk→FX15-6. Arch: R15-A1→FX15-1 · pre-lock pass note→log only · held-case reason→log only. PMM: PMM15-1→FX15-4 · PMM15-2→FX15-2 · E27 note→log only. agy: coincides with FX15-1/FX15-2.
