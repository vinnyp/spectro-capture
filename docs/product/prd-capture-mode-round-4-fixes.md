# Capture Mode PRD — round 4 fixes

Final wording pass: the residue of the round-3 closing check (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 3", R3-D1..R3-D7). Seven one-sentence items; one owner confirmation (fence F18). The fence file `prd-capture-mode-fences.md` (F1–F18) is the authorization. Tick each box as it lands in `prd-capture-mode.md`. Line numbers refer to commit `6e0c03d`.

- [x] **FX4-1** (R3-D1 · PM Major) — Session vocabulary (`:116`) and UJ3.4 (`:373-383`): quitting the app, a crash, a force-quit, or power loss while the session is paused by the operator interrupts the session exactly as it does while capturing; the partial set was already discarded by the Pause. Lifecycle diagram: add `Paused by operator --> Interrupted: quit, crash, force-quit, or power loss`. Check UJ3 step 2's cross-collection branch (`:261`) now has a reachable exit: an interrupted session on collection A does not block collection B (it is not in flight).
- [x] **FX4-2** (R3-D2 · Arch Major) — Remembered-row vocabulary (`:118`), UJ3 step 2 (`:257`), UJ3.5 step 3 (`:395`): the "opens the review at that row" exception holds only while that row is still deferred; once the row is captured, deliberately left deferred, or the review is left, the ordinary rule applies (open at the remembered row if pending, else the next pending row, else the nothing-to-capture state). Add one line to the Data Foundation hand-off (`:644`): the remembered row carries whether it was chosen in the review, and that mark clears when the row leaves deferred.
- [x] **FX4-3** (R3-D3 · Staff Major, Arch Minor, Test risk) — Session vocabulary (`:116`): "a collection holds at most one unresumed interrupted *bulk* session"; a one-row session that is itself interrupted is closed as **ended** on relaunch — its row stays as it was, nothing waits for resume. Say the same at UJ3.5 step 3's one-row bullet (`:400`). Lifecycle diagram: no new state; a note on the Interrupted state that one-row sessions never enter it.
- [x] **FX4-4** (R3-D4 · Test Major) — UJ3.9 step 5 (`:479`): reorder the walk — inject a save failure and a disconnect to walk UJ3.6, **resume scanning**, then quit the app mid-queue while capturing to walk UJ3.5 as a new session; then, separately, force-quit *during* a halt to walk the crash-from-halt path (the `Halted --> Interrupted` edge); state the check after each (counts identical, remembered row honoured, no duplicate rows).
- [x] **FX4-5** (R3-D5 · PM, Staff) — UJ3 step 2 (`:258`): the nothing-to-capture state offers re-scan only when the collection has captured rows, matching UJ1 step 6 (`:133`).
- [x] **FX4-6** (R3-D6 · Test; **fence F18**) — UJ3.1 step 4 (`:306-308`): the trigger clause reads "N_CONSEC_FLAGGED consecutive rows are deferred *by the instrument* — after K_FAILED_ATTEMPTS, by Skip after a failed attempt, or by a set that disagreed"; keep the sentence that operator deferrals (Flag as missing or damaged, Skip mid-set with no failure, Flag on a row that just landed) never count; cite fence F18. Constants bullet (`:607`): gloss N_CONSEC_FLAGGED as "consecutive instrument-caused deferrals". Assumptions list (`:630`): change the guard-scope entry from "assumed, owner to confirm" to "Confirmed — fence F18".
- [x] **FX4-7** (R3-D7 · Test) — UJ3 step 13 (`:287`) and UJ3.7 step 4 (`:450`): "a pass begins at the row that was current when the previous wrap-check ended — or when the session started — and ends when the queue comes back around to that position" (identical wording in both places).
- [x] **FX4-8** (housekeeping) — Assumptions list: add F18 with a link. Re-run the mermaid parser, the anchor check, and the jargon sweep; clear or justify every hit outside the two hand-off lists.

## Verification (item-for-item)

PM closing: Major→FX4-1; Minor→FX4-5. Arch closing: Major→FX4-2; Minor→FX4-3. Staff closing: Major→FX4-3; Minor→FX4-5. Test closing: Major→FX4-4; Minor N_CONSEC_FLAGGED→FX4-6; Minor pass anchor→FX4-7; risk one-row status→FX4-3. agy PM/Arch: none.
