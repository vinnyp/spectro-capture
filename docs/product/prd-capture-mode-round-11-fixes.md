# Capture Mode PRD — round 11 fixes

Resume point for round 11 (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 11"). One box per new finding from every lens. Fences F1–F43 plus the amendments (F6, F31, F36, F38, F40, F42 (1)–(3)) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected. Line numbers refer to commit `738af8b`. WHAT not HOW; the operator's vocabulary; no technical jargon. Read R3.6, R3.11, R7.1, R8.1, R8.3, R8.7, R9.6, R9.7 together before FX11-1, and E2, E23, E24, E25, E39 together before the copy items.

## Status flips (authorized by the round-11 dispositions in the log)

- [x] **FX11-0** — Flip to 🤝 Aligned the 3 rows the log's "Round 11 — Row flips" paragraph lists (R7.15, R8.2, E2); the 10 held rows stay ⌛️ with "edited in round 11, re-review" where a fix edits them (held rows a fix does not touch keep their current note). Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 11, re-review". Report final counts. — **applied.** R8.2 and E2 flipped to 🤝 Aligned; R7.15 flipped and then returned to ⌛️ because FX11-5 edits it. The 10 held rows all carry a round-11 edit, so each now reads "edited in round 11, re-review" (R3.6, R3.11, R7.1, R8.1, R8.3, R8.15, R11.12, E23, E24, E39). No other status moved and no new rows. Final: 188 🤝, 11 ⌛️ of 199 (144 R, 44 E, 11 M).

## Blocker

- [x] **FX11-1** (PMM PMM11-1 Blocker; PM PM11-1, Staff S11-F1, Arch A11-1, Test R11-T1, R11-T2, agy PM and staff — Major; **F42 clarification (3)**) — R8.1 (`:775`): the browse/in-session split is keyed on whether a session is capturing on the collection, never on the surface the offer was taken from. Replace the false premise ("no session is running behind it") with the real one and enumerate the four states: no session open → browse, a scan is a one-row session (R3.11); bulk session interrupted and unresumed → browse, a scan is a one-row session beside it (R3.11, R9.7, fence F17); bulk session paused → browse with nothing to discard because Pause already let the set go (R7.1, R7.3), a scan is a row inside that session (R9.6); bulk session capturing — from the collection surface under Reading B (R10.4) or from the end-early summary's "now" (E23, UJ3.4 step 4, UJ3.3) → the in-session review: partial set discarded with the cue and the header line (R7.1, fence F19), a selected row becomes the remembered row marked review-selected (R3.6, R8.3), and the session goes on to complete (R8.6) or end early (R7.5). In the three browse states the remembered row does not move. A test reads back the session chain (R11.11) to assert which session a scan from each route ran in. R3.6 (`:655`), R3.11 (`:660`), R8.3 (`:777`): re-key "opened from the collection" as "while no session is capturing" / "while a session is capturing" so the four rows say one thing. R7.1 (`:750`): the discard list reads "entering the set-aside review while a session is capturing"; the later "opening the set-aside list mid-session" sentence stays. — **done.** R8.1 now says once that whether the list opens to look through or as the review inside a session turns on whether a session is capturing on the collection, never on the surface the offer was taken from, and enumerates the four states: no session open (look through, a scan is a one-row session with the gate); a bulk session interrupted and unresumed (look through, a scan is a one-row session beside it, F17); a bulk session paused (look through with nothing left to discard, because Pause already let the part-finished set go; a scan is a row inside that session, the pause lifting for it); and a bulk session capturing, reached from the collection surface while capture is live (R10.4) or from the end-early summary's "now" (E23, UJ3.3, UJ3.4), which opens the review inside that session — the partial set let go with the cue and the header line, a selected row becoming the remembered row marked review-selected, and the session going on to complete or to end early. In the three look-through states the remembered row does not move, and a test takes a scan by each of the four routes and reads back the session chain to see which session each ran in (R11.11). The false premise ("no session is running behind it") is gone, as is the surface-keyed "Chosen from the collection surface" opener. R3.6, R3.11, and R8.3 are re-keyed on "while a session is capturing" / "while no session is capturing" and point at R8.1 for the four states rather than restating them; R7.1's discard list now reads "entering the set-aside review while a session is capturing" and cites R8.1, and its later "opening the set-aside list mid-session" sentence is untouched. Net effect on R8.1 is close to length-neutral: five sentences replaced, and the separate "Entering it discards any partial set" sentence folded into the fourth state.
- [x] **FX11-2** (PMM PMM11-2) — E39 (`:913`): "…and if that was the last thing waiting, it finishes your session." R8.15 (`:781`): satisfies R8.6's completion condition in a single keypress where nothing is left pending; with rows still pending the queue simply goes on, the set-aside rows now settled. — **done.** E39 now reads "…and if that was the last thing waiting, it finishes your session." R8.15 says the action satisfies R8.6's completion condition in a single keypress where nothing is left pending, and that with rows still pending the queue simply goes on, the set-aside rows now settled.

## Minors

- [x] **FX11-3** (PM PM11-2; PMM zero-count family; Staff deferred) — E23 (`:897`): the set-aside sentence carries its count in one sentence so it drops as a whole at zero ("⟨deferred⟩ swatches are set aside — deal with them now, another day, or just look back over them whenever you like."); R11.12 (`:860`) names E23's set-aside line beside E24's among the conditional lines. Placeholders paragraph (`:870`): in a sentence that carries several counts (E22, E23, E25's tallies) the clause whose count is zero is left out, not the sentence; E25 (`:899`) needs no change if that rule covers it — say so in the note. — **done.** E23's set-aside line is one sentence carrying its count — "⟨deferred⟩ swatches are set aside — deal with them now, another day, or just look back over them whenever you like." — so it drops as a whole at zero. R11.12 names it beside E24's among the conditional lines a test can list. The Placeholders paragraph now says that where one sentence carries several counts (E22's and E25's tallies) the zero clause is left out and the sentence stands, and that a one-count sentence drops whole, which is why E23 keeps its count and what to do about it together. E25 needs no change: its tally is exactly the multi-count case the new rule covers, and its other sentences carry no count.
- [x] **FX11-4** (PM PM11-3) — R11.12 (`:860`): "'Leave it set aside' on a row not yet settled, and 'Leave them all set aside' on the list while any row is still outstanding" — the second is a list-level action (R8.15), not a per-row one. — **done.** R11.12 now reads "'Leave it set aside' on a row not yet settled, and 'Leave them all set aside' on the list itself while any row is still outstanding, the second being an action on the list rather than one on a row", citing R8.5 and R8.15.
- [x] **FX11-5** (PMM PMM11-3; Arch note; fence F43) — E24 (`:898`): the action "Open the collection" goes, since the summary is already a state on the collection surface with that surface's entry points beside it (R7.15, fence F20); the action becomes "Done", which dismisses the summary and nothing else. Labels paragraph (`:868`) lists "Done" if a row names it; R7.15 (`:764`) says the summary is dismissed with "Done". — **done.** E24's primary action is "Done"; the body is unchanged, since fence F43's sentence already says the set-aside rows stay reachable. R7.15 names "Done", which dismisses the summary and nothing else, because the collection is already the surface it is shown on and its own ways in are beside it. The Labels paragraph lists "Done" with what it dismisses, in the same way it already lists "Review the set-aside swatches" — a label that also appears in states' copy.
- [x] **FX11-6** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph still lists every control named by a row; Traceability paragraph (`:572`) names F42 as clarified three times; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Every row a fix touched carries the right status (see FX11-0). The §12 Labels paragraph lists every control a row names, "Done" included, and every action a row names appears in that state's copy. The Traceability paragraph names fence F42 as clarified three times and says what the third clarification settles. All six mermaid diagrams are untouched and still fenced. No literal ampersand-escape anywhere in the file. Counts: 144 R, 44 E, 11 M — 199 rows, 188 🤝 and 11 ⌛️.

## Item-for-item map (verified against each review)

PM: PM11-1→FX11-1 · PM11-2→FX11-3 · PM11-3→FX11-4 · R8.1 density note→log only. Staff: S11-F1→FX11-1 · Q1 (interrupted session)→FX11-1 · Q2→FX11-1 · zero-count deferral→FX11-3. Test: R11-T1→FX11-1 · R11-T2→FX11-1 · session-chain test clause→FX11-1. Arch: A11-1→FX11-1 · F43 placement note→log only · E24 action note→FX11-5 · E33 third home→log only (parked). PMM: PMM11-1→FX11-1 · PMM11-2→FX11-2 · PMM11-3→FX11-5 · zero-count family→FX11-3. agy: PM/staff R7.1-vs-R8.1→FX11-1.
