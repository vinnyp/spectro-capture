# Capture Mode PRD — round 3 fixes

Resume point for round 3: the residue of the round-2 delta verification (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 2 — fix pass and delta verification", R2-D1..R2-D11). Wording and consistency only; one owner decision (fence F17). The fence file `prd-capture-mode-fences.md` (F1–F17) is the authorization. Tick each box as it lands in `prd-capture-mode.md`. Line numbers refer to commit `7a465d2`.

- [x] **FX3-1** (R2-D1 · agy-PM Major, agy-Arch, Test) — **fence F17.** Replace attach rule (c) in UJ3.8 step 1 (`:454`) and its echo in the UJ4 intro (`:496`) and UJ4.1 (`:518` context): a re-scan or ad-hoc capture against a collection with an unresumed interrupted bulk session runs as a one-row session of its own and ends itself; the interrupted session is untouched and still waits for a deliberate "Resume capture". Add one sentence to the session vocabulary (`:112`): an interrupted session is not in flight, so a one-row session beside it is allowed. Add: a one-row session that is itself interrupted (crash mid-scan) is simply closed on relaunch; its row stays as it was; no resume ceremony. Drop the UJ3.5 step 4 bullet's interaction with attach rule (c) if any remains.
- [x] **FX3-2** (R2-D2 · Arch Major, Staff, Test N7) — UJ3 step 13 (`:282`) and UJ3.7 step 4 (`:441`): define the pass without the remembered row: "a pass begins at the row that was current when the previous wrap-check ended (or when the session started) and ends when the queue comes back around to that position; a jump does not start a new pass; a row jumped away from without an attempt counts as skipped".
- [x] **FX3-3** (R2-D3 · Test Major, Arch) — UJ4.1 step 5 (`:524`): "once the inserted item is captured, the queue returns to the held row; when that row is captured, the queue continues in queue order from there, so nothing behind or ahead is skipped" — no reference to the inserted item's position (INSERT_POSITION stays an OQ).
- [x] **FX3-4** (R2-D4 · Staff, Arch) — UJ3 step 2 (`:253-256`): add the branch "If another collection's session is active or paused → starting capture here is not allowed until that session is ended or complete; the app says which collection's session is holding the instrument" — the same rule one-row sessions already carry.
- [x] **FX3-5** (R2-D5 · PM) — UJ3.1 step 3 (`:298-299`): after a "moved on" cue (K_FAILED_ATTEMPTS reached, or Skip) and before any trigger press, Flag does nothing except say the previous row is already deferred; it targets the new current row only once the trigger has been pressed on it.
- [x] **FX3-6** (R2-D6 · PM, Staff, Test) — Vocabulary partial-set list (`:116`): add "a mid-session re-scan entered by find (UJ3.7 step 4 → UJ3.8)" to the actions that discard the held samples; UJ3.8 step 5 (`:461`): "the queue returns to the row it left, at sample 0 of N".
- [x] **FX3-7** (R2-D7 · PM, Test) — UJ3.5 step 4 bullet (`:395`): an interrupted session with nothing pending and nothing deferred is closed as complete from the collection on relaunch, without the pre-flight gate; add `Interrupted --> Complete` to the lifecycle diagram.
- [x] **FX3-8** (R2-D8 · Arch) — Vocabulary (`:112`) and UJ3.5 step 1 (`:387`): "quit" means quitting while capturing; quitting from a halt goes through the device PRD's End-session warning and ends the session (device PRD §5). Lifecycle diagram: add `Halted --> Interrupted: crash, force-quit, or power loss during a halt`.
- [x] **FX3-9** (R2-D9 · Test) — UJ3.1 step 4 (`:301-304`): a trigger press during the guard's pause is not accepted and surfaces the pause; force-resume is a deliberate action.
- [x] **FX3-10** (R2-D10 · Test, Staff Q3) — Vocabulary remembered row (`:114`) and UJ3.5 step 3 (`:389`): when the remembered row was selected in the review, the next session (or resume) opens the review at that row rather than falling back to the next pending row.
- [x] **FX3-11** (R2-D11 nits) — `:377` cite F15 not F12; `:406`, `:470` "the row the session remembers" → "the collection's remembered row"; name the empty-collection condition once (use "nothing-to-capture state" in UJ1 step 6 and UJ3 step 2, with the offers that apply); lifecycle diagram: add `Item --> Paused by operator`, `Gate --> Complete` (nothing pending or deferred), and a `Capturing --> Capturing: Flag demotes the row that just landed (F16)` self-edge; state in UJ3.1 step 4 that an operator's Flag does not count toward N_CONSEC_FLAGGED (the guard watches instrument failures and instrument-caused deferrals, not the operator's decisions) — flag this as an assumption in the assumptions list; UJ4 step 9: a one-row session ends on capture, so "that one was wrong" for an ad-hoc item is a re-scan or Flag from the collection; add to `### Notes for the requirements pass`: a passing pre-flight gate is silent on repeat, so adding several ad-hoc items in a row does not re-show a passing gate. Leave the `#open-questions` legend link for the requirements pass. — *Applied in full except the `Gate --> Complete` edge: after FX3-7 no journey reaches it (the nothing-to-capture state fires before the gate in UJ1 step 6 / UJ3 step 2, and an interrupted session with nothing left closes without the gate), so `Interrupted --> Complete` is drawn instead.*
- [x] **FX3-12** (housekeeping) — Assumptions list: add F17 with a link; surfaced questions: none new (the pass definition, INSERT_POSITION, and guard mode stay as they are). Re-run the mermaid parser, the anchor check, the sleep grep, and the jargon sweep; clear or justify every hit outside the two hand-off lists.

## Verification (item-for-item)

PM round-2 delta: minors (a)→FX3-5, (b)→FX3-6, (c)→FX3-7, (d)→FX3-11; nits→FX3-11. Staff: (a)→FX3-2, (b)→FX3-4, (c)→FX3-6; nits→FX3-11; Q1→FX3-2, Q2→FX3-4, Q3→FX3-10. Test: Major→FX3-3; N7 PARTIAL→FX3-2; minors `:395`→FX3-1/FX3-7, `:362`→FX3-10, `:301-304`→FX3-9, `:461`→FX3-6; nits→FX3-11. Arch: Major→FX3-2; minors quit→FX3-8, UJ3 step 2→FX3-4, `:524`→FX3-3; nits→FX3-11; missing one-row interrupted→FX3-1; Flag window→FX3-11. agy-PM Major→FX3-1. agy-Arch Blocker→FX3-1.
