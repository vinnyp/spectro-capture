# Data Foundation and Data Export PRDs — round 4 fixes

Resume point for round 4 (log: "Round 4"). One file for both documents: DF = Data Foundation, DE = Data Export (`../export/`). Fences DF F1–F31 (F28 and F31 amended this round) and DE F1–F10 (F3, F7, F9 amended) are the owner's authorization; process rules 1–8 bind every edit — two sentences per row, cite rather than restate, never expand where a clause will do. Tick each box with a one-line **Landed:** note. FX4-15 is the only status change this pass makes. Lens keys: PM, S, T, P, MK, A, IF; C = the capture PRD, D = the device PRD.

## Majors

- [x] **FX4-1** (F31 amended; PM F4-1, A F4-1, A F4-2, PM F4-4, IF Nit E1, IF note) — DE R1.3: history rows are emitted on the collection's chosen condition, a non-chosen condition's set living in the file only. DE R1.1 says the same for the canonical export in one clause, or the Background's out-of-scope list names it. DE E1 gains a line "— the measurement condition these readings come out on, which is the one this collection is set to" and names the export-kind column beside the two version stamps; J1 says which condition. DE's inbound Data Foundation line: "the chosen condition's set current, absent only where the reading carries no measurement in it ([its R3.1], its F31), a non-chosen condition's set kept in the file and not exported"; DF's outbound line mirrors it.
  - **Landed:** DE R1.3 emits history rows on the chosen condition, a non-chosen set staying in the file; the Background out-of-scope list names it for the canonical export; DE E1 gains the condition line and names the export-kind column beside both version stamps; J1 step 3 says which condition; DE's inbound Data Foundation line and DF's outbound Data Export line reworded to match.
- [x] **FX4-2** (S MJ1, T Min R4.3) — DE R1.1: "…an export succeeding while the app holds the file open read-only; a file the app cannot interpret ([the Data Foundation PRD's R5.3], [its R5.7]) is not exportable". DF R7.2's declared list gains "a file the app holds open read-only"; DF's inbound Data Export line's Rows gains R7.2. DE R4.3's cite stands.
  - **Landed:** DE R1.1 now promises an export while the app holds the file open read-only and rules a file it cannot interpret (the Data Foundation PRD's R5.3, its R5.7) not exportable; DF R7.2's declared list gains that state; DF's inbound Data Export line names R7.2 in its text and Rows.
- [x] **FX4-3** (T T1, A F4-3, T Min R7.7, S Nit outbound, P Info) — DF R7.7's fixture list gains: a reading carrying a second condition's derived set; a collection imported into twice; a collection whose queue order differs from its items' insertion order; and the clause that the fixtures' device snapshots are the simulated device's settable ones ([the device PRD's R6.9]). M6's population gains "× the conditions kept per reading". DE's outbound Data Foundation line lists the identity-mapped column and the three new fixtures.
  - **Landed:** DF R7.7 gains three fixtures (a second condition's derived set, a collection imported into twice, a queue order differing from insertion order) and the simulated-device snapshot clause (the device PRD's R6.9); M6's population gains × the conditions kept per reading; DE's outbound Data Foundation line lists the identity-mapped column and the three new fixtures.
- [x] **FX4-4** (T T2) — DF R7.1's read-back set gains "each sample's decoded values beside its reading (fence F25)". R7.1 is 🤝 Aligned: Commit PR cell "edited FX4-4, alignment kept".
  - **Landed:** DF R7.1's read-back set gains each sample's decoded values beside its reading; 🤝 Aligned kept, Commit PR cell "edited FX4-4, alignment kept".
- [x] **FX4-5** (T T3, IF F4-02) — DE R4.1: "a golden file — header and rows — per fixture, per export kind, for every released export format version"; the app-version column is asserted present and well-formed and set aside before the byte-for-byte comparison; the corpus at ROWS_CEILING is asserted on header and dialect only. R4.2 consistent.
  - **Landed:** DE R4.1 keys the golden per fixture, per export kind, per released export format version; the app-version column is asserted present and well-formed and set aside before the byte-for-byte comparison; the ROWS_CEILING corpus is asserted on header and dialect alone. R4.2 already consistent (full column set, in order, in R2.2's dialect) and untouched; the Vocabulary's Golden entry and the Legend's constant list follow.
- [x] **FX4-6** (IF F4-01, S MN1) — DE R1.1's state column reads never-scanned, quarantined, or current — and, in the history export, superseded; DE R1.3: the state column is the row's, a superseded reading's row reading so, the current-reading flag saying which of an item's rows is canonical.
  - **Landed:** DE R1.1's state column reads never-scanned, quarantined or current, and superseded as well in the history export; DE R1.3 makes the state column the row's — a superseded reading's row reads superseded, the current-reading flag saying which of an item's rows is canonical.

## Minors and nits

- [x] **FX4-7** (MK F4-3, PM F4-2, P P4-1) — DF copy header defines a third marker: a sentence marked ‹until P1› is withdrawn when the P1 rows land. E8 and E14: "‹until P1› Once it's done it's final. Export first if you want to keep any of it — an export carries each swatch's current reading, not its earlier ones and not the scanning record. ‹P1› Exporting with full history carries every earlier reading too. ‹P1› You can undo it until you quit SpectroCapture; after that it's final."
  - **Landed:** DF copy header defines ‹until P1›; E8 and E14 take the new block — ‹until P1› Once it's done it's final; "each swatch's current reading, not its earlier ones and not the scanning record"; ‹P1› undo sentence gains "after that it's final".
- [x] **FX4-8** (PM F4-3) — DF R6.2: "…both scales offering an export first, which leaves the delete unperformed and returns to the confirmation"; DJ4 gains the alternative-path line.
  - **Landed:** DF R6.2's export-first offer leaves the delete unperformed and returns to the confirmation; DJ4 gains the alternative path "If I take the export first."
- [x] **FX4-9** (P P4-2) — DF R5.5: the salvage output is written where the user chooses and kept until they remove it (Commit PR cell "edited FX4-9, alignment kept"); E5's "Save what's readable" chooses the place and the body names ⟨path⟩ once written.
  - **Landed:** DF R5.5 gains "The salvage output is written where the user chooses and kept until they remove it", 🤝 Aligned kept, Commit PR cell "edited FX4-9, alignment kept"; E5's body names where it goes and that it stays at ⟨path⟩ until removed.
- [x] **FX4-10** (MK F4-1, MK N4-1, IF Nit E1) — DE E1: the two blank-row lines merge into "— ⟨n⟩ readings that can't be read and ⟨n⟩ you haven't scanned yet, which come out with their details, their colour columns empty, and a column saying which of the two it is"; the label reads "What's in the file and what's marked in it:".
  - **Landed:** DE E1's two blank-row lines merge into one naming both counts, the empty colour columns and the column saying which of the two; the label reads "What's in the file and what's marked in it:".
- [x] **FX4-11** (MK F4-2) — DJ3: "the last app version that can still change my file".
  - **Landed:** DJ3's floor branch now names "the last app version that can still change my file".
- [x] **FX4-12** (MK N4-2) — E13: the second "everything you scan goes into" becomes "goes into whatever folder you choose"; E25 unchanged.
  - **Landed:** DF E13's second occurrence reads "everything you scan goes into whatever folder you choose"; E25 untouched.
- [x] **FX4-13** (IF Nit R2.5, S Nit M1) — DE §2's user-story heading: "an app column never moves under me…"; DE M1's statistic: "the share reconstructable, an item with no canonical value counting as reconstructable when its identity and import columns round-trip and its state column reads so".
  - **Landed:** DE §2's user story reads "an app column never moves under me…"; DE M1's statistic drops the "among items with a canonical value" scope and counts an item with no canonical value as reconstructable on its identity, import and state columns.
- [x] **FX4-14** (T Nit R1.9, PM Nit R7.6k/l) — DF R1.9 drops the E10 cite (editorial, alignment kept); R7.6l drops "the export offer" (an action both rows list; editorial, alignment kept).
  - **Landed:** DF R1.9 drops the E10 cite and R7.6l's "What the test lists" reads "The collection-scale counts and the actions, no default delete"; both editorial, 🤝 Aligned kept, no note.
- [x] **FX4-15** (round-4 flips, log "Row flips") — DF to 🤝 Aligned: R2.3, R3.1, R3.3, R5.8, R6.5, R7.3, R7.8, M8, M9, E12, E16, E22, E25 (13). DE to 🤝 Aligned: R2.3, R2.4, R2.5, R4.2 (4). Held at ⌛️: DF R1.2, R5.7, R6.2, R7.2, R7.7, M6, E8, E13, E14; DE R1.1, R1.3, R4.1, R4.3, E1, M1.
  - **Landed:** DF → 🤝 Aligned: R2.3, R3.1, R3.3, R5.8, R6.5, R7.3, R7.8, M8, M9, E12, E16, E22, E25 (13). DE → 🤝 Aligned: R2.3, R2.4, R2.5, R4.2 (4). Held at ⌛️ exactly as the box lists: DF R1.2, R5.7, R6.2, R7.2, R7.7, M6, E8, E13, E14 (9); DE R1.1, R1.3, R4.1, R4.3, E1, M1 (6).
- [x] **FX4-16** (checks) — DF body ≤ 7,000 and DE ≤ 4,000 by the budget's count (6,997 and 3,193 before this pass; FX4-2, -3, -4, -8, -9 add DF clauses and FX4-14 removes words — compact prose under rules 7 and 8, never by deleting a rule; if DF cannot land under 7,000 without deleting a rule, land everything, leave this box unticked on that check alone, and report the count); two sentences per row; links and anchors across the ten files, the three siblings, README and AGENTS.md; the bare-label grep; the retired-ID list and origin table unchanged; counts per document; DF's fence → row map's F31 row and the export fence amendments (orchestrator-applied) present.
  - **Landed:** DF 6,981 and DE 3,368 by the budget's count (DF compacted under rules 7 and 8 — no rule deleted); every requirement row at most two sentences in both documents; all links and anchors resolve across the ten files, the three siblings, README and AGENTS.md bar one pre-existing sibling break (the capture PRD's journeys → ../README.md#5-collection-mode, now #6 after the F30 split — a locked sibling, not edited); bare-label grep empty; retired-ID list and origin table unchanged; counts DF R46/E22/M8/OQ12 (+13 lettered surface rows), DE R13/E4/M1/OQ4 (+2); DF's F31 map row and the DE fence amendments present.

Post-lock (unchanged): the vision's canonical-value line; D's and I's obligation retargets; C's E29 and OQ 19; the redacted bug-report artifact, the remembered-mapping lifecycle, the serial on D's authorization path; a v2 note on sample decoded values in the CSV; no mid-session or overwrite rule (deliberate).

Map: PM F4-1→FX4-1 · F4-2→FX4-7 · F4-3→FX4-8 · F4-4→FX4-1 · Nit R7.6k/l→FX4-14. Staff MJ1→FX4-2 · MN1→FX4-6 · Nit outbound→FX4-3 · Nit F7→orchestrator · Nit M1→FX4-13. Test T1→FX4-3 · T2→FX4-4 · T3→FX4-5 · Min R4.3→FX4-2 · Min R7.7→FX4-3 · Nit R1.9→FX4-14. Privacy P4-1→FX4-7 · P4-2→FX4-9 · Info snapshots→FX4-3 · Info notes→none needed · Info deferred→post-lock. Marketing F4-1→FX4-10 · F4-2→FX4-11 · F4-3→FX4-7 · N4-1→FX4-10 · N4-2→FX4-12. Architecture F4-1→FX4-1 · F4-2→FX4-1 (F31 amended) · F4-3→FX4-3 · F4-4→orchestrator (F28, F9 notes). Interface F4-01→FX4-6 · F4-02→FX4-5 · Nit fences→orchestrator · Nit E1→FX4-1/FX4-10 · Nit R2.5→FX4-13 · note F9/F31→FX4-1.
