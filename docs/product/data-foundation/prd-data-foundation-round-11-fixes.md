# Data Foundation and Data Export PRDs — round 11 fixes

Resume point for round 11 (log: "Round 11"). DF = Data Foundation, DE = Data Export (`../export/`). Fences DF F1–F32 and DE F1–F13 are the owner's authorization; process rules 1–8 bind every edit. Tick each box with a one-line **Landed:** note. Budgets: DF 8,000 (4 words of headroom before this pass; the pass is net −3), DE 4,000 (2 before; the pass is +1).

- [ ] **FX11-1** (SE11-1, T11-1, SR11-1, A11-1, IF11-1, P11-1, RL11-1, PL11-1) — DF R1.2's tail: "a later import into the collection keeps the existing columns' positions and values and appends its new ones after." becomes "a later import into the collection keeps the existing columns' positions and appends its new ones after." DF outbound Inventory Import obligations line: "keeps the existing columns' positions and values and appends its new ones after them" becomes "keeps the existing columns' positions and appends its new ones after them". Editorial reversion to the round-10-reviewed text: R1.2's Status → 🤝 Aligned, Commit PR "aligned round 11".
- [ ] **FX11-2** (IF11-2, T11-3) — DE R4.1: "a bump's diff confined to the derived-value and derivation-version columns" becomes "a bump's diff confined to the derived-value, gamut-mark and derivation-version columns". Stays ⌛️; Commit PR gains "edited FX11-2".
- [ ] **FX11-3** (A11-2, SR11-2) — DE fence file, F13's Why: "the app reshaping nothing on the way" becomes "the app reshaping nothing but its quoting on the way".
- [ ] **FX11-4** (SR11-4, SE11-4, PL11 Nit, IF11-3; SR11-3, SE11-3, PL11 Nit) — DE outbound Data Foundation line: "and a generated corpus at ROWS_CEILING carrying" becomes "beside a generated corpus at ROWS_CEILING carrying". DF §8: "each state's identity is stable when its wording changes" becomes "each state's identity is stable even when its wording changes".
- [ ] **FX11-5** — Status flips authorised by the log's round-11 flip note: DF R7.1, R7.7 → 🤝 Aligned ("aligned round 11"); DE R2.4 → 🤝 Aligned ("aligned round 11"). R7.5 and R5.7 stay ⌛️ (owner overrule at lock). Report both word counts by the log's method.
