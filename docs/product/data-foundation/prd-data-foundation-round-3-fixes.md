# Data Foundation and Data Export PRDs — round 3 fixes

Resume point for round 3 (log: "Round 3"). One file for both documents: DF = Data Foundation (`prd-data-foundation.md` and companions), DE = Data Export (`../export/prd-data-export.md` and companions). Fences DF F1–F31 and DE F1–F10 are the owner's authorization; process rules 1–8 bind every edit — two sentences per row, cite rather than restate, the table never expands where a clause will do. Tick each box with a one-line **Landed:** note. FX3-28 is the only status change this pass makes. Lens keys: PM, S (staff), T (test), P (privacy), MK (marketing), A (architecture), IF (interface); C = the capture PRD, I = the import PRD, D = the device PRD.

## Blocker and Majors

- [x] **FX3-1** (S B1) — DF R1.2's enumeration gains: every column an import brought in, under the name and in the position its import file gave it, a later import into the same collection keeping existing columns' positions and appending its new columns after them. DF's outbound Data Export line and DE's inbound Data Foundation line both name it with R1.2. DE R2.3 cites DF R1.2 for the passthrough order instead of "the import file's order", and says an item with no import contributes empty passthrough cells.
  - **Landed:** DF R1.2's enumeration gains every column an import brought in, under its file's name and position, with the second-import rule (existing positions kept, new columns appended); DF's outbound Data Export line and DE's inbound Data Foundation line both name R1.2; DE R2.3 takes the passthrough order from it and gives an item with no import empty passthrough cells.
- [x] **FX3-2** (S MJ1, IF F3-01) — DE R2.5: an appended app column lands at the end of the app block, passthrough columns keeping their relative order after it, and column position is not part of the contract — a consumer parses by name. DE R4.1: "a golden file — header and rows — per export kind, for every released export format version", the assertion its app columns in order at the head, appended app columns after them and before the passthrough block; retired-version goldens document what each release emitted and feed a future parse-an-old-export test. J1's "my parser keeps working" step says by name.
  - **Landed:** DE R2.5 lands an appended app column at the end of the app block with the passthrough block keeping its relative order and column position out of the contract; DE R4.1 is a golden per export kind per released version, asserting app columns at the head with an appended one before the passthrough block, and a retired version's golden documents that release; DE's Vocabulary entry for Golden follows; J1 step 12 reads columns by name.
- [x] **FX3-3** (S MJ2, T Min R6.5, P Info) — DE R4.2 gains "and asserts no export carries the vendor license credential ([the Data Foundation PRD's R6.5])"; DE's inbound Data Foundation line's Rows gains R4.2.
  - **Landed:** DE R4.2 asserts no export carries the vendor license credential (the Data Foundation PRD's R6.5); DE's inbound Data Foundation line's Rows gains R4.2 and R4.3.
- [x] **FX3-4** (T T1, S Min R1.1) — DE R1.1: "reads and never writes" means every value, mark and note reads back identical at SQLITE_READER_FLOOR before and after an export, and an export succeeds against a file the app has open read-only ([the Data Foundation PRD's R5.3]). DE R4.3 asserts that across a successful export and each induced failure, and runs one export against a file declared open read-only ([the Data Foundation PRD's R7.2]).
  - **Landed:** DE R1.1 reads "never writes" as every value, mark and note reading back identical at SQLITE_READER_FLOOR before and after, plus an export succeeding against a file held read-only (that PRD's R5.3); DE R4.3 asserts it across a success and each induced failure and runs one export against a file declared open read-only (its R7.2).
- [x] **FX3-5** (T T2) — DF R7.3 drops "a regeneration interrupted partway" from its named-state list. DF R7.5 gains: the test interrupts a regeneration, restarts, and asserts it resumes to completion with R7.1's older-stamp enumeration empty ([R3.3]). R7.5 is 🤝 Aligned: its Commit PR cell notes "edited FX3-5, alignment kept" (process rule 6).
  - **Landed:** DF R7.3 drops the interrupted regeneration from its list; DF R7.5 interrupts one, restarts, and asserts it resumes with R7.1's older-stamp enumeration empty, kept 🤝 Aligned with "edited FX3-5, alignment kept" in Commit PR; R3.3's now-stale R7.3 cite becomes R7.1.
- [x] **FX3-6** (A F3-1) — DF R3.1: "…is always present and is the current one, except where the reading carries no measurement in that condition, which [R3.5] marks absent". DE R2.1 gains: empty colour columns on a row whose state is current mean the reading carries no measurement in the row's condition.
  - **Landed:** DF R3.1's chosen-condition set is always present except where the reading carries no measurement in that condition, which R3.5 marks absent; DE R2.1 says empty colour columns on a row whose state reads current mean exactly that.
- [x] **FX3-7** (A F3-2, A Nit M1, IF Nit M1, T Min M1, S Min M1) — DF M6's population states two points — a typical collection (1,000 items) and ROWS_CEILING — with the budget read at the ceiling. OQ 5's interim candidate re-derived at the ceiling (the round-1 figures × 10 × N = 3: about 1.2 GB raw, 620 MB compressed, before F27's and F31's factors), its decision-so-far recording F31 as a third multiplier bounded by the instrument's condition count. DE M1: population is DF R7.7's fixture set; the statistic is scoped to items with a canonical value, a never-scanned or quarantined row counting as reconstructable when its identity and import columns round-trip and its state column reads so.
  - **Landed:** DF M6 states two population points (1,000 items and ROWS_CEILING) with the budget read at the ceiling; OQ 5's candidate is re-derived at the ceiling (about 1.2 GB raw, 620 MB compressed) and its decision-so-far records F31 as a third multiplier bounded by the instrument's condition count; DE M1's population is the Data Foundation PRD's R7.7 fixture set with the statistic scoped to items with a canonical value.
- [x] **FX3-8** (F31; A F3-5) — DF R3.1: a non-chosen condition's set is worked out when the user asks and then kept like any other, regenerated with the rest.
  - **Landed:** DF R3.1: a non-chosen condition's set is worked out when the user asks and then kept like any other, regenerated with the rest (fences F28, F31).
- [x] **FX3-9** (PM F3-1, P P3-1) — DF E8 and E14: the P0 sentence reads "Once it's done it's final, so export first if you want to keep any of it — an export carries the readings you have now, not the scanning record." then "‹P1› Exporting with full history carries every earlier reading too." then "‹P1› You can undo it until you quit SpectroCapture." DF R6.2: the export-first offer is the canonical export, the history option once [the export PRD's R1.3] lands.
  - **Landed:** DF E8 and E14 read "an export carries the readings you have now, not the scanning record", then ‹P1› for the full-history line and ‹P1› for the undo; DF R6.2's export-first offer is the canonical export, its history option once the export PRD's R1.3 lands.
- [x] **FX3-10** (MK F3-1, IF F3-06, PM F3-2, PM F3-3, MK F3-4, MK N3-3, S Nit E1) — DE R1.1: "the rest empty" → "its colour, spectral, and payload columns empty"; the single-item export uses the same surface with its counts scoped to one item. DE E1: "What's marked in the file" set as short lines, one per clause; adds "⟨n⟩ readings that can't be read, which come out with their details and a column saying so" and "⟨n⟩ you haven't scanned yet" the same way; adds the app version beside the export format version; the collision line reads "⟨n⟩ columns you imported whose names start with the prefix the app reserves"; "the wavelength readings" (drop "full", here and in J1). E1's placeholder rules say ⟨collection⟩ may be one swatch's code. J1 step 6 names `sc_nm_`; step 11 names the quarantined class.
  - **Landed:** DE R1.1 empties an unscanned item's colour, spectral and payload columns and gives a single item the same surface with counts scoped to it; DE E1 is set as short lines, one per clause, adding the unreadable-reading and not-yet-scanned classes, the app version beside the export format version, the reserved-prefix collision line, and "the wavelength readings"; ⟨collection⟩ may be one swatch's code; J1 steps 2, 6 (`sc_nm_`) and 11 (the quarantined class) follow.
- [x] **FX3-11** (PM F3-4, A F3-3, S Min R1.3) — DE R2.3: within an item, history rows follow the reading's sequence ascending ([the Data Foundation PRD's R2.1]); DE R1.3 cites it.
  - **Landed:** DE R2.3 orders history rows within an item by the reading's sequence ascending (the Data Foundation PRD's R2.1); DE R1.3 cites R2.3 for it.

## Minors and nits

- [x] **FX3-12** (IF F3-02, S Min out-of-grid) — DE R2.3: drop "and marked" — an empty wavelength column means no reflectance reported at that wavelength — and state that the out-of-grid branch cannot arise in v1, WAVELENGTH_GRID being the v1 family's own grid (fence F7), the way DF R5.7 states its floor. DE R1.2: a non-spectral reading's wavelength columns are empty.
  - **Landed:** DE R2.3 drops "and marked" and states the out-of-grid branch cannot arise in v1, WAVELENGTH_GRID being the v1 family's own grid, the way that PRD's R5.7 states its floor; DE R1.2 empties a non-spectral reading's wavelength columns.
- [x] **FX3-13** (IF F3-03) — DE R2.4 defines colliding: sharing an app column's name or carrying the `sc_` prefix; the rename repeats until the header is unique.
  - **Landed:** DE R2.4 defines colliding as sharing an app column's name or carrying the `sc_` prefix, the rename repeating until the header is unique.
- [x] **FX3-14** (IF F3-04; judgment call) — DE R2.4: columns this document does not name literally are fixed by [R4.1]'s golden and follow `sc_` plus lower snake_case, `sc_sRGB_*` the deliberate exception fences F4 and F6 name.
  - **Landed:** DE R2.4: a column this document does not name literally is fixed by R4.1's golden and follows `sc_` plus lower snake_case, `sc_sRGB_*` the deliberate exception fences F4 and F6 name.
- [x] **FX3-15** (IF F3-05, S Min R1.3 obligations, S Nit R7.7, T Min R7.7) — DE's inbound Data Foundation line gains the basis, the per-reading sequence and both time axes (DF R2.1), which reading is current (DF R2.2), and the imported columns (FX3-1); DF's outbound line mirrors it. DE gains a Capture Mode inbound line — a collection's queue order lives with the collection ([its R1.9]) → R2.3. DF R7.7's fixture list gains "a column mapped to identity" and cites [the export PRD's R2.4] for "colliding".
  - **Landed:** DE's inbound Data Foundation line gains the imported columns (its R1.2), the basis, the per-reading sequence and both time axes (its R2.1) and which reading is current (its R2.2), and DF's outbound line mirrors it; DE gains a Capture Mode inbound line for the collection's queue order (its R1.9) → R2.3; DF R7.7's fixture list gains a column mapped to identity and cites the export PRD's R2.4 for colliding.
- [x] **FX3-16** (MK F3-2, PM Nit) — DF E8, E14 and DJ4: "Deleting is the only thing that throws away a reading."
  - **Landed:** DF E8 and E14 open on "Deleting is the only thing that throws away a reading." and DJ4 step 4 says it in the Cataloger's voice.
- [x] **FX3-17** (MK F3-3) — DF R5.7: "the last app version that can still change it"; E16's body likewise.
  - **Landed:** DF R5.7 and E16 both name "the last app version that can still change it".
- [x] **FX3-18** (MK F3-5, N3-1, N3-2) — E25's headline "This folder syncs itself — two things that puts at risk"; E13 and E25 state the two risks in identical words; E22's body states the refusal ("so SpectroCapture won't open another until this one's done").
  - **Landed:** E25's headline reads "This folder syncs itself — two things that puts at risk"; E13 and E25 now state the two risks in identical words; E22's body carries the refusal ("so SpectroCapture won't open another until this one's done").
- [x] **FX3-19** (P P3-2) — DE E1: "which instrument each reading was taken on, by serial".
  - **Landed:** DE E1 names which instrument each reading was taken on, by serial.
- [x] **FX3-20** (P P3-3) — DF R2.3: "beyond an outside reader's recovery from the file"; R5.8 and E12: a copy holds the file as it was, including anything deleted or cleared since.
  - **Landed:** DF R2.3 clears a note beyond an outside reader's recovery from the file; DF R5.8 and E12 both say a copy holds the file as it was when taken, including anything deleted or cleared since.
- [x] **FX3-21** (T Min R6.3) — DF R7.2: after an undo, every reading of the restored item reads back with its history ([R6.3]); M9's population includes an undo.
  - **Landed:** DF R7.2 reads every reading of a restored item back with its history after an undo (R6.3); M9's population gains a delete undone inside its window.
- [x] **FX3-22** (T Min M8) — DF R7.4 names the timing facility: the quick check timed on R7.7's ceiling corpus every release; M8's Method cites R7.4 and R7.7.
  - **Landed:** DF R7.4's harness times the file-opening quick check on R7.7's corpus at ROWS_CEILING every release; M8's Method cites R7.4 and R7.7.
- [x] **FX3-23** (T Min R4.2, S deferred, T Nit R4.2 ×2) — DE R4.1: the comparison is byte-for-byte against the golden file. DE R4.2 cites R2.2 (dialect), carries the P1 seam ("the history export once [R1.3] lands"), and its "that PRD's M5" cite becomes M1 and [the Data Foundation PRD's M6].
  - **Landed:** DE R4.1 compares byte-for-byte against the golden; DE R4.2 asserts in R2.2's dialect, carries the P1 seam (the history export once R1.3 lands) and cites M1 and the Data Foundation PRD's M6 in place of its M5.
- [x] **FX3-24** (IF Nit R4.3, DF R7.8 same shape) — DE R4.3 and DF R7.8: "a failure mid-write, which resolves to one of the three by cause".
  - **Landed:** DE R4.3 and DF R7.8 both read "a failure mid-write, which resolves to one of the three by cause".
- [x] **FX3-25** (S Nit §7, IF Nit Vocabulary) — DF §7's intro: four owners; DF Vocabulary: "1–5 samples ([the capture PRD's R1.3])".
  - **Landed:** DF §7's intro says a full disk has four owners; DF's Vocabulary reading entry reads "1–5 samples (the capture PRD's R1.3)".
- [x] **FX3-26** (A F3-4) — DF R3.5 ends at "marked so rather than filled with a plausible number", citing [the export PRD's R2.1] for what the export writes. R3.5 is 🤝 Aligned: its Commit PR cell notes "edited FX3-26, alignment kept".
  - **Landed:** DF R3.5 ends at "marked so rather than filled with a plausible number", what the export writes being the export PRD's R2.1's; kept 🤝 Aligned with "edited FX3-26, alignment kept" in Commit PR.
- [x] **FX3-27** (A Nit) — DF R3.1 uses one qualifier to separate the two senses of "current" (the live derivation stamp within a pair; the working set across conditions).
  - **Landed:** DF R3.1 separates the two senses: "one live derivation per pair" within a reading-and-condition pair, and the chosen condition's set as "the one the app works from".
- [x] **FX3-28** (round-3 flips, log "Row flips") — DF to 🤝 Aligned: R1.1, R1.3–R1.10, R2.1, R2.4–R2.9, R5.1, R5.2, R5.6, R6.3, R7.1, R7.4, R7.6, R7.6b, R7.6d, R7.6e, R7.6i, R7.6m, E1–E3, E9–E11, E15, E19–E21, E23, E24, E26, M1, M4, M5 (44). DE to 🤝 Aligned: R1.2, R2.1, R2.2, R3.1, R4.4, E2, E3, E4 (8). Held at ⌛️: DF R1.2, R2.3, R3.1, R3.3, R5.7, R5.8, R6.2, R6.5, R7.2, R7.3, R7.7, R7.8, M6, M8, M9, E8, E12, E13, E14, E16, E22, E25; DE R1.1, R1.3, R2.3, R2.4, R2.5, R4.1, R4.2, R4.3, E1, M1.
  - **Landed:** DF → 🤝 Aligned (44): R1.1, R1.3–R1.10, R2.1, R2.4–R2.9, R5.1, R5.2, R5.6, R6.3, R7.1, R7.4, R7.6, R7.6b, R7.6d, R7.6e, R7.6i, R7.6m, E1–E3, E9–E11, E15, E19–E21, E23, E24, E26, M1, M4, M5. DE → 🤝 Aligned (8): R1.2, R2.1, R2.2, R3.1, R4.4, E2, E3, E4. Held at ⌛️ exactly as listed: DF 15 R/M rows and 7 states, DE 9 rows and E1.
- [x] **FX3-29** (checks) — DF body ≤ 7,000 and DE body ≤ 4,000 by the budget's count (links, HTML comments, code fences and table pipes stripped; 6,971 and 2,822 before this pass — FX3-1, -6, -8, -21, -22 add clauses to DF, FX3-26 removes one; compact under rules 7 and 8 as needed, never by deleting a rule); every requirement row at most two sentences; every citation and anchor resolves across the ten files, the three siblings, README and AGENTS.md; the bare-label grep empty in both directories; the retired-ID list and the origin table unchanged; counts of R, E, M, OQ rows per document; DF's fence → row map carries F31, DE's map unchanged.
  - **Orchestrator:** the F31 map row is added (the fence file is the orchestrator's to edit); every other check passed as the operator reports — DF 6,997 words, DE 3,193.
  - **Checks run (box left unticked — one check fails):** DF body 6,997 and DE body 3,193 by the budget's count, both under. Every requirement row at most two sentences, checked mechanically (DF 46 R rows, DE 13; the four rows reading three are two rule sentences plus process rule 4's "(Inherited obligation…)" marker). 1,700 links and anchors resolve across the ten files, the three siblings, README and AGENTS.md — 0 failures. The bare-label grep is empty in both directories. The retired-ID list and the origin table are untouched (the diff holds no line of either). Rows per document: DF 46 R plus 13 surface rows, 22 E, 8 M, 12 OQ; DE 13 R plus 1 surface row, 4 E, 1 M, 4 OQ; DE's fence → row map unchanged. **Missing:** DF's fence → row map carries no F31 line — F31's decision text is in the fence file but the map table still ends at F30, and adding the row means editing a fence file this pass was told not to touch. Owner or orchestrator to add it.

Post-lock (recorded, not this pass): the vision's canonical-value line (F10); D's R6.5 `sc_simulated` reading, UJ1/UJ1.2 first step, M2 start, R6.20 axis; I's R2.2 passthrough obligation naming Data Export; C's E29 and OQ 19; the redacted bug-report artifact, the remembered-mapping lifecycle, the serial on D's authorization path (privacy); a v2 note that a sample's decoded values are readable in the file but not carried in the CSV (staff Q13); no rule for an export mid-session or for overwriting at the destination (deliberate).

Map: PM F3-1→FX3-9 · F3-2→FX3-10 · F3-3→FX3-10 · F3-4→FX3-11 · Nit E8→FX3-16 · Nit mid-session→post-lock. Staff B1→FX3-1 · MJ1→FX3-2 · MJ2→FX3-3 · Min R1.1→FX3-4 · Min R1.3→FX3-15 · Min out-of-grid→FX3-12 · Min M1→FX3-7 · Min R1.3 order→FX3-11 · Nit §7→FX3-25 · Nit R7.7→FX3-15 · Nit E1→FX3-10 · Nit non-spectral→FX3-12. Test T1→FX3-4 · T2→FX3-5 · Min R4.2→FX3-23 · Min R7.7→FX3-15 · Min R6.5→FX3-3 · Min M1→FX3-7 · Min R6.3→FX3-21 · Min M8→FX3-22 · Nit ×2→FX3-23. Privacy P3-1→FX3-9 · P3-2→FX3-19 · P3-3→FX3-20 · Info→FX3-3 · Info deferred→post-lock. Marketing F3-1→FX3-10 · F3-2→FX3-16 · F3-3→FX3-17 · F3-4→FX3-10 · F3-5→FX3-18 · N3-1→FX3-18 · N3-2→FX3-18 · N3-3→FX3-10. Architecture F3-1→FX3-6 · F3-2→FX3-7 · F3-3→FX3-11 · F3-4→FX3-26 · F3-5→FX3-8 · Nit current→FX3-27 · Nit M1→FX3-7. Interface F3-01→FX3-2 · F3-02→FX3-12 · F3-03→FX3-13 · F3-04→FX3-14 · F3-05→FX3-15 · F3-06→FX3-10 · Nit R4.3→FX3-24 · Nit J1→FX3-10 · Nit M1→FX3-7 · Nit 1–5→FX3-25 · over-engineered goldens→FX3-2.
