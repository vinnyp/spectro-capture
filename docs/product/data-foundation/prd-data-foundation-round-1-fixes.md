# Data Foundation PRD — round 1 fixes

> **History.** The export contract moved to the [Data Export PRD](../export/prd-data-export.md) on 2026-09-09 under fence F30 after this round; §4's R4.x, R7.9, M3, E6/E7/E17/E18 and OQ 8/9/11/16 named below now live there under new IDs (its Traceability table maps them). This file is the round's record and is not rewritten.

Resume point for round 1 (log: "Round 1"). Fences F1–F21 (F1 amended: budget 7,000) are the owner's authorization; process rules 1–8 bind every edit — two sentences per row, cite rather than restate, testability pairing on every changed row. Tick each box with a one-line note. Statuses stay "⌛️ Ready for Alignment" on every row; nothing flips in a fix pass. C = the capture PRD.

## Rule-level items (each carries a fence)

- [x] **FX1-1** (F10; A1, S-B1, S-MJ3, S-MJ10, A-A1) — Vocabulary gains "sample" and "reading (a saved set of 1–N samples)". R2.1 rewritten: every sample kept as produced; the canonical value is the set's stored mean, readable without the app, recording its basis and derivation version; the illuminant/observer clause is deleted (C R4.6 puts them on the value; R3.2 is the single statement). Samples are a third tier: R6.2 and E8's counts exclude them; M6's population reads "1,000 items × 10 saved sets × N samples at the averaging default". The state diagram shows sample → reading → history. The vendor payload is an archived artifact where one exists.
  - **Landed:** Vocabulary gains sample/reading and re-bases canonical value on F10; R2.1 rewritten to the stored mean (illuminant/observer clause deleted, payload archived); R6.2 and E8 count readings not samples; M6's population is saved sets × N samples; the diagram runs sample → reading → history.
- [x] **FX1-2** (F11; agy-S Blocker, PM F-3, A-A5, S-MJ1, S-MJ2, I F1-08, S-MN1) — R1.2 split into the two guarantees F11 states. OQ 5 records the constraint and re-derives STORE_SIZE_BUDGET's candidate from the sample population, in samples. R2.6's "what it costs" gets a unit. OQ 2's candidate floor is derived from the features kept (not 3.31.0) and its closer names the open minimum-macOS dependency (AGENTS.md §3).
  - **Landed:** R1.2 keeps the readability half and new R1.6 the archived-payload half; OQ 5 records the F11 constraint and re-derives the candidate in samples; R2.6's cost is in megabytes on disk; OQ 2's candidate is derived from the features kept and its closer names the open minimum-macOS floor.
- [x] **FX1-3** (F12; PM Blocker F-1, PM F-2, I F1-06, T Major, S-MJ4, A-A2) — R2.4's reason set becomes initial / re-measurement / correction / correction-unconfirmed / restore, with a QC reading a record of its own. R2.5 excludes only confirmed corrections; unconfirmed readings are shown and marked. New row: every unconfirmed reading is enumerable and answerable in bulk after a session. E11 gains "Ask me later", opens with the re-scan date and the standing provisional status. The E29 obligation line uses "supersession reason" wording. R7.1's read-back set and M4's third question gain the reason and the never-true mark. R2.4's provisional text from Phase 3 is superseded by this.
  - **Landed:** R2.4's reason set is initial / re-measurement / correction / correction-unconfirmed / restore and R2.7 makes a QC reading a record of its own; R2.5 excludes only confirmed corrections; new R2.8 makes the unconfirmed set enumerable and bulk-answerable; E11 opens with the re-scan date and provisional status and offers "Ask me later"; R7.1 and M4's third question read back the reason and the never-true mark; the E29 obligation line says "supersession reason".
- [x] **FX1-4** (F10/A-A2) — Two time axes: every reading records when it was measured and when it was recorded; a restore carries the restored reading's measurement time; the over-time axis is measurement time (one clause on R2.1 or R2.3).
  - **Landed:** R2.1 records measurement time and record time, the over-time view reading measurement time; R2.3's restore carries the restored reading's measurement time.
- [x] **FX1-5** (F13; PM F-11, P-P2, A-A7, S-MJ7, T Major R1.3) — New row: the app names the risk when the file sits in a known sync-managed or network location; help docs carry the safe practice. R1.4 distinguishes what the app sends from what the location does. R7.4 and M1 scope the durability guarantee to local volumes, other classes needs-hardware-verify, M1's population recording which ran. R1.5: a hold left by a dead process never blocks opening. R1.3 states the outcome when the user opens a second file (orchestrator's default, owner may overrule: refused while a capture session is running, otherwise the first closes) and R7.3 induces it.
  - **Landed:** New R1.7 names the sync-managed/network risk and puts the safe practice in the help docs; R1.4 separates what the app sends from what the location does; R7.4 and M1 scope durability to local volumes with the other classes needs-hardware-verify and the class recorded; R1.5 says a dead process's hold never blocks opening; R1.3 states the second-file outcome (refused during a session, otherwise the first closes) and R7.3 induces it.
- [x] **FX1-6** (F14; PM F-8, A-A7) — New rows: the first launch asks where the file lives, offering a default; the app shows where the file is and lets the user move it or open another (backs E10). New copy state for the first-run prompt. Obligation line: C's OQ 19 closes on F2 and F14 as a post-lock amendment there.
  - **Landed:** New R1.8 (first launch asks where, with a default) and R1.9 (shows where the file is, move it, open another, backing E10); new state E13; the Capture Mode OQ 19 obligation line added.
- [x] **FX1-7** (F15; PM F-9, PMM F-8, T Major R6.1, A-A10, P-P7) — R6.1 drops the whole-file delete; help docs say Finder. New copy state for the collection-scale confirmation (counts, export-first offer per C R1.8), E8 stays the item state. R6.3: undo covers items and collections, a crash is not a quit (state the outcome), and after the window closes deleted content is not recoverable from the file by an outside reader — asserted in R7.6. R6.2's counts asserted against an R7.2 declared state. The Collection Mode obligation line gains "a single reading is never deletable out of history" and "delete is never a default action". R7.2 gains a delete-with-undo-pending state and says whether a pending undo is visible in the file.
  - **Landed:** R6.1 drops the whole-file delete and sends it to Finder plus the help docs; new state E14 for the collection-scale confirmation with counts and the export-first offer, E8 staying the item state; R6.3 covers items and collections, states the crash outcome, and asserts non-recoverability in R7.6; R6.2's counts assert against an R7.2 declared state; R7.2 gains the delete-with-undo-pending state and its file visibility; the Collection Mode line gains both delete guarantees.
- [x] **FX1-8** (F16; PM F-10, S-MN2, I F1-17, P-P4, PM F-12, T Major R6.5) — R6.1, R6.2, R6.5 → P0. R6.5's test asserts the credential is absent from the store file at the floor and from every export. R6.5's negative blanket becomes a positive inventory (the instrument's serial and firmware on every reading; the activity record C R8.13 keeps; what the user typed; nothing about the user's account or machine).
  - **Landed:** R6.1, R6.2 and R6.5 are P0 (Legend's P0/P1 split updated); R6.5 is a positive inventory — serial and firmware per reading, the capture PRD's R8.13 activity record, what the user typed, nothing else — and its test reads the store at the floor and every export.
- [x] **FX1-9** (F17; P-P5, P-P6) — R2.3 gains: version history covers measurements; item metadata and notes are editable and clearable, the edit superseding the old text without touching a reading.
  - **Landed:** R2.3 states that version history covers measurements and that metadata and notes are editable and clearable, the edit superseding the old text without touching a reading.
- [x] **FX1-10** (F18; PM F-6, A-A8, T Major M3, I) — R4.1 gains the vendor raw-payload column (empty and marked where none exists); M3 keeps its wording, its statistic "reconstructable to the fidelity its basis allows", population unchanged.
  - **Landed:** R4.1 carries the vendor raw-payload column, empty and marked where none exists; M3's statistic reads "reconstructable to the fidelity its basis allows", wording and population otherwise unchanged.
- [x] **FX1-11** (F19; P-P1, agy-P High) — New §6 row: v1 does not ship without the disclosure, with F19's content floor; OQ 12 keeps its disable-or-disclose half; the Telemetry/help-docs obligation line cites the row.
  - **Landed:** New R6.6 gates v1 on the disclosure with F19's content floor; OQ 12 keeps the disable half; the Telemetry/help-docs obligation line cites R6.5 and R6.6.
- [x] **FX1-12** (F20; T Minor OQ 8) — OQ 8 closes: Decision so far "`sRGB_gamut_clipped` (fence F20)", results section quoting the fence; R4.2 names the column.
  - **Landed:** OQ 8 answered — `sRGB_gamut_clipped` (fence F20) — with its own results section; R4.2 names the column.

## Consensus items (no fence needed; contradictions with locked siblings or missing tests)

- [x] **FX1-13** (S-B2, I F1-01, PMM F-3, S-MJ8, T Minor R3.5) — R3.5: a non-spectral reading carries all six spaces under the fixed reference it was taken at, marked non-spectral and not re-derivable under another illuminant or observer; absent-and-marked only where conditions are missing, with the absent token named for the export. R3.3: a reading with no spectral curve is not regenerated, keeps its reference, and is marked as not matching the collection's. R3.1 "except as R3.5 provides". The export marks non-spectral readings and carries the averaging basis and working-out version (C R4.24's obligation landed); R7.6 asserts them. E6 gains the conditional non-spectral and simulated sentences.
  - **Landed:** R3.5 rewritten: all six spaces under the fixed reference, marked non-spectral and not re-derivable, absent-and-marked only where a condition is missing, the absent token an empty field; R3.3 says a non-spectral reading is not regenerated and is marked as not matching the collection's; R3.1 adds "except as R3.5 provides"; R4.3 carries the mark, the averaging basis and the working-out version, asserted through R7.7's golden header; E6 gains the conditional non-spectral and simulated sentences.
- [x] **FX1-14** (T Blocker R2.2, A-A3, S-MN4, PM F-7) — R2.2 "at most one"; an imported row has none until scanned; a quarantined reading stays the record marked unreadable, the item has no current value, nothing is promoted, the quarantine mark persists in the file. A file carrying two current readings on an item opens read-only into E5 and is never auto-repaired; R7.2 asserts the named state per injected violation. E4 offers "Use the previous reading" (a restore per R2.3) with the surviving count. A re-scan after quarantine records its reason like any other. The diagram gains Quarantined → Canonical via re-scan or restore.
  - **Landed:** R2.2 is "at most one", an imported row has none until scanned, and a two-current-reading file opens read-only into E5 and is never auto-repaired; new R2.9 holds the quarantine rules and the persisting mark; R7.2 asserts the named state per injected violation; E4 offers "Use the previous reading" with the surviving count; the diagram gains Quarantined → Canonical.
- [x] **FX1-15** (A-A4, T Major R3.3) — R3.1/R3.3: a derived value's key is condition × illuminant/observer × derivation version; exactly one set is current per reading, the rest retained and marked superseded; the export emits the current set. A regeneration interrupted partway is resumable, and every value still on the old stamp is findable and named; R7.3 induces it.
  - **Landed:** R3.1 keys a derived value by condition × illuminant/observer × derivation version with exactly one current set, the rest retained and marked superseded and the export emitting the current one; R3.3 makes an interrupted regeneration resumable with old-stamp values findable and named, R7.3 inducing it.
- [x] **FX1-16** (I F1-10, A-A6, S-MJ6) — New row: any write the app makes lands whole or not at all, a crash or removed volume mid-write leaving the file as it was, and a confirmed reading durable before it is confirmed. The import obligation line remaps to it. R7.3's "a full disk" gains a store-volume-full state (new copy state), "a file taken away" cites C's E26 explicitly.
  - **Landed:** New R1.10 for atomic writes and pre-confirmation durability; the import obligation line remaps to it; R7.3 gains the full-store-volume condition with new state E15, and cites the capture PRD's E26 for a file taken away.
- [x] **FX1-17** (S-MJ5, A-A11, I F1-07, A-A9, P-P3, T Blocker R5.1, T Major R5.2, T Major R5.3–R5.5, agy-S Minor) — New rows: the file states its format version in one place and one form that never changes, readable before any other interpretation; the compatibility floor (the oldest file version the app upgrades, or none) with a state for a file below it naming both versions and the last app that reads it; the snapshot's lifecycle (retained until the user removes it, discoverable after the session, named as the route back to an older app, removable on request). R5.1: a test opens the snapshot and the user's copy at the floor with the app closed and reads back every reading, the source under an active write. R7.3 gains a lossy-upgrade fixture with the refusal asserted and the source byte-identical; every read-only path asserts the file byte-identical after open and close; E5's salvage output asserted complete. E2 drops "This takes a moment", gains progress; E3 handles no room for the snapshot; E1's action "Check for updates". "export format version" and "file format version" named apart.
  - **Landed:** New R5.6 (the file's own format version, one place, one form, readable first), R5.7 (the compatibility floor and E16), R5.8 (snapshot lifecycle); R5.1's test opens the snapshot and the user's copy at the floor under an active write; R7.3 gains the lossy-upgrade refusal, the byte-identical assertion on every read-only path, and E5's salvage completeness; E2 drops "This takes a moment" and gains progress, E3 handles no room for the snapshot, E1's action is "Check for updates"; R4.9 and R5.6 name the two format versions apart.
- [x] **FX1-18** (I F1-02..05, F1-12, F1-21, T Major R4.1/R4.4/R4.5, PM F-5, P-P8, S-MN3, PMM F-4, F-2) — The CSV contract: dialect (UTF-8, comma, RFC 4180 quoting, `.` decimal, LF); one column per wavelength named by wavelength; a reserved prefix for app-emitted columns with a stated collision rule for imported passthrough columns; column order and a deterministic row order (queue order); export format version and app version as columns on every row; the bump rule (a removal, rename, reorder, or meaning change bumps; a strictly appended column does not, and says so); measurement condition, derivation version, illuminant and observer beside every colour value; R4.4's history export columns (version ordinal, measured-at, supersession reason, current flag, simulated) and its own assertion; R7.6 asserts the full header in order against a checked-in golden per format version for both exports; E6 names all six spaces, the simulated line, the format version; E7 becomes three variants (no room, not writable, gone) and §7 gains injectable export-destination failures. R4.5's test has an owner.
  - **Landed:** New R4.6 (dialect), R4.7 (one column per wavelength, fixed column order, queue row order), R4.8 (reserved prefix and collision rule), R4.9 (the bump rule); R4.1 carries format and app version on every row; R4.2 puts condition, illuminant, observer and derivation version beside every colour value; R4.4 lists the history-export columns and cites its assertion; new R7.7 asserts the full header in order against a checked-in golden per format version for both exports; E6 names all six spaces, the simulated line and the format version; E7 splits into E7/E17/E18 and new R7.8 injects export-destination failures, giving R4.5's test an owner.
- [x] **FX1-19** (T Blocker R1.4, T Blocker R3.4, T Major R7.5/M2, T Major M5, T Major R7.6, T Major R2.6, T Minor R5.4/M4/R7.4, S-MN5) — R1.4's test: online, reachability injected on, every outbound attempt enumerated via the device PRD's R6.17, destination set exactly the vendor authorization and telemetry-when-on, no request carrying an item or payload. R7.5's reference set carries payloads with known gamut verdicts including the boundary, from an independent published source, checked in; a metric row for gamut-verdict accuracy; DERIVATION_TOLERANCE cited in R7.5. New §7 row: a checked-in fixture file per released file version and a golden export header per format version; M5 asserts a non-empty corpus. R7.6 gains lettered rows a–n, one per surface (C R11.15's pattern), including the history-cost display (R2.6) added to Surfaces. M4's statistic is answered correctly. An M row for INTEGRITY_CHECK_BUDGET. R7.1 cites R1.2. R5.4 says "at ROWS_CEILING (the capture PRD's OQ 13)".
  - **Landed:** R1.4's test is online with reachability injected, enumerating outbound attempts via the device PRD's R6.17 against an exact destination set with no item or payload; R7.5's reference set is published, checked in, and carries known gamut verdicts including the boundary, citing DERIVATION_TOLERANCE, with new M7 for verdict accuracy; new R7.7 for fixtures and goldens and M5 asserting a non-empty corpus; R7.6 gains lettered rows a–n with the history-cost display added to Surfaces; M4's statistic is answered correctly; new M8 for INTEGRITY_CHECK_BUDGET; R7.1 cites R1.2; R5.4 says at ROWS_CEILING (the capture PRD's OQ 13).
- [x] **FX1-20** (PMM Blocker F-1, PMM F-5, F-6, F-7, F-10, F-11, F-12, F-13, F-14, PM F-4, T Major R1.5, S-MJ9, I F1-19, F1-20) — E6's gamut sentence: "Colours that fall outside standard sRGB are marked in the file too — most screens can't show these accurately." E9 rewritten as an always-available re-read that claims no detection (R1.5's rule), its detection variant gated on OQ 14. E4's earlier-readings line; E5's headline "Some of this file can't be read"; E10 "Try again; Open a different file"; E1 "Check for updates"; E2 per FX1-17; E7 per FX1-18; E11 per FX1-3. New states: first-run prompt (FX1-6), collection delete (FX1-7), store volume full (FX1-16), older than the floor (FX1-17), absent derived value (R3.5) or its Collection Mode obligation. Surfaces files E9 with the file-opening states.
  - **Landed:** E6 carries the exact gamut sentence; E9 is an always-available re-read claiming no detection with the detection line marked ‹OQ 14›; E4 gains the earlier-readings line, E5 the headline "Some of this file can't be read", E10 "Try again; Open a different file", E1 "Check for updates", E2/E7/E11 per their items; new states E13, E14, E15, E16; the absent derived value goes to Collection Mode as an obligation rather than a state; Surfaces files E9 with the file-opening states.
- [x] **FX1-21** (S-MN7, I Minor, A-A12, PM Nit, T Nit) — Obligations: C's OQ 19 line (FX1-6); C's two Data Foundation lines both cited; the device R1.22 over-claim dropped; the Collection Mode line gains R3.5's absent mark and the two delete guarantees. R1.3: the matching rule applied consistently across the one file, uniqueness scoped per collection (import R3.3, C R1.2). R3.3's "never a schema change" → "never asks the user to upgrade their file and never touches a raw payload". "Data consumer" persona wording and "colour" spelling untouched (house decision, post-lock).
  - **Landed:** Obligations: the Capture Mode OQ 19 line added, both of that PRD's Data Foundation lines cited, the device R1.22 over-claim dropped, the Collection Mode line gains R3.5's absent mark and the two delete guarantees; R1.3 applies one matching rule across the file with Swatch Code uniqueness per collection and collection names across it; R3.3 now says "never asks the user to upgrade their file and never touches a raw payload"; "Data consumer" and "colour" left untouched.
- [x] **FX1-22** (checks) — Body ≤ 7,000 words (report); every requirement row at most two sentences; every citation and anchor resolves in all five files and the three siblings; no bare cross-PRD row label; the mermaid diagram renders; row, state, metric, OQ counts; OQ results sections for OQ 8 and any other fence-closed question; the fence → row map updated for F10–F21; statuses all "⌛️ Ready for Alignment".
  - **Landed:** Body 7,000 words exactly (raw `wc -w` 8,248); no requirement row over two sentences; 0 link/anchor errors across the five files, 0 unresolved cross-PRD row cites, the bare-label grep empty; mermaid parses; 38→55 R rows (+14 lettered R7.6a–n), 12→18 E states, 6→8 M rows, 15 OQs unchanged (6→7 answered, 7 results sections); every row ⌛️ Ready for Alignment.

Map: PM F-1→FX1-3 · F-2→FX1-3 · F-3→FX1-2 · F-4→FX1-20 · F-5→FX1-18 · F-6→FX1-10 · F-7→FX1-14 · F-8→FX1-6 · F-9→FX1-7 · F-10→FX1-8 · F-11→FX1-5 · F-12→FX1-8 · F-13→FX1-19 (M row) · F-14→FX1-16 · nit OQ 2→FX1-2. Staff B1→FX1-1 · B2→FX1-13 · MJ1→FX1-2 · MJ2→FX1-2 · MJ3→FX1-1 · MJ4→FX1-3 · MJ5→FX1-17 · MJ6→FX1-16 · MJ7→FX1-5 · MJ8→FX1-13 · MJ9→FX1-20 · MJ10→FX1-1 · MN1→FX1-2 · MN2→FX1-8 · MN3→FX1-18 · MN4→FX1-14 · MN5→FX1-19 · MN6→FX1-21 · MN7→FX1-21 · nits→FX1-13/19. Test B R1.4→FX1-19 · B R2.2→FX1-14 · B R3.4→FX1-19 · B R5.1→FX1-17 · Majors→FX1-5/20/3/19/15/18/17/7/8/19/19/19 · Minors→FX1-13/7/7/19/19/19/12 · nits→FX1-13/19/20. Privacy P1→FX1-11 · P2→FX1-5 · P3→FX1-17 · P4→FX1-8 · P5→FX1-9 · P6→FX1-9 (rejected half in the fence file) · P7→FX1-7 · P8→FX1-18 · P9→post-lock · P10→FX1-19 · Info→post-lock. PMM F-1→FX1-20 · F-2/F-3→FX1-18/13 · F-4→FX1-18 · F-5→FX1-17 · F-6→FX1-20 · F-7→FX1-3 · F-8→FX1-7 · F-9→FX1-20/21 · F-10..F-14→FX1-20/18. Arch A1→FX1-1 · A2→FX1-4 · A3→FX1-14 · A4→FX1-15 · A5→FX1-2 · A6→FX1-16 · A7→FX1-5/6 · A8→FX1-10 · A9→FX1-17 · A10→FX1-7 · A11→FX1-17 · A12→FX1-21 · nits→FX1-2/19. Interface F1-01→FX1-13 · F1-02..05→FX1-18 · F1-06→FX1-3 · F1-07→FX1-17 · F1-08→FX1-2 · F1-09→FX1-16 · F1-10→FX1-16 · F1-11→FX1-14 · F1-12→FX1-18 · F1-17→FX1-8 · F1-18→FX1-19 · F1-19/20/21→FX1-20/18 · minors→FX1-21/18. agy staff Blocker→FX1-2 · Major→rejected (fence file) · Minor→FX1-17. agy privacy High→FX1-11 · Low→rejected (fence file). Post-lock: the open-source bug-report redacted artifact (P9); remembered-mapping lifecycle (P Info); "colour" spelling.
