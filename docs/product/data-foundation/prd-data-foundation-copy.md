# Data Foundation PRD — error & state copy

Companion to [prd-data-foundation.md](prd-data-foundation.md): the shipping copy for every state that PRD names.
Each `E<n>` ID is the contract between a state and the rows that cite it; IDs never renumber. The Status column holds a value from that PRD's [Legend](prd-data-foundation.md#legend). A state whose name is marked ‹P1› does not appear until its own rows land, and a marked action or sentence inside an unmarked state is withheld until then — the P1-seam convention the sibling PRDs use; a sentence marked ‹OQ 14› is withheld until that question closes. The collection-unavailable state is the [capture PRD's E26](../capture-mode/prd-capture-mode-copy.md#error--state-copy)'s and the halt and pre-flight states are the [device PRD's](../device-management/prd-device-management-copy.md#error--state-copy); neither is restated here.

**Placeholders.** ⟨…⟩ stands for something filled in when the state is shown: ⟨code⟩ a Swatch Code, ⟨collection⟩ a collection's name, ⟨n⟩ a count of items or readings, ⟨size⟩ a file size, ⟨path⟩ a file location, ⟨version⟩ an app or file version, ⟨date⟩ a date. A sentence whose count would be zero is left out rather than shown as "0"; a sentence carrying a count agrees with it — "1 reading", "⟨n⟩ readings"; and a named provisional constant renders as the number it currently holds, never as its name.

## Error & state copy

Written in the Cataloger's and the Data consumer's vocabulary: plain language, names the recovery, never SDK-speak, never a schema word. Every promise below is backed by a requirement row in the PRD ([process rule 5](prd-data-foundation.md#legend)).

| ID | State | Headline | Body | Primary action | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| E13 | Where your file should live | Choose where SpectroCapture keeps your colours | Everything you scan goes into one file that belongs to you — you can move it, copy it, back it up, and open it in other tools. We suggest ⟨path⟩. A folder that syncs itself can damage a file that's open, so if you choose one, quit SpectroCapture before it syncs. | Use ⟨path⟩; Choose a different folder | ⌛️ Ready for Alignment |
| E1 | File from a newer app | This file was made by a newer version of SpectroCapture | You can look at it, but you can't change it until you update. Your file hasn't been touched. It was written by version ⟨version⟩ and this is version ⟨version⟩. | Check for updates; Open it read-only | ⌛️ Ready for Alignment |
| E16 | File older than this app opens | This file is older than this version can open | It was made by version ⟨version⟩ and this version opens files from ⟨version⟩ onwards. It's open read-only and nothing has been changed. The last version that can open it is ⟨version⟩. | Open it read-only; Where to get ⟨version⟩ | ⌛️ Ready for Alignment |
| E2 | Upgrading your file | Getting your file ready for this version | A copy of your file exactly as it is now is saved at ⟨path⟩ first, so nothing you've scanned is at risk. You'll see how far along it is as it goes, and that copy stays where it is until you remove it — it's how you go back to version ⟨version⟩ if you ever want to. | None (it finishes on its own) | ⌛️ Ready for Alignment |
| E3 | Upgrade didn't finish | Your file is exactly as it was | Something stopped partway, so nothing was changed. Your file is still version ⟨version⟩ and this app expects ⟨version⟩, and the copy taken before the attempt is at ⟨path⟩. If there wasn't room for that copy, the upgrade was never started — free up space at ⟨path⟩ and try again. | Try again; Show me the copy | ⌛️ Ready for Alignment |
| E4 | A reading can't be read | One reading on ⟨code⟩ can't be read | The measurement itself is unreadable, so this swatch has no value until you scan it again — or go back to one of its ⟨n⟩ earlier readings, which are all still here. Everything else in ⟨collection⟩ is fine, and nothing has been removed. | Scan ⟨code⟩ again; Use the previous reading | ⌛️ Ready for Alignment |
| E5 | This file has a problem | Some of this file can't be read | It's open read-only so nothing makes it worse. The app can write out everything it can still read into a fresh file and leave this one untouched for you to keep. | Save what's readable; Leave it alone | ⌛️ Ready for Alignment |
| E6 | Export a collection | Export ⟨collection⟩ | ⟨n⟩ swatches, with everything you imported, the full wavelength readings, and all six colour spaces — XYZ, Lab, LCh, Luv, sRGB, and HSL — each one saying which illuminant, observer, and measurement condition it came from. Colours that fall outside standard sRGB are marked in the file too — most screens can't show these accurately. ⟨n⟩ readings taken against the Demo Device are marked simulated; ⟨n⟩ taken without wavelength data are marked too, along with how their average was worked out. ⟨n⟩ columns you imported share a name the app uses and go out under a slightly different one, which the file records. The file says which version of the export format it is. | Export current readings; Export with full history ‹P1›; Choose where | ⌛️ Ready for Alignment |
| E7 | Export didn't finish — no room | There isn't room for this export | ⟨collection⟩ is untouched, nothing was written, and no half-finished file was left behind. ⟨path⟩ needs about ⟨size⟩ free. | Choose somewhere else | ⌛️ Ready for Alignment |
| E17 | Export didn't finish — can't write there | That folder can't be written to | ⟨collection⟩ is untouched, nothing was written, and no half-finished file was left behind. Choose somewhere you can write to, or change who can write to ⟨path⟩. | Choose somewhere else | ⌛️ Ready for Alignment |
| E18 | Export didn't finish — destination gone | That folder isn't there any more | ⟨collection⟩ is untouched, nothing was written, and no half-finished file was left behind. The drive may have been disconnected, or ⟨path⟩ moved. | Choose somewhere else | ⌛️ Ready for Alignment |
| E8 | Delete a swatch | Delete ⟨code⟩? | This is the one thing that does throw data away. It takes the reading it has now, its ⟨n⟩ earlier readings, and everything recorded about scanning it. You can undo this until you quit SpectroCapture ‹P1›; after that it's final, so export first if you want to keep any of it. | Cancel; Export first; Delete | ⌛️ Ready for Alignment |
| E14 | Delete a collection | Delete ⟨collection⟩? | This throws away all ⟨n⟩ swatches in it, the ⟨n⟩ readings behind them, and everything recorded about scanning them. You can undo this until you quit SpectroCapture ‹P1›; after that it's final, so export first if you want to keep any of it. | Cancel; Export first; Delete | ⌛️ Ready for Alignment |
| E15 | No room for your file | There's no room left where your file is | ⟨path⟩ is full, so the last thing you did wasn't saved and your file is exactly as it was before it. Free up space, or move your file somewhere with room, and try again. | Try again; Show me the file | ⌛️ Ready for Alignment |
| E9 | Read the file again | Read your file again | SpectroCapture shows your file as it was when it last read it, and it can't tell when something else writes to it — so this is here whenever you want it. Reading your file in another tool is always safe; changing it while the app is open isn't. ‹OQ 14› If something else has changed the file, the app will say so here. | Read it again | ⌛️ Ready for Alignment |
| E10 | The file is open somewhere else | Another copy of SpectroCapture has this file | Two copies writing to one file would damage it. Close the other one and try again, or open a different file here. | Try again; Open a different file | ⌛️ Ready for Alignment |
| E11 | Correction or re-measurement? | ⟨code⟩ was scanned again on ⟨date⟩ — has the swatch changed, or was the old reading wrong? | Until you answer, the new reading is ⟨code⟩'s value and the old one is kept and marked as not yet settled either way. Both answers keep the old reading — the difference is what it means later: if the swatch itself has changed, both readings were true and both belong in its history; if the first reading was a mistake, the old one is marked as never having been right and is left out of anything showing how ⟨code⟩ changed over time. | The swatch has changed; The old reading was wrong; Ask me later | ⌛️ Ready for Alignment |
| E12 | Save a copy of your file | Save a copy | This makes a complete, safe copy even while the app is running — ⟨size⟩, everything, including the history behind every swatch. Copying the file yourself in Finder while SpectroCapture is open can give you a damaged copy. Copies stay where you put them until you remove them. | Save a copy | ⌛️ Ready for Alignment |
