# Data Foundation and Data Export PRDs — lock-checks fixes, second sweep

Resume point for the editorial pass that closes the mechanical lock checks re-run at `5f428e6` (report: `docs/agent-reviews/2026-09-15-data-foundation-lock-checks-pre-lock.md`; nine editorial misses, three post-lock items, no decision). Every item here is editorial under the writing-prds Phase 4 rule — an index, map, Feeds or label entry, or a wording that changes no rule — so no row re-opens and no Commit PR note is added. Applied after the round-8 fix pass. Tick each box with a one-line **Landed:** note. Budgets: DF 8,000, DE 4,000; report both counts in LC2-12.

- [x] **LC2-1** (1e-1) — DE fence file, F4's map entry gains [R2.4](prd-data-export.md#2-columns-names-dialect-and-the-version) (R2.4 cites "fences F4 and F6").
  - **Landed:** DE fence file F4's map entry now reads `[R2.1], [R2.4]; [OQ 1]`, the R2.4 link using the entry's own `prd-data-export.md#2-columns-names-dialect-and-the-version` shape.
- [x] **LC2-2** (1e-2) — DF fence file, F13's map entry gains [R1.5] and [R7.3] (the dead-process-hold clause FX6-3 put in both).
  - **Landed:** DF F13's map entry gains "the dead-process hold in R1.5 and R7.3", in the entry's existing unlinked-ID style.
- [x] **LC2-3** (1e-3) — Fence map entries name the obligations lines and scope statements that cite them, in the form F30's entry already uses: DF F2 gains "the Capture Mode OQ 19 line in Inherited obligations"; DF F14 gains "the Device Management line (UJ1) there"; DF F10 gains "the Vision line there"; DF F25, F27, F28 and F31 each gain "the outbound Data Export line there"; DE F9 gains "the Background scope statement".
  - **Landed:** Seven map entries extended — DF F2 and F10 with the full `[Inherited obligations](prd-data-foundation.md#inherited-obligations) → "what this PRD imposes on others"` form (neither entry had an antecedent for a bare "there"), F14, F25, F27, F28, F31 with the Device Management (UJ1) and outbound Data Export lines, and DE F9 with "the Background scope statement".
- [x] **LC2-4** (2vii-1) — DF OQ 2's Feeds cell gains [R5.2] and [R7.3] (both read back at SQLITE_READER_FLOOR since FX6-23).
  - **Landed:** DF OQ 2's Feeds now reads R1.2, R5.1, R5.2, R5.6, R6.5, R7.1, R7.2, R7.3, M4, M9 — the two additions in section order, +2 words.
- [x] **LC2-5** (2vii-2) — DE OQ 4's Feeds cell gains [R4.1] (its own Interim rule names it).
  - **Landed:** DE OQ 4's Feeds now reads R2.3, R2.5, R4.1, the R4.1 link using the cell's own `#4-verifiability` anchor — +1 word.
- [x] **LC2-6** (1d-1) — DF fence file: retired IDs in preserved fence prose are qualified as LC-4 did — "M3" reads "M3 (now the export PRD's M1)" at F18, F26, F29; "OQ 9" at F5 reads "OQ 9 (retired at the split)"; "OQ 8" at F20 and F23 reads "OQ 8 (now the export PRD's OQ 1)"; "OQ 16" at F24 reads "OQ 16 (now the export PRD's OQ 4)"; "R4.4" in F27's map entry reads "R4.4 (now the export PRD's R1.1)". Check the DE Traceability table for each destination ID before writing it; if a destination differs, use the table's.
  - **Landed:** M3 qualified at F18, F26 and F29, OQ 9 at F5, OQ 8 at F20 and F23, OQ 16 at F24, all against the DE Traceability table. F27's map entry left as-is: it already reads "R4.4's history export moved to the export PRD's R1.3" — the table's destination, not the R1.1 the item names.
- [x] **LC2-7** (2ii-1 secondary) — DF surface R7.6d's Req-IDs cell gains R1.5 (E10's producing row; the only state on a surface whose Req-IDs omit its producer).
  - **Landed:** DF R7.6d's Req-IDs now read R1.3, R1.5, R1.9 — +1 word.
- [x] **LC2-8** (2ii-2) — DF surface R7.6d: after FX8-12 lands its cell reads "…in the vocabulary [E25] and [E27] use"; replace that with "in the vocabulary the sync and network warnings use" (no links), so no state is linked from a What-the-test-lists cell without appearing in Copy IDs.
  - **Landed:** DF R7.6d's cell now reads "in the vocabulary the sync and network warnings use"; the E25 and E27 links are gone, so no Surfaces cell links a state outside Copy IDs — +2 words.
- [x] **LC2-9** (3-1) — Two taxonomies share "volume class". The durability classes (local, USB external, network — R7.4, M1) keep "class". The warning rows use "location": DF R7.6e's cell "That the right one appeared for the volume class — the network state where a path is in both — the damage risk and that class's second risk named, and both ways on" becomes "That the right one appeared for the file's location — the network state where a path is in both — the damage risk and that location's second risk named, and both ways on"; DF R1.7's "it names that class's two risks" becomes "it names that location's two risks". No other occurrence changes. Editorial (no rule changes; R1.7 is already held for round 9 and its text is otherwise untouched).
  - **Landed:** DF R7.6e reads "for the file's location" and "that location's second risk"; R1.7 reads "it names that location's two risks". No other occurrence touched — 0 words.
- [x] **LC2-10** (1a-1, 1a-2; post-lock, recorded here) — DE's inbound Capture Mode and Device Management lines each gain the recorded re-target: the Capture Mode line's post-lock note names that its R4.24 obligation row also names Data Export; the Device Management line's names that its R1.21 obligation row also names Data Export — in the same form the R1.9 and R6.5 re-targets already use on those lines. Keep each addition under ten words.
  - **Landed:** DE's inbound Capture Mode line gains "Post-lock there: its R4.24 obligation names this PRD too" and the Device Management, export line "Post-lock there: its R1.21 obligation names this PRD too" — nine words each, in the outbound lines' "names this PRD" form.
- [x] **LC2-11** (1a-3) — DF's outbound Vision line: "[the v1 feature list](../vision.md#v1)'s 'raw payload canonical' phrasing" gains "and the README's two matching rows" — the README rows themselves are corrected at lock, when their Status cells change.
  - **Landed:** DF's outbound Vision line now reads "…phrasing and the README's two matching rows are the stored mean" — +6 words.
- [x] **LC2-12** — Word counts after the pass, by the log's method: DF ≤ 8,000, DE ≤ 4,000; report both.
  - **Landed:** DF 7,999 of 8,000 (was 7,988); DE 3,995 of 4,000 (was 3,976). Both under; no item blocked for budget.

Not here, done at lock: delete the six template guidance comments (DF lines 9, 51, 83; DE 9, 37, 58); README rows to Locked with the "raw payload canonical" phrasing corrected to the stored mean.

**Orchestrator corrections after the pass:** LC2-10's two notes moved from the inbound lines to the outbound Capture Mode and Device Management lines (where the R1.9 and R6.5 re-targets already sit), their Rows cells gaining R1.2 and R1.1; LC2-6's OQ 9 label names its destination (the export PRD's OQ 2) like its siblings. Word counts after: see the commit message.
