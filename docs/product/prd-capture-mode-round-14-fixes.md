# Capture Mode PRD — round 14 fixes

Resume point for round 14 (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 14"). One box per new finding from every lens. Fences F1–F44 plus the amendments (F6, F31 (1)–(2), F36, F38, F40, F42 (1)–(5), F44 clarification) are the authorization. Tick each box as it lands. Row IDs never renumber; no new rows are expected. Line numbers refer to the round-13 commit. WHAT not HOW; the operator's vocabulary; no technical jargon. Read R3.6, R3.7, R3.8, R3.11, R7.1, R7.11, R8.1, R8.3, R8.6, R8.12, R8.15, R9.6, R9.11, E23, E39, E42 and UJ3.3 step 4 together first. R8.1 and R8.15 are long: replace sentences, do not add, and never restate a rule that lives in another row.

## Status flips (authorized by the round-14 dispositions in the log)

- [x] **FX14-0** — Flip to 🤝 Aligned R3.11, R7.11, E25; return E42 to ⌛️ (re-opened in round 14); the 5 held rows stay ⌛️ with "edited in round 14, re-review" where a fix edits them (held rows a fix does not touch keep their current note). Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 14, re-review". Report final counts. — **applied.** Flipped to 🤝 Aligned: R3.11 (no fix edits it — FX14-4 leaves it unchanged) and E25 (no fix edits it). R7.11 flipped too and was then edited by FX14-7, so it ends "⌛️ Ready for Alignment — edited in round 14, re-review", as rule 3 directs. E42 returned to ⌛️ "re-opened in round 14" and was edited by FX14-6, keeping the re-opened note as R7.11 did in round 13. All five held rows carry a round-14 edit, so each now reads "edited in round 14, re-review" (R7.1, R8.1, R8.6, R8.15, E39). Four 🤝 rows were edited and returned to ⌛️ "edited in round 14, re-review": R3.6, R8.3 and R3.8 (FX14-2, as the log anticipated) and R11.12 (FX14-6 and FX14-3 gave a state a variant and a conditional line, which §12's rules make R11.12 enumerate). Nothing else moved and no new rows. Final: 188 🤝, 11 ⌛️ of 199 (144 R, 44 E, 11 M).

## Blocker

- [x] **FX14-1** (PMM PMM14-1, Staff S14-1 — Blocker; PM PM14-1; **F44 clarification (1)**) — One precedence, stated once in R8.6 (`:783`) and pointed at from R8.1's fourth state (`:778`), R8.15 (`:784`), and UJ3.3 step 4 (`:350-352`): the review taken from the end-early summary's "now" is the end-of-run review however many rows are still pending, because the operator has already asked to stop — leaving it completes or ends the session early; F44's detour is only a review the operator opened from the collection surface while the bulk session was still capturing. UJ3.3 step 4 bullet 1 gains "or from the stop-for-now summary after I'd already chosen to stop"; bullet 2 is scoped to "as a detour, with swatches still waiting and no stop asked for". — **done.** The precedence is now stated once, in R8.6: the review at the end of the run has two ways in — reached with nothing left waiting in the queue, or taken from the end-early summary's "now" — the second however many swatches are still waiting, because by then the operator has already asked to stop, so leaving it completes the session or ends it early exactly as the first does. One review is the detour: the one opened from the collection surface while the session was still capturing, with swatches still waiting and no stop asked for. R8.1's fourth state no longer restates either rule; it names the two ways in and points at R8.6 for which exit follows. R8.15 keys its three branches the same way (end of the run / detour / look-through) and cites R8.6 rather than re-deriving it. UJ3.3 step 4 bullet 1 now reads "came here with nothing left waiting in the queue, or from the stop-for-now summary after I'd already chosen to stop"; bullet 2 is scoped to "as a detour, with swatches still waiting and no stop asked for".

## Majors

- [x] **FX14-2** (Arch A14-1, Test F14-1; **F44 clarification (2)**) — R8.6 (`:783`), R8.1 (`:778`), R8.15 (`:784`): a detour review returns the operator to the row they were on when they entered it, at sample 0, the way R8.12 and R9.11 already say ("returns the queue to the row it left"); "at the remembered row" goes. The detour moves nothing: R3.6 (`:658`) and R8.3 (`:780`) scope the remembered-row update and the review-selected mark to the end-of-queue review (reached with the queue exhausted or from the summary's "now"); R3.8 (`:660`) consistent. UJ3.3 step 4 bullet 2 already says "where I was" — keep it. — **done.** "At the remembered row" is gone from all three: R8.6, R8.1 and R8.15 now say the detour puts the operator back on the swatch they were on when they opened it, at its first sample, the way R8.12 and R9.11 already say it, and R8.6 gives the reason — a detour moves nothing, neither the collection's remembered row nor a review-selected mark. R3.6 and R8.3 are scoped to match: the remembered-row update and the review-selected mark belong to the end-of-run review (reached with the queue exhausted, or taken from the summary's "now"), and a row selected in a detour review moves neither. R3.8's exception now names the end-of-run review too. UJ3.3 step 4 bullet 2 keeps "where I was" — it now says the swatch I was on, at its first sample.
- [x] **FX14-3** (Arch A14-2, PMM PMM14-2) — R8.15 (`:784`): "finishes no session of the operator's" becomes "completes no session under R8.6" with the three look-through cases as they are; add the one-row case — where a one-row session started from the look-through list is under way, settling the row it is on ends that session with its partial set let go (R7.1, R3.11), and "Leave them all set aside" says so; the list's leave actions never touch the row currently under the instrument otherwise. E39 (`:916`) body: after "waiting on you any more": "If that finishes a run you'd left part-way, you'll see its summary on your collection." R11.12 names the line as conditional. — **done.** R8.15's look-through branch reads "completes no session under R8.6", with the three cases as they were (ended stays ended; paused or otherwise held cannot be finished this way; interrupted and unresumed is closed as complete by R7.11's rule). The one-row case follows as the exception under the instrument: where a one-row session started from the list is under way, the swatch it is on is one of the rows being settled, so settling it ends that session with its part-finished set let go, cue and all, and the line saying that swatch is back among the swatches set aside (R7.1, R3.11, E42) — and no other row under the instrument is touched by either leave action. E39 gains the conditional line "If that finishes a run you'd left part-way, you'll see its summary on your collection." and R11.12 lists it among the conditional lines a test can assert.
- [x] **FX14-4** (Test F14-2, Staff S14-3) — R8.1 (`:778`) held-state sentence: the reason is the lift — the operator can lift their own pause for one row (R9.6), but the guard's pause lifts only on force-resume (R5.11) and a halt only when its cause clears (device PRD §5) — so nothing scans from the list under either; dealing with the cause closes the look-through and puts the operator back on the capture surface at the held row, so there is no state with the list open beside a live bulk session. Drop the R4.2 citation. R3.11 (`:663`) unchanged: "exactly two cases" stays true. — **done.** R8.1's held-state sentence is re-keyed to the lift rather than the trigger, and the R4.2 citation is gone: the operator's own pause lifts for a single swatch where they stand, which is why a scan from the list is a row inside that session (R9.6), while the guard's pause lifts only on a force-resume (R5.11) and a halt only when its cause has cleared (device PRD §5) — and either of those is dealt with on the capture surface, which closes the look-through and puts the operator back on the held row with its samples where they were, so the list is never left open beside a run that has come back to life. The open-ended "until the operator has dealt with the cause" is gone with it. R3.11 is unchanged and its "exactly two cases" still holds.
- [x] **FX14-5** (Test F14-3) — UJ3.3 step 4 third bullet (`:352`): "If I opened the list from my collection with nothing running → what I decide here just settles those swatches; a run I'd paused stays paused, one I'd already ended stays ended, and one that was cut short is closed as complete once nothing is left waiting and nothing is set aside undecided." — **done.** UJ3.3 step 4's third bullet now reads: "If I opened the list from my collection with nothing running → what I decide here just settles those swatches; a run I'd paused stays paused, one I'd already ended stays ended, and one that was cut short is closed as complete once nothing is left waiting and nothing is set aside undecided."
- [x] **FX14-6** (Staff S14-2) — E42 (`:919`): a second body variant for a swatch that goes back to the set-aside list rather than the queue — "You'd started ⟨code⟩ before this. It goes back to its first sample and is back in the set-aside swatches." — used when the abandoned row was set aside (a one-row session from the look-through list, R7.1, R8.2). R11.12 (`:862`) enumerates the variant; R7.1 (`:753`) cites it. — **done.** E42 is written in two variants, the way E20 and E44 are: in the queue — "You'd started ⟨code⟩ before this. It goes back to its first sample and is still waiting in the queue." — and set aside — "You'd started ⟨code⟩ before this. It goes back to its first sample and is back in the set-aside swatches." R11.12 names the pair among the states written in variants, and R7.1's one-row clause now says the line is worded for where that swatch goes back to, citing E42's set-aside variant. R8.15's one-row sentence uses the same line.

## Minors

- [x] **FX14-7** (Staff Q3) — R7.11 (`:763`): the mid-run trigger names both ways the last outstanding row can clear — a settle decision (R8.5, R8.15) or a capture from the look-through list — so the enumeration matches the headline "whenever that becomes true". — **done.** R7.11's mid-run trigger now names both ways the last outstanding row can clear in the set-aside list: a decision that settles it, one row at a time or all at once (R8.5, R8.15), or a scan the operator takes from the list that captures it (R8.1, R3.11) — so the enumeration matches the headline "the moment that becomes true".
- [x] **FX14-8** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph still lists every control named by a row; Traceability paragraph names F44 as clarified; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Every row a fix touched carries the right status (see FX14-0); the 11 open rows are R3.6, R3.8, R7.1, R7.11, R8.1, R8.3, R8.6, R8.15, R11.12, E39, E42. The §12 Labels paragraph still lists every control a row names and needed no change: no fix added or renamed a control, and the two leave actions R8.15 names are still the ones E39 and E27 carry. Two states changed shape, and R11.12 now names both — E42's in-the-queue and set-aside variants, and E39's conditional line about the run that decision finished. The Placeholders paragraph is untouched and still true: E42's two variants use the same ⟨dropped⟩ and ⟨code⟩ it already governs, and E39's new line carries no placeholder. The Traceability paragraph no longer calls F44 new and instead lists it among the fences carrying a clarification — seven of them now — with what the clarification settles. All six mermaid diagrams are untouched and still fenced; UJ3.3 step 4 is numbered-list text and stayed so. No literal ampersand-escape anywhere in the file. Counts: 144 R, 44 E, 11 M — 199 rows, 188 🤝 and 11 ⌛️. **Residue, not fixed:** R8.6's second sentence still states the early-end rule before the sentence that scopes it, which reads as unconditional until the next sentence lands; and R8.1's fourth state now names the surface an offer was taken from as part of the detour test, a few sentences after the row says the look-through-versus-review question turns on the live bulk session "and never on the surface the offer was taken from" — the two are about different questions (which way the list opens, versus which exit a review has) but they sit close together (see the report).

## Item-for-item map (verified against each review)

PM: PM14-1→FX14-1 · non-findings→log only. Staff: S14-1→FX14-1 · S14-2→FX14-6 · S14-3→FX14-4 · Q1→FX14-1 · Q2→log only · Q3→FX14-7 · Q4→FX14-6. Test: F14-1→FX14-2 · F14-2→FX14-4 · F14-3→FX14-5. Arch: A14-1→FX14-2 · A14-2→FX14-3 · table suggestion→log only. PMM: PMM14-1→FX14-1 · PMM14-2→FX14-3. agy: advisory; see log.
