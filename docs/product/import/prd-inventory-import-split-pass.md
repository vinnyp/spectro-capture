# Inventory import PRD — the F49 split pass

Resume point for the F49 split (fence file: `../capture-mode/prd-capture-mode-fences.md`, F49 and its clarification (1)). F45 (two sentences per row, never expand) and F46's companion-file shape bind. Tick each box with a one-line note. No rule changes anywhere: this pass moves text and rewrites citations.

## Layout

- `docs/product/import/prd-inventory-import.md` — the PRD, same skeleton as `../capture-mode/prd-capture-mode.md`: header (Author, Status: draft, Companion files line), Background, User Journeys (pointer to the journeys file), Requirements with Vocabulary, Legend, Traceability, Surfaces, then the numbered sections, Inherited obligations, the copy pointer section, Success Metrics, Open Questions. Sections: `### 1. Reading the file` (R2.1–R2.4), `### 2. Target, mapping and the matching rule` (R2.5–R2.8), `### 3. Preview and commit` (R2.9–R2.14), `### 4. Demo Device and verifiability` (the import preview test row, from R11.15h).
- `docs/product/import/prd-inventory-import-journeys.md` — UJ 2, UJ 2.1, UJ 2.2 moved verbatim with their diagrams, under the same header shape as the capture journeys file; renumbered as UJ 1, UJ 1.1, UJ 1.2 is NOT done — journeys keep their names (UJ 2, 2.1, 2.2) so citations from the capture journeys stay true.
- `docs/product/import/prd-inventory-import-copy.md` — E4–E14, E38, E40 moved verbatim, same header and table shape as the capture copy file, IDs kept.
- `docs/product/import/prd-inventory-import-fences.md` — F4 and F11 copied verbatim with the line "Copied from the capture fence file under F49; the capture file remains the record of the decision date" and a fence → row map for the import rows; F49 itself stays in the capture fence file and is cited.
- `docs/product/import/prd-inventory-import-oq-results.md` — empty results file with the header (no import OQ is answered yet).

## Boxes

- [x] **SP-1** — Rows. R2.1–R2.14 become R1.1–R1.4, R2.1–R2.4, R3.1–R3.6 in the import PRD in the same order (R2.1→R1.1, R2.2→R1.2, R2.3→R1.3, R2.4→R1.4, R2.5→R2.1, R2.6→R2.2, R2.7→R2.3, R2.8→R2.4, R2.9→R3.1, R2.10→R3.2, R2.11→R3.3, R2.12→R3.4, R2.13→R3.5, R2.14→R3.6). Text unchanged except citation targets. Status "🤝 Aligned"; Commit PR column empty. The import PRD's Traceability section carries this old→new ID table. _Done — 14 rows moved into §1–§3 on SP-1's map, text byte-identical except the citation targets; all 🤝 Aligned, Commit PR empty; the old→new table is in [Traceability](prd-inventory-import.md#traceability)._
- [x] **SP-2** — Copy. E4–E14, E38, E40 move to the import copy file verbatim; their E cites inside moved rows point at `prd-inventory-import-copy.md#error--state-copy`. The capture copy file keeps the rows' IDs listed as retired in its header line ("Retired under F49, never reused: E4–E14, E38, E40") and drops the rows. _Done — E4–E14, E38, E40 moved verbatim to `prd-inventory-import-copy.md`; their in-row cites now point there, and the capture copy file's header line lists them as retired under F49._
- [x] **SP-3** — Journeys. UJ 2, UJ 2.1, UJ 2.2 (the whole "Cluster 2 — Import an inventory" block) move to the import journeys file verbatim. Every capture journey that links to them now links across (`../import/prd-inventory-import-journeys.md#…`). The capture journeys file's cluster list notes the move. _Done — the whole Cluster 2 block (UJ 2, 2.1, 2.2 and their flow chart) moved verbatim with its numbers; UJ 1 step 6 links across, and the capture cluster heading carries the move note._
- [x] **SP-4** — The import PRD's verifiability row. R11.15h's rule ("what R2.9 requires of its preview") becomes the import PRD's `R4.1`: "A test lists what the import preview shows, without matching wording ([R3.1])." The capture PRD retires R11.15h from its lettered table (row removed; the lead row's "(d, e)" text unchanged). _Done — R11.15h became the import PRD's R4.1 in `### 4. Demo Device and verifiability`; the capture lettered table now runs a–g and the lead row's "(d, e)" is untouched._
- [x] **SP-5** — Metrics and OQs. M7 moves to the import PRD as M1 (text unchanged); M8 stays in capture with its start point cited as the import PRD's commit row (`R3.2`). OQ 12 moves to the import PRD as OQ 1 (Feeds: R1.2, M1). OQ 13 stays in capture; its Feeds cell cites the import PRD's R3.1 in place of R2.9, and the import PRD's Legend names IMPORT_BUDGET as a provisional constant closed by the capture PRD's OQ 13. _Done — M7 → import M1 (OQ 12 cite renumbered to OQ 1); M8 stays in capture and now starts at the import PRD's R3.2; OQ 12 → import OQ 1 (Feeds R1.2, M1); OQ 13 stays and feeds import R3.1; the import Legend names IMPORT_BUDGET as closed by capture's OQ 13._
- [x] **SP-6** — Cross-document citations from capture. Every remaining `[R2.n](#2-inventory-import)` in the capture PRD becomes a link into the import PRD with the new ID (R1.2 and R9.2 → `[R2.3](../import/prd-inventory-import.md#2-target-mapping-and-the-matching-rule)`; the Legend's OQ line; the Data Foundation and Collection Mode obligation lines). The capture PRD's §2 heading and story line are removed; the Legend's retired-ID line gains "Retired under F49, never reused: R2.1–R2.14, R11.15h; E4–E14, E38, E40 (moved to the inventory-import PRD)". The Traceability section's section list and the Surfaces table's "Import flow" row are updated (the Import flow surface is the import PRD's; the capture Surfaces table drops it and the Collection surface's entry-point list keeps "import" as a cross-PRD entry). _Done — every `[R2.n](#2-inventory-import)` in the capture PRD is now a cross-PRD link (0 local anchors left); §2 heading, Traces, both story lines and both tables removed; Legend retired line, Traceability range, Surfaces table and the Collection surface's import entry all updated._
- [x] **SP-7** — Inherited obligations both ways. Capture's Inherited-obligations table gains a line: "Inventory import — the one matching rule for Swatch Codes and collection names ([R2.3]); import ends at ready-to-capture with every imported row pending, in file order ([R3.2]); rows: R1.2, R9.2, M8". The import PRD's Inherited-obligations table names Data Foundation (the matching rule and the all-or-none commit), Collection Mode (renaming an imported column), and Capture Mode (a collection's pending rows are what a session scans). _Done — capture's obligations table gains the "Inventory import" line worded as SP-7 states; the import PRD's names Data Foundation, Collection Mode and Capture Mode._
- [x] **SP-8** — Fences. F4 and F11 copied into the import fence file; the capture fence file's map lines for F4 and F11 say "moved to the import PRD (F49)"; clarification (1) under F49 records the owner's two answers (R2.7 moves; M8 stays). _Done — F4 and F11 copied verbatim into `prd-inventory-import-fences.md` with the provenance line and an import-row fence map; the capture map's F4 and F11 lines record the move. Clarification (1) under F49 was already in the capture fence file and needed no edit._
- [x] **SP-9** — README and vision. `docs/product/README.md` gains a row for Inventory import (status: Drafted from the locked capture rows under F49, pending its verification round) and lists its companions; use cases U1 stays with Capture Mode and import is noted as the wedge's first step. vision.md is not edited. _Done — README gains row 3 Inventory Import (Drafted under F49, pending its verification round) with its four companions listed, sections renumbered 4–8, U1 stays with Capture Mode with import named as its first step, and the CSV-import feature row reassigned. AGENTS.md §7's "(not yet written)" note updated._
- [x] **SP-10** — Checks. Every citation in both PRDs and all companions resolves (rows exist; anchors match GitHub slugs); no `[R2.` cite remains in the capture PRD; no `&amp;`; every requirement row in both PRDs at most two sentences; all mermaid blocks in both journeys files render (`node <scratchpad>/mmcheck/check.mjs <file>`); counts: capture PRD rows (expect 161 − 14 − 1 = 146 R+M rows, all 🤝), import PRD rows (14 R + 1 R4.1 + M1 = 16), copy files (capture 31, import 13), word counts of all files. _Done — 779 links checked across the ten files, 2 problems and both pre-existing (see the report); 0 local `#2-inventory-import` anchors; no `&amp;`; 0 rows over two sentences in either PRD; all 6 mermaid blocks render; counts and word counts in the report._
