# PRD: Device Management

Status: locked (2026-09-08)

Companion files: the journeys are in [prd-device-management-journeys.md](prd-device-management-journeys.md), the shipping copy in [prd-device-management-copy.md](prd-device-management-copy.md), the answers to part-closed open questions in [prd-device-management-oq-results.md](prd-device-management-oq-results.md), and the owner's decisions in [prd-device-management-fences.md](prd-device-management-fences.md).

# Background

SpectroCapture requires a connection to a spectrophotometer to acquire color information. The scope of this PRD covers all the requirements for the use cases, features, and user journeys related to managing that connection. The device lifecycle end-to-end — activation, discovery and pairing, calibration, the pre-flight gate, and in-session failure and recovery — is drawn as one diagram in the [device workflow](prd-device-management-journeys.md#device-workflow).

### Use cases

Defined in the [Vision doc](../vision.md#use-cases):

| # | Use case | Persona | What serves it |
| :--- | :--- | :--- | :--- |
| U3 | **Start a session with a healthy device** | Cataloger | known-device management (BLE + USB), calibration-due prompt, QR-tile calibration |
| U8 | **Scan where there is no internet** | Cataloger | offline-first design + per-device pre-authorization ("Offline use through ⟨date⟩") |
| U9 | **Contribute code without hardware** | Contributor | mock-device layer behind the device-service interface; what CI exercises |

### Feature list

Defined in the [Vision doc](../vision.md#feature-list):

| Release | Feature | Serves | Notes |
| :--- | :--- | :--- | :--- |
| v1 | Spectro 2/L connect (BLE + USB) | U3 |  |
| v1 | Known-device management | U3 |  |
| v1 | Tile calibration with due-prompts | U3 |  |
| v1 | Offline operation + per-device pre-authorization | U8 | "Offline use through ⟨date⟩" surfaced in the device panel |
| v1 | Mock-device layer | U9 | App runs, tests, and takes contributions with no hardware or key; what CI exercises |

## User Journeys

Every journey — UJ1 through UJ6.1, and the five flow diagrams — is in [prd-device-management-journeys.md](prd-device-management-journeys.md). UJ2, the start-acquisition path this document gates, is repeated here in short.

**UJ2. Start acquisition.**

1. Open the app and turn on the device.
2. The app auto-reconnects to the last known connected device.
3. The pre-flight health check evaluates on connect as an advisory — battery, calibration currency, authorization window, storage headroom — with guided calibration if the gate tripped and "Extend offline use" if the window has lapsed.
4. The app is ready for acquisition.
5. The first capture action against the loaded queue re-runs the checks as the blocking gate, and the session starts.

## Requirements

### Legend

**Priority**

Priority is build order within v1, not a cut line — everything in this document ships in v1: P0 is the first build phase, P1 the second, P2 last (fence F8). A P0 state whose remediation is owned by a P1 row renders in the first build with guidance and without that action, the action arriving with its row; that is the P1-seam convention [R4.4](#4-pre-flight-device-health) states and [R2.12](#2-licensing--pre-authorization) applies.

Provisional constants: every TBD-on-spike constant is a named provisional constant carrying its candidate value where one exists, and its [Open Questions](#open-questions) id; mechanisms build against these constants, and no provisional constant ships in a release without its OQ resolved (fence F7); a dogfood build is not a release for that rule, so the first sessions run on candidate values and produce the data those questions need, and where a constant has no candidate the engineering plan sets a dogfood value, which the OQ's closer then replaces (F7, clarification (1)). The constants in this document, each with the row that holds it and its OQ id:

- the discovery timeout — candidate 20 s vendor default, documented 10 s floor ([R1.5](#1-device-pairing), OQ 6)
- the calibration pre-emptive-check constant — candidate one-third of the typical interval ([R3.3](#3-calibration), OQ 3)
- the renewal trigger point — candidate one-third of the window remaining ([R2.11](#2-licensing--pre-authorization), OQ 8)
- the battery halt threshold, the strictly-higher resume threshold, and the pre-flight battery boundaries ([R4.1](#4-pre-flight-device-health), [R5.1](#5-mid-session-device-failure), [R5.14](#5-mid-session-device-failure), OQ 4b)
- the pre-flight storage warn and block boundaries ([R4.1](#4-pre-flight-device-health), OQ 5)
- the reconnect bound, n attempts / t minutes ([R5.6](#5-mid-session-device-failure), OQ 7)
- the liveness timeout and the disconnect-detection tolerance ([R5.1](#5-mid-session-device-failure), [R5.13](#5-mid-session-device-failure), OQ 20b)
- the simulated device's default latency, standing for the real device's measured timing ([R6.27](#6-mock-device-layer), OQ 10)
- the pairing retry count and the calibration retry count — candidate 3 consecutive failures ([R1.8](#1-device-pairing), [R3.5](#3-calibration), OQ 28)

An "OQ n" written without a qualifier is this document's; another PRD's are named as that PRD's. A row ID written without a qualifier is this document's; a row cited from another PRD names that PRD in the link. "First run" means the app has no saved device yet.

**For the engineering plan.** These are the plan's to answer, not this document's.

- Five open questions hold every constant that has no candidate value: the battery halt, resume, and pre-flight boundaries (OQ 4b), the storage warn and block boundaries (OQ 5), the reconnect bound (OQ 7), the liveness timeout and disconnect-detection tolerance (OQ 20b), and the simulated device's default latency, whose candidate is a measurement (OQ 10).
- P0 rows that defer, in whole or in part, to an open question with no interim rule: [R1.14](#1-device-pairing) (OQ 24), [R4.1](#4-pre-flight-device-health) (OQ 4b, 5), [R5.1](#5-mid-session-device-failure) (OQ 4b, 20b), [R5.6](#5-mid-session-device-failure) (OQ 7), [R5.13](#5-mid-session-device-failure) (OQ 20b), [R5.14](#5-mid-session-device-failure) (OQ 4b), [R6.27](#6-mock-device-layer) (OQ 10).
- Phase-0 predecessors: the storage ADR [R3.3](#3-calibration) waits on, the sandboxing ADR (OQ 19), the module-layout ADR, the shell-side seam ADR and the macOS CI lane the every-PR obligations need (OQ 21), and the capture PRD's OQ 16 amendment. The Data Foundation, Collection Mode, and Telemetry PRDs this document hands obligations to are not yet written.
- The hardware spike's scope is every OQ whose closer is "hardware" except OQ 4b, 7 and 20b, which close on first-build halt logs; the questions an owner or a checked-in artifact closes without hardware (OQ 12, 13, 14, 19, 21, 22, 24) run now; OQ 3 waits on OQ 2's hardware probe, and OQ 4b, 5, 7, 20b and 28 wait on halt-log and dogfood data from the first build. The plan orders the three tracks.

**Status**

A status cell in this document holds one of the six values below and nothing else; which round a row was last edited in lives in the [review log](../../agent-reviews/2026-09-08-prd-device-management-refactor-peer-reviews.md). The [Open Questions](#open-questions) table has its own three values — open, residual, and answered.

- ⌛️ Ready for Alignment - Waiting for cross-functional team to align on requirements
- ✋ Needs Discussion - Cross-functional team needs to discuss with PM
- 🤝 Aligned - Cross-functional team aligned on the requirement
- 🦺 In Progress - Implementation in flight (Optional status)
- ✅ Completed - Implementation completed & merged. PR # & link added in the "Commit PR" column.
- ✂️ Deferred - Deferred from current release

### Traceability

Three row-ID families, one per dispositionable table, all under one rule: an ID is assigned once and never renumbered — a row that is cut or deferred keeps its ID rather than freeing it for reuse. Fence F9 assigned every ID in this document and its companions; before that pass the rows carried none, so no ID here supersedes an earlier one.

- Every requirement row in [§1](#1-device-pairing) through [§6](#6-mock-device-layer) carries an ID of the form `R<section>.<n>` — for example `R5.7`.
- Every state row in the [copy file](prd-device-management-copy.md#error--state-copy) carries an ID of the form `E<n>`.
- Every metric row in [Success Metrics](#success-metrics) carries an ID of the form `M<n>`.

The Commit PR column is where a row maps onto the work that lands it. Owner decisions F1–F9 are recorded in the [fence file](prd-device-management-fences.md); where a row names one it is for provenance only, and no row re-argues one. Which rows carry each fence is that file's "Fence → row map".

### Surfaces

The device experience lives on four surfaces. The device panel and the pre-flight readiness panel are one surface — the device panel, with a readiness zone; wording elsewhere in this PRD refers to that zone, never to a separate panel. First-run setup states are non-modal ([R1.23](#1-device-pairing)).

| Surface | Shows | The [copy](prd-device-management-copy.md#error--state-copy) states that render here |
| :--- | :--- | :--- |
| Device picker | Discoverable devices (including the Demo Device — always listed, license or not; live discovery requires an active license), display names, add/switch entry points | No device found ([E1](prd-device-management-copy.md#error--state-copy)); Bluetooth permission denied ([E2](prd-device-management-copy.md#error--state-copy)); license needed before searching ([E6](prd-device-management-copy.md#error--state-copy)); switch-device confirmation ([E31](prd-device-management-copy.md#error--state-copy)) |
| Saved devices | The known-device list and removal | Remove-device confirmation ([E32](prd-device-management-copy.md#error--state-copy)) |
| Device panel | Device identity, "Offline use through ⟨date⟩", the four readiness checks (readiness zone), ambient indicators and advisories | Device not reachable at launch ([E16](prd-device-management-copy.md#error--state-copy)); no internet to authorize device ([E3](prd-device-management-copy.md#error--state-copy)); invalid license ([E4](prd-device-management-copy.md#error--state-copy)); license expired ([E5](prd-device-management-copy.md#error--state-copy)); vendor service unreachable ([E10](prd-device-management-copy.md#error--state-copy)); extend-offline-use no internet ([E11](prd-device-management-copy.md#error--state-copy)); offline-use expired, offline and online ([E17](prd-device-management-copy.md#error--state-copy), [E18](prd-device-management-copy.md#error--state-copy)); license missing spectral data ([E7](prd-device-management-copy.md#error--state-copy)); device not covered by your license ([E8](prd-device-management-copy.md#error--state-copy)); device held by another app ([E9](prd-device-management-copy.md#error--state-copy)); pre-flight blocked states ([E19](prd-device-management-copy.md#error--state-copy), [E20](prd-device-management-copy.md#error--state-copy)); calibration due ([E21](prd-device-management-copy.md#error--state-copy)); pairing and calibration failed and isn't-working ([E12](prd-device-management-copy.md#error--state-copy), [E13](prd-device-management-copy.md#error--state-copy), [E14](prd-device-management-copy.md#error--state-copy), [E15](prd-device-management-copy.md#error--state-copy)) |
| Capture surface | The live capture loop, the persistent simulated indicator, halt and paused states | Every halt state ([E23](prd-device-management-copy.md#error--state-copy)–[E30](prd-device-management-copy.md#error--state-copy)); the simulated-readings indicator, whose collection-mode banner is [E22](prd-device-management-copy.md#error--state-copy) |

The simulated-readings collection banner ([E22](prd-device-management-copy.md#error--state-copy)) renders in collection mode, a surface the Collection Mode PRD owns ([Inherited obligations](#inherited-obligations)). Bluetooth permission denied also renders on the device panel at launch ([R1.14](#1-device-pairing)). First-run setup states — the activation states, pairing, and calibration — render in the device panel; no-device-found renders in the device picker.

### 1. Device Pairing

Traces [UJ1](prd-device-management-journeys.md#uj-1-first-run), [UJ1.1](prd-device-management-journeys.md#uj-11-cannot-complete-a-first-run), [UJ3](prd-device-management-journeys.md#uj-3-remove-a-saved-device); serves [U3](../vision.md#use-cases).

#### As a Cataloger, I can connect a new spectrophotometer so that I can acquire color information into my collection.

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R1.1 | v1 | P0 | Adding a new device is an explicit user action. | 🤝 Aligned |  |
| R1.2 | v1 | P0 | The user can connect to a Nix Spectro 2 or a Spectro L. | 🤝 Aligned |  |
| R1.3 | v1 | P0 | The app discovers a device over both USB and BLE; either transport alone is enough to connect, and both are supported. Discovered devices list strongest signal first (vision [J1](../vision.md#j1-first-run-cataloger); [SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), vendor-confirmed UX guidance). | 🤝 Aligned |  |
| R1.4 | v1 | P0 | The user activates the SDK by entering their vendor-issued license credential — two strings, options and signature — and activation precedes every device operation, including discovery ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 1). The shipped app never embeds a credential and every user supplies their own ([R2.3](#2-licensing--pre-authorization), [AGENTS.md §5](../../../AGENTS.md#5-hardware--the-public-repo-boundary)); the offline-use window is shown as "Offline use through ⟨date⟩" ([R2.9](#2-licensing--pre-authorization)). | 🤝 Aligned |  |
| R1.5 | v1 | P0 | When discovery does not find the device, the app shows the no-device-found state ([E1](prd-device-management-copy.md#error--state-copy)) with a retry action. The discovery search runs the vendor-default 20 s as its provisional constant, with a documented 10 s floor — devices advertise once per second ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 6). | 🤝 Aligned |  |
| R1.6 | v1 | P0 | The list of discovered devices is de-duped by device identity — (kind, model, serial) — so a device detected over both USB and BLE resolves to one record. Discovery entries carry id, type, name, signal strength, and transport but no serial ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 15), so post-connect dedup is the primary path and [R1.7](#1-device-pairing)'s provisional display form stands until the serial is read. | 🤝 Aligned |  |
| R1.7 | v1 | P0 | Every surface where a device appears — the device picker, Saved devices, switch and remove confirmations, and the [copy states](prd-device-management-copy.md#error--state-copy) — shows the display name "⟨model⟩ (⟨serial⟩)", for example "Spectro 2 (19H1)", and "Demo Device (⟨serial⟩)" for a simulated device; the serial is the identity key within (kind, model), and full device identity is (kind, model, serial). While the serial is not yet readable, those surfaces show the provisional display form instead — the discovery entry's device name and transport, "⟨name⟩ — Bluetooth" ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs). | 🤝 Aligned |  |
| R1.23 | v1 | P0 | A pairing attempt that fails shows the pairing-failed state ([E12](prd-device-management-copy.md#error--state-copy)) with a retry action. First-run setup states render non-modally on the device panel or in the device picker, both of which stay reachable, so leaving one never needs an action. | 🤝 Aligned |  |
| R1.8 | v1 | P1 | After repeated consecutive pairing failures (retry count is a TBD-on-spike constant, candidate 3, [Legend](#legend), OQ 28), the app shows the pairing-isn't-working state ([E13](prd-device-management-copy.md#error--state-copy)) with cause guidance and an explicit "Leave setup for now" exit to the collection, so pairing is never an inescapable loop. The counter resets on a successful attempt and on leaving setup — navigating away from a setup state by that action or otherwise — and the states that block first-run progress (device authorization ([E3](prd-device-management-copy.md#error--state-copy)), pairing, calibration) offer the same exit, which dismisses the state and on first run returns the device panel to its normal content. | 🤝 Aligned |  |

#### As a Cataloger, I can connect to a spectrophotometer I've used before so that I can quickly resume acquiring colors to my collection

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R1.9 | v1 | P0 | A device becomes a known device after its first successful pairing. The known-device record is keyed by the serial within (kind, model) and persists across app launches; pairing always happens under an already-active license ([R1.4](#1-device-pairing), OQ 1). | 🤝 Aligned |  |
| R1.10 | v1 | P0 | On launch the app auto-reconnects to the last-used known device with zero user action, so a device that is powered on and in range needs no connection step between opening the app and scanning; simulated devices are excluded and connect only by explicit selection. Whether the SDK's Bluetooth-ID-based reconnect offers auto-reconnect any handle over USB is unverified (OQ 23). | 🤝 Aligned |  |
| R1.11 | v1 | P1 | When the last-used known device is reachable over both USB and BLE, the app connects over USB (proposed preference — OQ 9), chosen once at connect time and never migrated during a live session; a mid-session transport change is a disconnect under [§5](#5-mid-session-device-failure). On reconnect after a transport-loss halt, any transport that reaches the bound serial is acceptable, and the in-use transport stays visible. | 🤝 Aligned |  |
| R1.12 | v1 | P0 | The app connects to exactly one device at a time — the vendor SDK is single-device, single-session, and a device serves one app at a time ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) — and a connect that fails because another app holds the device shows the device-held state ([E9](prd-device-management-copy.md#error--state-copy)). Connecting a different device requires an explicit switch action with a confirmation naming both devices ([E31](prd-device-management-copy.md#error--state-copy)); auto-reconnect never switches devices on its own. | 🤝 Aligned |  |
| R1.13 | v1 | P0 | A capture session binds to the device identity it started with; a different device cannot take over an in-progress session. | 🤝 Aligned |  |
| R1.14 | v1 | P0 | If the last-used known device is unreachable at launch, the app shows the non-modal device-not-reachable state naming "⟨model⟩ (⟨serial⟩)" ([E16](prd-device-management-copy.md#error--state-copy)), keeps retrying discovery in the background, and completes the connection by itself when the device appears, with nothing for the user to dismiss. At any launch where Bluetooth authorization is denied or undetermined and the known device is not reachable over USB, the app shows the Bluetooth-permission-denied state ([E2](prd-device-management-copy.md#error--state-copy)) instead. | 🤝 Aligned |  |
| R1.15 | v1 | P0 | Single connection authority: at most one connect, disconnect, or retry attempt is in flight at any time — across background reconnect, launch auto-reconnect, and user-initiated connect and switch actions — and a user-initiated action supersedes and cancels any in-flight background attempt. Ending a session releases the session's device binding; whether the vendor SDK performs its own automatic reconnection, and whether it can be disabled or subordinated, is OQ 17. | 🤝 Aligned |  |
| R1.16 | v1 | P0 | Auto-reconnect may open a probe connection to a candidate solely to read its serial; a device whose serial does not match is released immediately, never presented as connected and never bound. The known-device record persists the per-transport reconnect handle alongside the serial — confirmed for BLE, where the SDK recalls a known device by its Bluetooth ID ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) — while the USB handle is unverified (OQ 23, whose deliverable includes the persisted handle's schema shape: nullable per-transport vs BLE-only). | 🤝 Aligned |  |
| R1.17 | v1 | P0 | When a post-connect serial read matches an existing known device, the connection is recognized as that device: the known-device record updates — the per-transport reconnect handle, for one — and the flow proceeds as a normal reconnect, with no duplicate record, no error, and no interruption. | 🤝 Aligned |  |

#### As a Cataloger, I can manage my saved devices so that my device list only contains the instruments I actually use

Vocabulary: "known device" is the internal, data-model term; the UI always says "Saved devices".

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R1.18 | v1 | P1 | The user can remove a known device via an explicit action, with a confirmation step that names the device, "⟨model⟩ (⟨serial⟩)" ([E32](prd-device-management-copy.md#error--state-copy)). | 🤝 Aligned |  |
| R1.19 | v1 | P1 | Removing a known device never deletes or alters measurements acquired with it — canonical values and version history are untouched ([AGENTS.md §4](../../../AGENTS.md#4-non-negotiables)). Removal deletes only the saved connection record. | 🤝 Aligned |  |
| R1.20 | v1 | P1 | Re-adding a removed device follows the normal new-device pairing flow ([R1.1](#1-device-pairing)–[R1.8](#1-device-pairing) and [R1.23](#1-device-pairing)); there is no special re-add path. | 🤝 Aligned |  |
| R1.21 | v1 | P0 | Every measurement permanently records the acquiring device's identity — kind (live or simulated), model, serial, and firmware version, measurement modes being firmware-dependent ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) — as an immutable snapshot independent of saved-device records. Removing a saved device can never alter, orphan, or cascade into measurements. | 🤝 Aligned |  |
| R1.22 | v1 | P0 | Device identity for dedup and display stays (kind, model, serial), with the firmware version belonging to the measurement snapshot only. A simulated device can never collide with or occupy a live device's record, even with the same serial. | 🤝 Aligned |  |

### 2. Licensing & Pre-Authorization

Traces [UJ1](prd-device-management-journeys.md#uj-1-first-run), [UJ1.1](prd-device-management-journeys.md#uj-11-cannot-complete-a-first-run), [UJ6](prd-device-management-journeys.md#uj-6-pre-authorize-before-going-offline), [UJ6.1](prd-device-management-journeys.md#uj-61-authorization-window-expired-while-offline); serves [U8](../vision.md#use-cases).

Vocabulary: "pre-authorization" is internal vocabulary and never appears in the UI — the button is "Extend offline use" and the panel string is "Offline use through ⟨date⟩".

Two authorization layers, named ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 1, OQ 8): app-level license activation is offline, client-side signed, and happens on every launch — it needs no internet; device-level serial authorization is online for an unknown device, then cached ~30 days. "No internet" failures belong to the device layer, never to activation. The SDK's private-label exemption from online device checks exists ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) and is out of scope for v1 — recorded here so it isn't rediscovered.

#### As a Cataloger, I can enter my Nix license once so that the app doesn't ask me for it again

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R2.1 | v1 | P0 | The user enters the license credential once — two vendor-issued strings, options and signature, and the entry accepts both ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs). It is stored locally on the user's machine and silently re-activated, offline, on every later launch; activation is session-only per SDK docs, which makes launch-time silent re-activation load-bearing for [R1.10](#1-device-pairing). | 🤝 Aligned |  |
| R2.2 | v1 | P0 | A silent re-activation failure at launch routes to the invalid-license ([E4](prd-device-management-copy.md#error--state-copy)) or license-expired ([E5](prd-device-management-copy.md#error--state-copy)) state; the user is never re-prompted otherwise. | 🤝 Aligned |  |
| R2.3 | v1 | P0 | The license credential — both parts — never appears in the repository, in CI configuration, or in any commit ([AGENTS.md §5](../../../AGENTS.md#5-hardware--the-public-repo-boundary)); the shipped app embeds no credential and every user supplies their own. | 🤝 Aligned |  |
| R2.4 | v1 | P1 | A device's first-ever connect requires an internet connection for device-level serial authorization, while license activation itself is offline ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 1). The app surfaces this requirement clearly at pairing or at pre-flight before a session starts, never as a mid-queue failure. | 🤝 Aligned |  |
| R2.5 | v1 | P0 | License and authorization failures are distinct states, each naming its recovery: an invalid or incomplete license asks for both parts again ([E4](prd-device-management-copy.md#error--state-copy)), no internet at first device authorization asks the user to connect and retry ([E3](prd-device-management-copy.md#error--state-copy)), and a vendor auth service unreachable while the internet is up asks the user to try again later ([E10](prd-device-management-copy.md#error--state-copy)) ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs). They are never collapsed into one generic error. | 🤝 Aligned |  |
| R2.6 | v1 | P0 | A valid license, an authorized serial, and Spectro-class hardware can still lack the spectral-data feature entitlement and return no spectral data, the product's core payload ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 27). The shortfall is detected and surfaced at pairing time and in the readiness zone as its own state ([E7](prd-device-management-copy.md#error--state-copy)) — an indicator under the authorization check rather than a new check ([R4.3](#4-pre-flight-device-health)) — never discovered mid-session, and distinct from per-serial refusal ([E8](prd-device-management-copy.md#error--state-copy)) — the wording softening pending the capture PRD's OQ 16 amendment ([Inherited obligations](#inherited-obligations)). | 🤝 Aligned |  |
| R2.7 | v1 | P0 | Detection uses the SDK's post-connect capability queries, providesSpectral and providesDensity ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs); whether they answer without taking a measurement is OQ 27. | 🤝 Aligned |  |
| R2.8 | v1 | P0 | The vendor SDK carries its own mandatory analytics telemetry — events on connect, scan, and calibration carrying device type, batch ID, and SDK version, never color data ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) — an accepted, disclosed network path outside the app's own three ([R2.13](#2-licensing--pre-authorization)). The app's user-facing privacy story must disclose it, and whether it can affect offline capture is OQ 16. | 🤝 Aligned |  |

#### As a Cataloger, I can pre-authorize my device before going offline so that a full bulk session runs with no internet at all

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R2.9 | v1 | P0 | The device panel shows "Offline use through ⟨date⟩" for the connected device, the date being the earlier of the ~30-day device-authorization cache expiry from the last online check and the license's own expiry ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 8). If no API exposes the cache expiry, the app derives the date from the last successful check and labels it approximate, never presenting a derived date as exact (OQ 8). | 🤝 Aligned |  |
| R2.10 | v1 | P1 | While online, the user can invoke an explicit "Extend offline use" action that renews the pre-authorization window immediately and updates the displayed date. This assumes the device-authorization cache can be proactively renewed — unverified against SDK docs (OQ 8). | 🤝 Aligned |  |
| R2.11 | v1 | P1 | When online, the app renews the pre-authorization window silently in the background before the window runs low — the trigger point is a TBD-on-spike constant, candidate one-third of the window remaining ([v2 research §5.1](../../briefs/acquisition-experience-research-results-v2.md#5-pre-flight-and-session-readiness)) — and never interrupts the user while the current window is still valid. On renewal failure it retries with exponential backoff, 1 min → 10 min → 100 min → daily (same source), assuming proactive renewal is possible (OQ 8). | 🤝 Aligned |  |
| R2.12 | v1 | P0 | If the pre-authorization window has expired, the pre-flight check blocks an acquisition session start — with the reconnect-once state ([E17](prd-device-management-copy.md#error--state-copy)) while offline, or the offline-use-ended state ([E18](prd-device-management-copy.md#error--state-copy)) while online — and while either is shown the app renews silently whenever connectivity is present, including the moment it returns; expiry never surfaces as a mystery mid-queue failure. Under the P1-seam convention ([R4.4](#4-pre-flight-device-health)) the P0 phase renders E18 without the "Extend offline use" action, which arrives with [R2.10](#2-licensing--pre-authorization) alongside [R2.11](#2-licensing--pre-authorization)'s pre-emptive renewal. | 🤝 Aligned |  |
| R2.13 | v1 | P1 | Device-authorization renewal, opt-in telemetry (off by default), and the software-update check are the only network calls the app itself initiates in v1 — license activation is offline and is not a network call — and there are no other outbound calls of any kind, beyond the vendor SDK's own mandatory analytics ([R2.8](#2-licensing--pre-authorization)). Update checking is user-controllable, sends no system profile, and can never block, delay, or halt launch or capture. | 🤝 Aligned |  |
| R2.14 | v1 | P1 | Telemetry is opt-in and off by default, forks ship their own provider ID, and it is specified in a dedicated future PRD gated on a provider spike (fence F6; OQ 12, OQ 13, OQ 14). This supersedes the no-telemetry posture in [v2 research §10.5](../../briefs/acquisition-experience-research-results-v2.md#10-measuring-acquisition-throughput). | 🤝 Aligned |  |
| R2.15 | v1 | P0 | The app persists the offline-use end date per device — the earlier of the device-authorization cache expiry and the license's own expiry ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 8) — and treats it as the source of truth for both the displayed date and the pre-flight gate, with the vendor SDK's own refusal as a defensive backstop rather than the primary check; where no API exposes the cache expiry, the persisted date is derived from the last successful check and labeled approximate (OQ 8). If the SDK refuses while the persisted date is still valid, the app surfaces the offline-use-expired state ([E17](prd-device-management-copy.md#error--state-copy), [E18](prd-device-management-copy.md#error--state-copy)) rather than a generic error, and re-syncs the date on the next connection. | 🤝 Aligned |  |
| R2.16 | v1 | P0 | Until the spike answers per-serial refusal (OQ 18), a permanent per-device refusal may be indistinguishable from a lapsed window, so the offline-use-expired copy ([E17](prd-device-management-copy.md#error--state-copy), [E18](prd-device-management-copy.md#error--state-copy)) must not promise that reconnecting will fix it. | 🤝 Aligned |  |
| R2.17 | v1 | P0 | A session started inside the offline-use window runs to completion even if the window expires mid-session; the expiry block applies only at the next session start (fence F5). Whether the vendor SDK itself enforces mid-session expiry is OQ 11. | 🤝 Aligned |  |
| R2.18 | v1 | P1 | Invoking "Extend offline use" while offline resolves to the extend-offline-use-no-internet state ([E11](prd-device-management-copy.md#error--state-copy)) — never a spinner and never a silent failure. | 🤝 Aligned |  |
| R2.19 | v1 | P0 | The license credential at rest — both strings — is stored as a protected credential in the system credential store, never in plaintext files, the collection database, logs, or any export. Re-entry uses empty masked fields, and the stored credential is never redisplayed. | 🤝 Aligned |  |

### 3. Calibration

Traces [UJ4](prd-device-management-journeys.md#uj-4-calibrate-a-connected-device); serves [U3](../vision.md#use-cases). Calibration is strictly pre-flight — checked before a session, never during one (fence F1).

#### As a Cataloger, I can calibrate my device with a guided flow so that every session starts measurement-accurate

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R3.1 | v1 | P0 | A guided QR-tile calibration flow runs on first run, on demand from the device panel, and when the session-boundary gate trips; it walks tile placement, runs the calibration, and ends in either a confirmed success or a named failure state with a retry action ([E14](prd-device-management-copy.md#error--state-copy)). Calibration takes 5–10 s on a Spectro 2 ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; informative, not gating), and the gate rule itself is TBD pending the hardware spike (OQ 3). | 🤝 Aligned |  |
| R3.2 | v1 | P0 | The SDK's calibration-due signal is authoritative — due-ness derives from elapsed time plus ambient temperature drift, inputs only the SDK has, so the app cannot compute it from a stored constant ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; confirm on hardware — OQ 2) — and the app surfaces that due state in the readiness zone ([E21](prd-device-management-copy.md#error--state-copy)). This is a deliberate asymmetry with [§2](#2-licensing--pre-authorization), where the app is authoritative and the SDK is the backstop. | 🤝 Aligned |  |
| R3.3 | v1 | P0 | The app persists calibration-event timestamps as the pre-emptive check's input, which the storage ADR will freeze; the due signal itself stays the SDK's. The session-boundary gate fires on that due signal plus an elapsed-time pre-emptive check — a TBD-on-spike constant, candidate one-third of the typical interval ([v2 research §5.1](../../briefs/acquisition-experience-research-results-v2.md#5-pre-flight-and-session-readiness), OQ 3) — so the gate trips before a session rather than mid-run. | 🤝 Aligned |  |
| R3.4 | v1 | P0 | Capture mode is never interrupted for calibration and no calibration countdown appears in the capture loop; calibration currency shows only as an ambient indicator in the device panel's readiness zone (fence F1). Drift can also surface at measurement time as a scan-delta error ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs), which arrives under [§5](#5-mid-session-device-failure)'s halt taxonomy rather than as a calibration interrupt. | 🤝 Aligned |  |
| R3.5 | v1 | P1 | After repeated consecutive calibration failures (retry count is a TBD-on-spike constant, candidate 3, [Legend](#legend), OQ 28), the app shows the calibration-isn't-working state ([E15](prd-device-management-copy.md#error--state-copy)) with cause guidance — tile condition, lighting — and an explicit "Leave setup for now" exit to the collection, so calibration is never an inescapable loop. The counter reset and the exit's effect follow [R1.8](#1-device-pairing)'s leaving rule. | 🤝 Aligned |  |

### 4. Pre-Flight Device Health

Traces [UJ2](prd-device-management-journeys.md#uj-2-start-acquisition).

#### As a Cataloger, I can see that my device is session-ready before I start so that a bulk run never dies mid-queue for a preventable reason

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R4.1 | v1 | P0 | The device panel's readiness zone covers four device-readiness checks — power/battery state, calibration currency, authorization window, and storage headroom — plus the muted-audio alert-channel advisory ([R5.5](#5-mid-session-device-failure), settable shell-side via [§6](#6-mock-device-layer)'s seam note, [R6.9](#6-mock-device-layer)), which is not a fifth readiness check, appears only when output is verified muted, and has no pass/warn/block value. When all four checks pass the readiness zone requires no interaction and does not block session start; the pass/warn/block boundaries for the battery, calibration and storage checks are TBD pending their open questions (OQ 3, OQ 4b, OQ 5); the authorization check's block boundary is [R2.12](#2-licensing--pre-authorization)'s. | 🤝 Aligned |  |
| R4.2 | v1 | P0 | Pre-flight evaluates in three contexts: at connect, as an ambient advisory; at session start, as the blocking gate — a session starts at the user's first capture action against a loaded queue, the session-start event this PRD gates; and mid-session on wake, as a re-evaluation that may raise a halt cause under [§5](#5-mid-session-device-failure) but never blocks a start. | 🤝 Aligned |  |
| R4.3 | v1 | P0 | Each check reports pass, warn, block, or not applicable — "not applicable" is for a check that cannot apply to the connected device kind, and it neither blocks nor needs remediation. A warn state reuses the corresponding blocked state's copy ([copy file](prd-device-management-copy.md#error--state-copy)) in a non-blocking presentation, and the spectral-entitlement indicator ([R2.6](#2-licensing--pre-authorization)) surfaces under the authorization check rather than as a new check — the amendment's readiness half, already absorbed (the capture PRD's OQ 16; [Inherited obligations](#inherited-obligations)). | 🤝 Aligned |  |
| R4.4 | v1 | P0 | Every warn or block state carries a one-tap way forward — re-check after charging or connecting power ([E19](prd-device-management-copy.md#error--state-copy)), recalibrate ([§3](#3-calibration)), extend offline use or reconnect ([§2](#2-licensing--pre-authorization)), re-check after freeing disk space ([E20](prd-device-management-copy.md#error--state-copy)) — so no check ends in a dead end. Remediations owned by P1 rows arrive with those rows; in the P0 phase the corresponding states render with guidance and without that action, and the no-dead-end rule is satisfied from P1 — the P1-seam convention ([Legend](#legend)). | 🤝 Aligned |  |
| R4.5 | v1 | P0 | If the battery level is not pollable outside a measurement (OQ 4), the battery check reports the last-known level together with its age. | 🤝 Aligned |  |
| R4.6 | v1 | P0 | The storage check measures free space on the volume holding the active collection file, or, when no collection is open, on the volume where a new collection would default. | 🤝 Aligned |  |

### 5. Mid-Session Device Failure

Traces [UJ5](prd-device-management-journeys.md#uj-5-device-failure-during-a-session).

> Scope boundary (restated from [UJ5](prd-device-management-journeys.md#uj-5-device-failure-during-a-session)): this PRD owns device-level failure — detect → alert → reconnect → resume. Per-scan retry/skip/flag and the dead-letter queue belong to the [capture PRD](../capture-mode/prd-capture-mode.md).

Definition: the "current item" is the first queue row with no durably written reading, evaluated when capture resumes — so a successful save retry can never produce a duplicate. "Halt" is the internal term; the UI shows the session as paused.

#### As a Cataloger, when my device fails mid-queue, the app stops me immediately and gets me back to the current item so that I never lose a scan or scan into a dead connection

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R5.1 | v1 | P0 | Capture halts immediately on the first BLE/USB disconnect ([E23](prd-device-management-copy.md#error--state-copy)), a device that stops responding, a store-write failure, or battery falling below the operational threshold (TBD pending the hardware spike — OQ 4b); the app never continues silently (accelerated breaking, [v2 research §4.2](../../briefs/acquisition-experience-research-results-v2.md#4-mid-run-error-handling)). Disconnect detection carries a stated tolerance, TBD-on-spike and bounded by BLE supervision timeouts (OQ 20b). | 🤝 Aligned |  |
| R5.2 | v1 | P0 | The durable write completes before the queue advances — a constraint ADR-0004 inherits — and no unwritten scan is ever held across a halt except the single failed-save reading ([R5.10](#5-mid-session-device-failure)). | 🤝 Aligned |  |
| R5.3 | v1 | P0 | The error alert cannot be dismissed by the same action that advances the queue; acknowledging a failure requires a distinct, deliberate action ([v2 research §4.1](../../briefs/acquisition-experience-research-results-v2.md#4-mid-run-error-handling)). | 🤝 Aligned |  |
| R5.4 | v1 | P0 | A halt alerts through at least two senses simultaneously — an interrupting tone and a blocking on-screen state, with haptics where the hardware provides them; Spectro 2/L carry device-side haptics ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs), which may reach the heads-down user's hand directly, but whether the SDK exposes triggering them is OQ 20. An error tone pre-empts any in-flight success tone. | 🤝 Aligned |  |
| R5.5 | v1 | P0 | The blocking on-screen state is peripherally perceivable — a full-capture-surface state change, not a dialog. Pre-flight shows the advisory when system audio output is muted, so the alert channel is verified before the session ([R4.1](#4-pre-flight-device-health)). | 🤝 Aligned |  |
| R5.6 | v1 | P0 | The app auto-retries reconnection to the same device identity, bounded — after n attempts or t minutes (TBD pending the hardware spike — OQ 7) it falls back to a manual waiting state that keeps listening for the device without active retries, offering "Try reconnecting now" and "End session" ([E24](prd-device-management-copy.md#error--state-copy)). | 🤝 Aligned |  |
| R5.7 | v1 | P0 | Recovery never auto-resumes capture (fence F2): when the halt cause clears — the device reconnects or responds again, the battery level recovers, the save succeeds, or, after an unrecognized device error, the next command to the device succeeds — the app enables a single "Resume scanning" action, the deliberate acknowledgment and the only path from a halt back to capture, never bound to the queue-advance action. Resume is not available while the cause holds, and resuming resets the failure counters (manual override, [v2 research §4.2](../../briefs/acquisition-experience-research-results-v2.md#4-mid-run-error-handling)). | 🤝 Aligned |  |
| R5.8 | v1 | P0 | Resume returns to the current item; every scan the queue advanced past was durably written, so recovery re-inserts nothing and creates no duplicates (fence F3). | 🤝 Aligned |  |
| R5.9 | v1 | P0 | When multiple halt causes hold at once, the displayed state is the first-raised unresolved cause, and Resume enables only once every cause has cleared. | 🤝 Aligned |  |
| R5.10 | v1 | P0 | A completed scan whose save fails is retained across the halt; "Try saving again" re-attempts the same reading, and on success "Resume scanning" is enabled (fence F3). The three save-failure halts are distinct states — disk full ([E27](prd-device-management-copy.md#error--state-copy)), collection file unreachable ([E28](prd-device-management-copy.md#error--state-copy)), and unexpected error ([E29](prd-device-management-copy.md#error--state-copy)) — and the unexpected-error fallback never shows a raw error code. | 🤝 Aligned |  |
| R5.11 | v1 | P0 | Every halt offers an explicit "End session" action that closes the capture session from the halted state; ending from a save-failure halt explicitly warns that the one unsaved reading will be discarded and that its item remains unscanned in the queue. What happens to the un-scanned remainder of the queue is the [capture PRD](../capture-mode/prd-capture-mode.md)'s to define, not this one's. | 🤝 Aligned |  |
| R5.12 | v1 | P0 | Every halt writes a local record when the halt occurs — cause, timestamp, battery level, item index — amended on resolution with the recovery action taken and the time to recovery; a halt that never resolves remains an open record, and the record stays in the user's local store, never transmitted. Writing the record is best-effort and never itself halts, blocks, or cascades, a save-failure halt's record is persisted once the store is writable again, and this log is the data source for [M3](#success-metrics) and the input for the spike's threshold tuning. | 🤝 Aligned |  |
| R5.13 | v1 | P0 | A device that accepts no command and returns no reading within a liveness timeout (TBD-on-spike — OQ 20b) halts as "device not responding" ([E25](prd-device-management-copy.md#error--state-copy)), with the same alert and recovery contract as a disconnect. | 🤝 Aligned |  |
| R5.14 | v1 | P0 | The battery-low halt clears when the reported battery level rises above a distinct resume threshold, strictly above the halt threshold (TBD-on-spike — OQ 4b), and shows the battery-too-low halt state ([E26](prd-device-management-copy.md#error--state-copy)) until it does; connecting power alone never clears it. This assumes a pollable battery level (OQ 4); if the level is not pollable outside a measurement, a battery halt clears via an explicit user-initiated level check, so the halt is never unreachable. | 🤝 Aligned |  |
| R5.15 | v1 | P0 | A halt that occurs after one or more readings of an incomplete multi-sample set discards the partial set; the item re-scans from the first sample on resume (fence F4). | 🤝 Aligned |  |
| R5.16 | v1 | P0 | While capture is halted the scan-accept path is suppressed: a scan attempt during a halt surfaces the halt state itself rather than being accepted, so the user can never scan into a dead connection, even having missed the alert. | 🤝 Aligned |  |
| R5.17 | v1 | P0 | A halt record left open by process termination is closed as "unresolved — app terminated" on next launch, keeping [M3](#success-metrics) honest. | 🤝 Aligned |  |
| R5.18 | v1 | P1 | Quitting the app from a halted state routes through the same End-session warning ([R5.11](#5-mid-session-device-failure)), so a held unsaved reading is never silently discarded. | 🤝 Aligned |  |
| R5.19 | v1 | P1 | System sleep mid-session halts under this section as a disconnect; on wake the device-health checks re-evaluate as advisories that may raise a new halt cause — the authorization check excluded for the in-flight session ([R2.17](#2-licensing--pre-authorization)) — and "Resume scanning" remains the only path back to capture. | 🤝 Aligned |  |
| R5.20 | v1 | P1 | Every halt state posts an accessibility announcement; the capture surface is navigable by assistive technology, and the halt state is announced without requiring focus. | 🤝 Aligned |  |

Halt thresholds are provisional: per the v2 research ([§4.2](../../briefs/acquisition-experience-research-results-v2.md#4-mid-run-error-handling)), halt records are logged across the first ~10 real sessions with halting tuned offline against that data before any threshold ships as final.

### 6. Mock Device Layer

Traces [UJ1.2](prd-device-management-journeys.md#uj-12-first-run-with-no-hardware-contributor); serves [U9](../vision.md#use-cases) and vision journey [J5](../vision.md#j5-first-contribution-contributor).

#### As a Contributor, I can run and test the whole app against a simulated spectrophotometer so that I can contribute without hardware or a license key

| ID | Release | Pri | Requirement | Status | Commit PR |
| :--- | :--- | :--- | :--- | :--- | :--- |
| R6.1 | v1 | P0 | The app ships a "Demo Device (simulated — no instrument)" entry selectable in the same device picker as real hardware, with the helper text "For trying the app and for contributors. Readings are generated, not measured." — a first-class device, not a debug flag or a hidden build configuration. The picker opens without an active license and always lists the Demo Device, while live discovery within it requires an active license ([E6](prd-device-management-copy.md#error--state-copy)). | 🤝 Aligned |  |
| R6.2 | v1 | P0 | The simulated device requires no hardware and no vendor license credential; a contributor can run the full app — pairing, calibration, capture, collection — from a plain clone against it. | 🤝 Aligned |  |
| R6.3 | v1 | P0 | Selecting the Demo Device never invokes license activation and never shows the credential prompt, so a plain clone with no hardware and no credential reaches a working capture session ([UJ1.2](prd-device-management-journeys.md#uj-12-first-run-with-no-hardware-contributor)). | 🤝 Aligned |  |
| R6.4 | v1 | P0 | Every measurement from the simulated device permanently records simulated provenance ([R1.21](#1-device-pairing)), and the capture surface carries a persistent simulated indicator while a simulated device is connected. | 🤝 Aligned |  |
| R6.5 | v1 | P0 | CSV export emits a `simulated` column (true/false). In collection mode the item badge reflects the canonical value's provenance and the collection banner ([E22](prd-device-management-copy.md#error--state-copy)) shows while any item's canonical value is simulated, with version history keeping the flag regardless. | 🤝 Aligned |  |
| R6.6 | v1 | P0 | The app behaves identically against the simulated and the live device, with a closed list of exceptions: data provenance (simulated readings are always marked, never presented as measured), licensing and activation (never invoked for the Demo Device), the physical transport mechanism (the simulated device does not use real USB/BLE — transport-selection behavior itself is simulated and asserted), OS-permission surfaces, timing, and launch auto-reconnect (never attempted for simulated devices). Everything else — flows, states, error surfaces — is identical. | 🤝 Aligned |  |
| R6.7 | v1 | P0 | Against the Demo Device all four pre-flight checks ([§4](#4-pre-flight-device-health)) run and report, and the authorization check reports a distinct "not applicable — simulated" result that a test can assert. | 🤝 Aligned |  |
| R6.8 | v1 | P0 | CI exercises the simulated-device path on every PR. | 🤝 Aligned |  |
| R6.9 | v1 | P0 | The simulated device exposes an explicit configurable state surface: settable serial and model, presence on either or both transports, battery level, a settable calibration-due signal (due / not due / unknown) plus the elapsed-time and — if the SDK exposes it — temperature inputs the pre-emptive check consumes ([§3](#3-calibration)), injectable disconnects, injectable silence (the device stops responding without disconnecting), injectable unrecognized device errors (an error outside the known set), injectable "device held by another app" connect failures, injectable malformed frames, settable measurement results (deterministic and seedable), and, shell-side as [§6](#6-mock-device-layer)'s seam note says, a settable system audio output state (muted / not muted / not determinable) ([R4.1](#4-pre-flight-device-health)). Device-side haptic triggering is spike-gated (OQ 20) — no injection until the SDK's exposure is known. | 🤝 Aligned |  |
| R6.10 | v1 | P0 | One simulated licensing layer carries two distinct outcome sets, mirroring [§2](#2-licensing--pre-authorization)'s two layers: local license validation (valid / invalid-or-incomplete / expired) and the simulated device-authorization service (no internet, vendor service unreachable, expired window, window granted until ⟨date⟩, refuses despite a valid persisted window, license valid but this serial refused, spectral entitlement absent), so every [§2](#2-licensing--pre-authorization) state is reproducible without the vendor service. The two expiries — device-authorization cache expiry and license expiry — are independently settable, so the earlier-of rule is assertable in both orders and at equality. | 🤝 Aligned |  |
| R6.11 | v1 | P0 | An injectable clock, so offline-use window expiry and the renewal backoff ladder ([§2](#2-licensing--pre-authorization)) are testable in virtual time rather than wall-clock time. | 🤝 Aligned |  |
| R6.12 | v1 | P0 | An injectable network-reachability state, so online/offline transitions and every offline journey ([UJ6](prd-device-management-journeys.md#uj-6-pre-authorize-before-going-offline), [UJ6.1](prd-device-management-journeys.md#uj-61-authorization-window-expired-while-offline)) are testable without changing the machine's network. | 🤝 Aligned |  |
| R6.13 | v1 | P0 | Store fault injection — fail the Nth write, disk-full, fail on commit, store unreachable (volume detached or file moved), unclassified store error — so the halt-on-write-failure and hold-and-retry behaviors in [§5](#5-mid-session-device-failure) are testable. | 🤝 Aligned |  |
| R6.14 | v1 | P0 | Simulated discovery entries carry exactly id, type, name, settable signal strength, and transport, with the serial readable only after connect — so post-connect dedup, provisional display names, probe-and-release, and strongest-signal ordering ([§1](#1-device-pairing)) are tested against reality. | 🤝 Aligned |  |
| R6.15 | v1 | P0 | A test can set the outcome of the Bluetooth-authorization prompt (authorized / denied / undetermined), of any pairing attempt (success / failure), and of any calibration run (success / failure). | 🤝 Aligned |  |
| R6.16 | v1 | P0 | The simulated layer can present multiple simulated devices at once, each with independent identity and state — so the one-device-at-a-time rule, the switch confirmation, and session binding ([§1](#1-device-pairing)) are testable. | 🤝 Aligned |  |
| R6.17 | v1 | P0 | Every outbound network attempt the app makes is observable in tests (destination and time), including activation and renewal attempts against the simulated authorization service on the injectable clock, so the only-network-paths claim and the backoff ladder ([§2](#2-licensing--pre-authorization)) are assertable — including the negative cases, where a test can assert zero corresponding attempts with the Demo Device selected or with telemetry off. Audio and haptic cue events are likewise observable (kind, time, and whether a cue pre-empted one in flight), as are connection attempts (start, outcome, cancellation, initiator), the request payload shape of each permitted network path, and whether the device panel's other affordances stay operable while a setup state shows ([R1.23](#1-device-pairing)). | 🤝 Aligned |  |
| R6.18 | v1 | P0 | A test can set the reported free disk space, so the pre-flight storage check ([§4](#4-pre-flight-device-health)) can be induced into warn and into block. | 🤝 Aligned |  |
| R6.19 | v1 | P0 | A test can set device presence per transport — remove USB while BLE remains, for one — and observe which transport a connection is using, so the transport preference and the mid-session transport-change rule ([§1](#1-device-pairing)) are assertable. | 🤝 Aligned |  |
| R6.20 | v1 | P0 | A test can start the app from a declared persisted state — known devices, last-used device, stored credential present or absent, calibration timestamps — so first-run and later-launch scenarios are constructible without manual setup. | 🤝 Aligned |  |
| R6.21 | v1 | P0 | A SpectroDevice conformance suite runs the same behavioral checks against three subjects: the simulated device, in CI on every PR; the live device, run by a hardware owner for `needs-hardware-verify` PRs, who attaches a named results artifact (suite identity plus per-check pass/fail) to the PR; and a hardware-free subject exercising the real cross-language boundary — the contract-test obligation [ADR-0001](../../decisions/0001-rust-core-swiftui-shell.md#enforcement-gates-part-of-this-decision) already mandates. | 🤝 Aligned |  |
| R6.22 | v1 | P0 | Error-case parity is assertable: for every error the live device can produce, the simulated layer provides an injection that yields it, verified by a test that enumerates the full error set (the pinned enumeration of OQ 22), so a new live error case without a matching injection fails the build or the test suite rather than review vigilance. The injectable set includes ambient light leakage and out-of-range temperature ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs), whose per-scan UX is the [capture PRD](../capture-mode/prd-capture-mode.md)'s. | 🤝 Aligned |  |
| R6.23 | v1 | P0 | The app's device-error set is closed and exhaustively matched at the app's boundary; any SDK error the mapping does not recognize resolves to a named "unrecognized device error" — itself a halt cause with its own copy state ([E30](prd-device-management-copy.md#error--state-copy)) and its own injection. Whether the vendor SDK's error surface is closed is OQ 22. | 🤝 Aligned |  |
| R6.24 | v1 | P0 | Telemetry is fire-and-forget and can never block, delay, or halt capture. With telemetry off the app makes zero telemetry network attempts, assertable via [R6.17](#6-mock-device-layer)'s outbound-attempt observability. | 🤝 Aligned |  |
| R6.25 | v1 | P0 | Any simulated device or service seam a test did not explicitly configure fails the test when exercised, rather than answering with a plausible default. | 🤝 Aligned |  |
| R6.26 | v1 | P0 | The app always declares the Bluetooth usage description macOS requires — its absence breaks Bluetooth access and may crash the app (vision [J1](../vision.md#j1-first-run-cataloger) risk point) — verified automatically in CI on every PR. | 🤝 Aligned |  |
| R6.27 | v1 | P0 | The simulated device has a configurable latency defaulting to the real device's measured timing (OQ 10), and is never instant by default. An instant mock hides every race in the capture surface. | 🤝 Aligned |  |
| R6.28 | v1 | P2 | The simulated device can replay fixtures recorded from real hardware, including calibration sequences and malformed frames. | 🤝 Aligned |  |

Device-path PRs still need a human with real hardware to verify before merge and carry the `needs-hardware-verify` label ([AGENTS.md §5](../../../AGENTS.md#5-hardware--the-public-repo-boundary)). These requirements describe product behavior only; module layout is an open ADR ([AGENTS.md §3](../../../AGENTS.md#3-decided--recommended--open)) and is not decided here. The seams for OS surfaces — Bluetooth authorization, free disk space, the credential store, system audio output, the device picker — are shell-side test doubles, distinct from the pure-Rust SpectroDevice mock [ADR-0001](../../decisions/0001-rust-core-swiftui-shell.md) defines; the obligation is that a test can control them, wherever they live, and which ADR owns that shell-side seam family, along with the macOS CI lane these every-PR obligations require, is OQ 21.

### Inherited obligations

Each line is a requirement on the document named, not a suggestion. A row cited here carries the rule; a line with no row is one handed over whole.

**What this PRD imposes on other PRDs**

| Target PRD | Obligation | Rows |
| :--- | :--- | :--- |
| Data Foundation | Every measurement permanently records the acquiring device's identity as an immutable snapshot independent of saved-device records, and removing a saved device can never alter, orphan, or cascade into measurements; a simulated device can never collide with a live device's record even on the same serial | [R1.21](#1-device-pairing), [R1.22](#1-device-pairing) |
| Data Foundation, export | CSV export emits a `simulated` column (true/false) | [R6.5](#6-mock-device-layer) |
| Collection Mode | The simulated-readings collection banner ([E22](prd-device-management-copy.md#error--state-copy)) and the per-item simulated badge, which render in collection mode but are specified here | [R6.5](#6-mock-device-layer) |
| Capture Mode | What happens to the un-scanned remainder of the queue when a session ends from a halted state; per-scan error UX — retry / skip / flag-row — and the dead-letter queue, including the ambient-light and out-of-range-temperature cases the simulated layer must be able to inject; and session and queue persistence across app launches | [R5.11](#5-mid-session-device-failure), [R6.22](#6-mock-device-layer) |
| Telemetry | Fire-and-forget delivery that can never block, delay, or halt capture; zero telemetry network attempts with telemetry off, assertable via the outbound-attempt observability required here; and the Add Device task-completion and task-duration metrics, which this PRD defines but cannot measure without it | [R6.17](#6-mock-device-layer), [R6.24](#6-mock-device-layer), [M4](#success-metrics), [M5](#success-metrics) |

**What other PRDs impose on this one**

A row in the "Rows here" column is the row the obligation attaches to, not the rule's wording; the naming PRD's own row is authoritative for that.

| Source PRD | Obligation | Rows here |
| :--- | :--- | :--- |
| Capture Mode, on the simulated layer | The record of every measurement asked for and a measurement a test can hold open; a declared starting device state; a clock a test controls and an observable, closed cue set; per-mode measurements identical to a live reading ([the capture PRD's R11.5](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability), [the capture PRD's R11.6](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability), [the capture PRD's R11.7](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability), [the capture PRD's R11.8](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability)) | [R6.9](#6-mock-device-layer), [R6.11](#6-mock-device-layer), [R6.17](#6-mock-device-layer), [R6.20](#6-mock-device-layer) |
| Capture Mode, on the parity gate | Capture flows, states, and error surfaces identical against the Demo Device and a live instrument within this PRD's closed list of exceptions, as amended by [the capture PRD's R11.3](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability) and [the capture PRD's R4.26](../capture-mode/prd-capture-mode.md#4-the-scan-loop); every failure that PRD relies on producing on demand has a simulated twin, CI exercises it on every PR, and a live error without one fails the build — the live half being the human `needs-hardware-verify` gate, not CI. Also the always-visible simulated indicator and the permanent simulated mark on every Demo Device reading | [R6.4](#6-mock-device-layer), [R6.6](#6-mock-device-layer), [R6.8](#6-mock-device-layer), [R6.22](#6-mock-device-layer) |
| Capture Mode, on [§5](#5-mid-session-device-failure) | Three restatements: ending a session from a halt routes through the End-session warning first, the held unsaved reading discarded and its row left pending; a halt left open at termination is closed as "unresolved — app terminated"; and system sleep is a halt, not an interruption. Two amendments: this section's "current item" is that PRD's remembered row whenever nothing was jumped or reordered, and a resume after an interruption is a new session start, so the runs-to-completion and never-interrupted-for-calibration carve-outs are re-established rather than carried across it ([the capture PRD's R3.4](../capture-mode/prd-capture-mode.md#3-the-capture-session), [the capture PRD's R7.5](../capture-mode/prd-capture-mode.md#7-pause-end-interruption-and-resume), [the capture PRD's R7.14](../capture-mode/prd-capture-mode.md#7-pause-end-interruption-and-resume)) — and [R5.18](#5-mid-session-device-failure), P1 here, must move with the amendment carrying these notes (that PRD's OQ 16) | [R5.11](#5-mid-session-device-failure), [R5.17](#5-mid-session-device-failure), [R5.18](#5-mid-session-device-failure), [R5.19](#5-mid-session-device-failure) |
| Capture Mode, on [§6](#6-mock-device-layer) | The Demo Device's trigger source — on-screen or keyboard, since there is no physical button — joins the closed exception list, falling away again if that PRD's OQ 2 answers that the instrument's button never reaches the app; the configurable-latency row ([R6.27](#6-mock-device-layer)) is in the first build phase, pacing at that PRD's DEMO_SCAN_CYCLE, already absorbed; and the settable state surface gains a spectral-data-available-or-absent switch ([the capture PRD's R11.3](../capture-mode/prd-capture-mode.md#11-demo-device-and-verifiability), [the capture PRD's R4.26](../capture-mode/prd-capture-mode.md#4-the-scan-loop); that PRD's OQ 16) | [R6.6](#6-mock-device-layer), [R6.9](#6-mock-device-layer), [R6.27](#6-mock-device-layer) |
| Capture Mode, on [§2](#2-licensing--pre-authorization), [§4](#4-pre-flight-device-health) and [§7](#7-error--state-copy) | A license valid in every other way but carrying no spectral data is a capability the app tells the user about, not a dead end: the gate shows the shortfall as an indicator rather than blocking, the copy state gains a way forward, and §2's wording is softened ([the capture PRD's R4.22](../capture-mode/prd-capture-mode.md#4-the-scan-loop), [the capture PRD's R4.24](../capture-mode/prd-capture-mode.md#4-the-scan-loop); that PRD's OQ 16) | [R2.6](#2-licensing--pre-authorization), [R4.3](#4-pre-flight-device-health), [E7](prd-device-management-copy.md#error--state-copy) |
| Inventory Import | [The import PRD's §4](../import/prd-inventory-import.md#4-demo-device-and-verifiability) builds on [§6](#6-mock-device-layer) without restating it, so the mock layer stays the single source for what a contributor can run without hardware | [§6](#6-mock-device-layer) |

One wording note, not a behavior: the [capture PRD](../capture-mode/prd-capture-mode.md) says "the instrument" where this document's halt copy, which renders on the same capture surface, says "the device", and one noun should win when this document is next amended.

### 7. Error & State Copy

The shipping copy for every error, waiting, and blocked state in this PRD is in [prd-device-management-copy.md](prd-device-management-copy.md). Confirmation dialogs and warn-level presentations reuse the copy of the rows that require them ([R4.3](#4-pre-flight-device-health)). Copy is written in the Cataloger's vocabulary: plain language, names the recovery, never SDK-speak.

Every state there is a distinct, named state whose identity is stable even when its copy changes, so behavior can be asserted independently of wording; that table is a P0 requirement and every state row carries its own status, a value from the [Legend](#legend). The journeys and the requirement rows link to a state rather than restating its copy. A state whose name is marked ‹P1› does not appear until its own rows land, and a marked action inside an unmarked state is withheld until then — the P1-seam convention ([R4.4](#4-pre-flight-device-health)).

**Placeholders.** ⟨…⟩ stands for something filled in when the state is shown. The tokens in use: ⟨model⟩ the device's model name, ⟨serial⟩ its serial, ⟨name⟩ the discovery entry's device name, and ⟨date⟩ an offline-use end date. Where the serial is not yet readable, a state naming "⟨model⟩ (⟨serial⟩)" shows [R1.7](#1-device-pairing)'s provisional display form — the discovery entry's name and transport, "⟨name⟩ — Bluetooth" — instead.

## Success Metrics

Numeric targets below are proposals, not commitments — the device's scan cycle time is unknown until the hardware spike (OQ 10), and it sets the floor for anything time-based.

| ID | Metric | Definition (start event, end event, statistic, population) | Candidate target | Method | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| M1 | Launch-to-first-scan (known device) | Start: process launch. End: the first measurement durably written. Statistic: median, on a known device with calibration gates intact ([STRATEGY.md key metric](../../../STRATEGY.md#key-metrics)). Population: ≥ 20 dogfooded launches with pre-flight passing. | ≤ 30 s when the device is on and pre-flight passes | Dogfooding: timed real sessions, measured locally | 🤝 Aligned |
| M2 | Install-to-first-scan (first run) | Start: install or first launch on a clean state. End: the first measurement durably written — enter credential → pair → calibrate → scan ([UJ1](prd-device-management-journeys.md#uj-1-first-run) end to end). | ≤ 15 min with device and key in hand | Timed walkthroughs on a clean state (n stated with the result) | 🤝 Aligned |
| M3 | Device-caused session stalls | The count of open halt records in [R5.12](#5-mid-session-device-failure)'s halt log — halts that never resolved to a guided recovery, directly assertable. | 0 — every halt resolves to reconnect, charge, retry the save, resume, or end session | [R5.12](#5-mid-session-device-failure)'s halt log: local records of cause, recovery action, and time to recovery | 🤝 Aligned |
| M4 | Add Device task completion (v1.x — requires the Telemetry PRD) | The share of device-add attempts completed: an attempt is the user invoking Add Device, a completion is a known-device record being written. | ≥ 95% task completion | Telemetry events (opt-in) — pending the telemetry spike (OQ 12); opt-in sample only, a directional signal never quotable as a population statistic | 🤝 Aligned |
| M5 | Add Device task duration (v1.x — requires the Telemetry PRD) | Median time from invoking Add Device to a known-device record being written. | ≤ 2 min | Telemetry events (opt-in) — pending the telemetry spike (OQ 12); opt-in sample only, a directional signal never quotable as a population statistic | 🤝 Aligned |

## Open Questions

| # | Question | Decision so far | Interim rule | Closer | Feeds | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | Activation-before-discovery sequencing | Answered by the SDK audit: app-level activation precedes every device operation including discovery, and device-level serial authorization happens at connect ([results](prd-device-management-oq-results.md#oq-1--activation-before-discovery-sequencing)). | The rows build on that sequencing as written. | Observe on hardware: discovery and connect attempted before and after activation — hardware. | [R1.4](#1-device-pairing), [R1.9](#1-device-pairing), [R2.4](#2-licensing--pre-authorization), [device workflow](prd-device-management-journeys.md#device-workflow) | residual |
| 2 | Calibration state exposure | Due-ness is SDK-signaled from elapsed time plus ambient temperature drift, so a stored per-instrument constant is the wrong shape ([results](prd-device-management-oq-results.md#oq-2--calibration-state-exposure)). | The SDK's due signal is authoritative and the app stores only calibration-event timestamps. | Probe the SDK's calibration state surface on hardware — a due flag, a timestamp, the temperature input? — hardware. | [R3.2](#3-calibration), [R6.9](#6-mock-device-layer) | residual |
| 3 | Calibration gate design | None. Given SDK-signaled due-ness, what remains is the elapsed-time pre-emptive check constant and whether the gate should also weigh expected session length. | Candidate one-third of the typical interval, with the gate firing on the SDK's due signal alone. | Owner decision once OQ 2 is answered — owner, after OQ 2. | [R3.1](#3-calibration), [R3.3](#3-calibration), [R4.1](#4-pre-flight-device-health) | open |
| 4 | Battery level capability | None. The docs show low battery arriving in the measurement status ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; [results](prd-device-management-oq-results.md#oq-4--battery-level-capability)). | If a level is not pollable outside a measurement: last-known level with its age at pre-flight, and an explicit user-initiated level check to clear a halt. | Probe the battery surface on hardware — hardware. | [R4.1](#4-pre-flight-device-health), [R4.5](#4-pre-flight-device-health), [R5.1](#5-mid-session-device-failure), [R5.14](#5-mid-session-device-failure) | open |
| 4b | Battery constants | None, and no numbers until the spike. | None — the mid-session halt threshold, the strictly-higher resume threshold, and the pre-flight boundaries stay unset. | Halt-log data from the first ~10 real sessions — hardware, after OQ 4. | [R4.1](#4-pre-flight-device-health), [R5.1](#5-mid-session-device-failure), [R5.14](#5-mid-session-device-failure) | open |
| 5 | Storage headroom thresholds | None. | None — the pre-flight warn and block boundaries for disk space stay unset. | Owner decision with dogfood data — owner. | [R4.1](#4-pre-flight-device-health), [R4.6](#4-pre-flight-device-health) | open |
| 6 | Discovery timeout | Answered by the SDK audit: 20 s vendor default with a documented 10 s floor, devices advertising once per second ([results](prd-device-management-oq-results.md#oq-6--discovery-timeout)). | The 20 s default is the provisional constant and 10 s the floor. | Confirm the timing behavior on hardware — hardware. | [R1.5](#1-device-pairing) | residual |
| 7 | Reconnect retry bounds | None. | None — the n attempts / t minutes bound before falling back to manual waiting stays unset. | Tune against real-session halt logs — hardware. | [R5.6](#5-mid-session-device-failure) | open |
| 8 | Offline-use window | Answered in part by the SDK audit: a ~30-day device-authorization cache, two expiries, gate on the earlier ([results](prd-device-management-oq-results.md#oq-8--offline-use-window)). | The persisted earlier-of date is the source of truth, labeled approximate where it is derived; proactive renewal is assumed. | Probe the authorization surface on hardware — is the cache expiry readable, at what precision, and can the cache be renewed proactively? — hardware. | [R2.9](#2-licensing--pre-authorization), [R2.10](#2-licensing--pre-authorization), [R2.11](#2-licensing--pre-authorization), [R2.15](#2-licensing--pre-authorization) | residual |
| 9 | USB-vs-BLE transport preference | None. [R1.11](#1-device-pairing)'s USB preference is proposed, not confirmed against real transport behavior. | Prefer USB when both transports reach the device. | Side-by-side transport behavior on hardware — hardware, after OQ 23. | [R1.11](#1-device-pairing) | open |
| 10 | Scan cycle time | None, and no candidate yet; it sets the floor for every time-based target. | None — the simulated device's default latency stands for it. | Measure scan cycles on hardware — hardware. | [R6.27](#6-mock-device-layer), [M1](#success-metrics), [M2](#success-metrics) | open |
| 11 | Mid-session expiry: SDK enforcement | None. [R2.17](#2-licensing--pre-authorization) guarantees a session started inside the window runs to completion (fence F5). | A session runs to completion; if the SDK hard-stops, that becomes a [§5](#5-mid-session-device-failure) halt. | Let the window lapse mid-session on hardware — hardware. | [R2.17](#2-licensing--pre-authorization), [R5.1](#5-mid-session-device-failure) | open |
| 12 | Telemetry provider | None. A TelemetryDeck-like provider is the working shape, and it needs a spike before the dedicated Telemetry PRD (fence F6). | Telemetry is off by default and no provider is chosen. | Provider spike — owner. | [R2.13](#2-licensing--pre-authorization), [R2.14](#2-licensing--pre-authorization), [M4](#success-metrics), [M5](#success-metrics) | open |
| 13 | Per-fork telemetry provider ID | Forks supply their own provider ID so no fork data reaches the project and vice versa (fence F6). | The Telemetry PRD's to design. | Owner design — owner. | [R2.14](#2-licensing--pre-authorization) | open |
| 14 | Telemetry opt-in UX | None. Off by default is settled (fence F6); how and when users are offered opt-in is not. | The Telemetry PRD's to design. | Owner design — owner. | [R2.14](#2-licensing--pre-authorization) | open |
| 15 | Serial at discovery | Answered by the SDK audit: discovery carries id, type, name, signal strength, and transport but no serial, so post-connect dedup is the primary path ([results](prd-device-management-oq-results.md#oq-15--serial-at-discovery)). | Dedup after connect, showing the provisional display form until the serial is read. | Confirm the discovery payload on hardware — hardware. | [R1.6](#1-device-pairing), [R1.7](#1-device-pairing), [R1.16](#1-device-pairing), [R6.14](#6-mock-device-layer) | residual |
| 16 | SDK network behavior | Answered in half by the SDK audit: mandatory analytics events on connect, scan, and calibration, carrying no color data ([results](prd-device-management-oq-results.md#oq-16--sdk-network-behavior)). | The SDK's telemetry is disclosed as an accepted network path outside the app's own three. | Network observation on hardware, offline and firewalled: does SDK telemetry block, delay, or retry offline, is there a suppression hook, and what else does the SDK reach for? — hardware. | [R2.8](#2-licensing--pre-authorization), [R2.13](#2-licensing--pre-authorization) | residual |
| 17 | Vendor SDK auto-reconnect | None. | The app holds single connection authority regardless. | Observe reconnect behavior on hardware — can the SDK's own reconnection be disabled or subordinated? — hardware. | [R1.15](#1-device-pairing) | open |
| 18 | Per-serial refusal | Confirmed possible by the SDK audit ([results](prd-device-management-oq-results.md#oq-18--per-serial-refusal)). | The offline-use-expired copy must not promise that reconnecting will fix it. | Induce a refusal on hardware, or get vendor confirmation of what the SDK reports — hardware. | [R2.15](#2-licensing--pre-authorization), [R2.16](#2-licensing--pre-authorization), [R6.10](#6-mock-device-layer), [E8](prd-device-management-copy.md#error--state-copy) | residual |
| 19 | Sandbox entitlement conflict | None. The vendor documents four required macOS sandbox entitlements — Bluetooth, serial, USB, network client ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs) — while Apple's current reference lacks the serial entitlement ([results](prd-device-management-oq-results.md#oq-19--sandbox-entitlement-conflict)). | None — sandboxed vs unsandboxed is undecided. | Owner decision plus a sandboxed build experiment — owner. | The sandboxing ADR; [UJ1.1](prd-device-management-journeys.md#uj-11-cannot-complete-a-first-run)'s USB fallback | open |
| 20 | Liveness & haptic capability | None. | Not-responding is treated as its own halt cause, and no haptic injection ships until the SDK's exposure is known. | Probe the SDK's event and haptics surface on hardware — is liveness distinct from disconnect, and is haptic triggering exposed? — hardware. | [R5.4](#5-mid-session-device-failure), [R5.13](#5-mid-session-device-failure), [R6.9](#6-mock-device-layer) | open |
| 20b | Liveness & disconnect constants | None. BLE supervision timeouts bound the disconnect-detection tolerance. | None — the liveness timeout and the tolerance stay unset. | Tune against real-session halt logs — hardware, after OQ 20. | [R5.1](#5-mid-session-device-failure), [R5.13](#5-mid-session-device-failure) | open |
| 21 | Shell-side seams & macOS CI lane | None. Current CI has no macOS job, and the candidate home for the shell-side test-double family is ADR-0005. | The obligation is that a test can control each OS seam, wherever it lives. | Owner decision — owner. | [§6](#6-mock-device-layer)'s seams note, [R6.8](#6-mock-device-layer), [R6.26](#6-mock-device-layer) | open |
| 22 | SDK error-set enumeration | None. The pinned list is to be a checked-in enumeration of the SDK's documented error set as of 4.2.3, regenerated on SDK upgrade, and it is [R6.22](#6-mock-device-layer)'s source of truth for the full error set. | The unrecognized-error fallback is permanent regardless of what the enumeration finds. | The checked-in enumeration itself — none. | [R5.7](#5-mid-session-device-failure), [R6.22](#6-mock-device-layer), [R6.23](#6-mock-device-layer) | open |
| 23 | macOS SDK USB: remaining half | Answered in part by the SDK audit: USB is supported on macOS and each discovery entry reports its transport ([results](prd-device-management-oq-results.md#oq-23--macos-sdk-usb-remaining-half)). | The known-device record persists a per-transport reconnect handle, BLE confirmed and USB assumed. | Connect via a USB discovery entry on hardware — does connecting an entry select the transport, and does the Bluetooth-ID reconnect give USB a handle? — hardware. | [R1.10](#1-device-pairing), [R1.11](#1-device-pairing), [R1.16](#1-device-pairing), [R6.19](#6-mock-device-layer) | residual |
| 24 | Bluetooth grant: recover in place? | None. | None — neither in-place recovery nor a relaunch is assumed. | A macOS build experiment, no instrument needed — none. | [R1.14](#1-device-pairing), [E2](prd-device-management-copy.md#error--state-copy) | open |
| 25 | Entitlement change mid-session | None. Entitlement changes invalidate open connections ([SDK audit](../../briefs/nix-universal-sdk-audit-findings.md), per SDK docs; [results](prd-device-management-oq-results.md#oq-25--entitlement-change-mid-session)). | Such a loss surfaces as a disconnect halt like any other. | Change entitlements against a live session on hardware — can the disconnect copy distinguish a license event from a link loss? — hardware. | [R5.1](#5-mid-session-device-failure), [E23](prd-device-management-copy.md#error--state-copy) | open |
| 26 | Held-by-another-app distinguishability | None. | The device-held state is shown on a connect failure attributed to another app. | A two-app connect experiment on hardware — does the SDK distinguish it, and with what error value? — hardware. | [R1.12](#1-device-pairing), [R6.9](#6-mock-device-layer), [E9](prd-device-management-copy.md#error--state-copy) | open |
| 27 | Spectral entitlement queryable | None. | Detection is at pairing, via the post-connect capability queries. | Probe the capability query on hardware — is providesSpectral answerable without taking a measurement? — hardware. | [R2.6](#2-licensing--pre-authorization), [R2.7](#2-licensing--pre-authorization) | open |
| 28 | Pairing and calibration retry counts | None; candidate 3 consecutive failures. | Candidate 3 consecutive failures, which the escape hatches build against. | Owner decision with dogfood data — owner. | [R1.8](#1-device-pairing), [R3.5](#3-calibration) | open |

Closing rule: the hardware spike's deliverable is one results file with a section per OQ id, [prd-device-management-oq-results.md](prd-device-management-oq-results.md); an OQ is marked answered only when that section exists, and a section existing is necessary rather than sufficient — eleven sections exist already, the eight `residual` questions and the three `open` ones the audit bears on (OQ 4, 19, 25). A question number is never reused. Every inline "per SDK docs; confirm on hardware" marker in this document carries its OQ id — a marker without a matching entry above is invalid. The 5–10 s calibration-duration figure ([R3.1](#3-calibration)) is informative, not gating. Entries whose closer is "none" or "owner" form a parallel track that can run now, without hardware, except where the closer names a prerequisite — OQ 3 after OQ 2, and OQ 5 and OQ 28 after the first dogfood sessions; OQ 4b, 7 and 20b are labelled hardware but close on the same first-build halt logs.
