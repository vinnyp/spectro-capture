# Capture Mode PRD — round 8 fixes

Resume point for round 8 (log: `../agent-reviews/2026-09-06-prd-capture-mode-peer-reviews.md`, "Round 8"). One box per new finding from every lens. Fences F1–F41 (F39, F40, F41 recorded this round) plus the recorded amendments (F6, F31, F36, F38) are the authorization. Tick each box as it lands. Row IDs never renumber; new rows take the next number (next E-row is E44). Line numbers refer to commit `e3991a7`. WHAT not HOW; the operator's vocabulary; no technical jargon.

## Status flips (authorized by the round-8 dispositions in the log)

- [x] **FX8-0** — Flip to 🤝 Aligned the 20 rows the log's "Round 8 — Row flips" paragraph lists; return E20, R9.10, and E32 to ⌛️ (re-opened); the 9 held rows stay ⌛️. Any 🤝 row a fix below edits returns to ⌛️ with "edited in round 8, re-review". Report final counts. — **applied.** 18 of the 20 listed rows flipped to 🤝; 2 (R5.7, R11.12) stayed ⌛️ because a fix edited them. E20, R9.10, E32 re-opened to ⌛️. All 9 held rows were edited and now read "edited in round 8, re-review". Two 🤝 rows a fix edited returned to ⌛️: R5.9, R7.13. One new row lands ⌛️: E44. Final: 182 🤝, 17 ⌛️ of 199 rows.

## Majors

- [x] **FX8-1** (R8-F1 · Staff S8-F1, Test R8-T1, Arch A8-1, Staff Q1, Arch's missing journey line) — New §12 state **E44** for "Re-take sample" or "Restart item" pressed in a non-landing F16 window: nothing to undo, two variants (row just left already set aside / still pending), body in the register of E20 but about undoing, not flagging, and ⟨code⟩ is the row just left (add E44 to the Placeholders paragraph's "row just left" list, `:869`). R4.21 (`:691`) cites E44's fitting variant instead of E20; E20 (`:893`) stays as the Flag answer. R11.12 (`:859`) enumerates E44's variants. UJ3.2 step 4 (`:299-301`) gains the non-landing-window line. Surfaces table Capture row (`:582`) states column adds "nothing to undo". — **done.** New state **E44** "Nothing to undo", two variants on the row just left (already set aside / still waiting in the queue), written in E20's shape but about taking something back rather than flagging. R4.21 now answers the non-landing window with E44's fitting variant and says why E20 stays the Flag answer; its test line reads back E44's still-pending variant. R5.7 no longer says the undo actions "answer the same way" as Flag. R11.12 enumerates E44's two variants and how a test tells E20 and E44 apart. UJ3.2 step 4 gains the non-landing-window line. Placeholders paragraph adds E44 to the row-just-left list; the Surfaces table's Capture states add "nothing to undo".
- [x] **FX8-2** (R8-F2 · PM PM8-1, Staff S8-F2, Arch A8-2, PMM PMM8-1, Staff Q2; **fence F39**) — One condition everywhere: "Review the set-aside swatches" is offered while any set-aside row exists, settled or not, and not offered when none is. R8.1 route 2 (`:774`) says so and names the end-early summary as a way in (five ways, or route 2 reached from the summary); routes 1 and 3 keep "unsettled" (completion and auto-entry). R7.5 (`:753`): "Where any swatch is set aside the summary offers the review now or later". E23 (`:896`): the action is the Labels wording "Review the set-aside swatches — only while any swatch is set aside"; the body's set-aside sentence stays true for settled rows ("you can deal with them now or another day" is fine; do not say they are waiting on a decision). R7.13 (`:761`): "deferred rows waiting" → "an unsettled set-aside row waiting" to match R3.9. Leave R3.9, R8.5, R8.15, E33 as they are unless a word contradicts F39. — **done.** One condition, fence F39 cited at each site. R8.1 route 2: offered wherever the set-aside rows are reachable from — the collection surface and the end-early summary (R7.5, E23) — whenever the collection holds any set-aside row at all, settled or not; a new sentence says "settled" governs only the ways in that happen on their own (routes 1 and 3), so a collection of settled rows still has a door into the list. R7.5: "Where any swatch is set aside the summary offers the review — now or another day — on the one condition the offer carries everywhere." E23's action is now the Labels wording, "Review the set-aside swatches — only while any swatch is set aside"; its body's set-aside sentence is unchanged. R7.13: "deferred rows waiting" → "an unsettled set-aside row waiting". R11.12's description of E23's action follows the new label. R3.9, R8.5, R8.15, E33 needed no change.
- [x] **FX8-3** (R8-F3 · Staff S8-F3, Staff Q3; **fence F40**) — R5.9 (`:712`): N_CONSEC_HARD is never below 2, whatever OQ 3 tunes, because a guard that fires on one row is not watching for a run; that is why one row's auto-deferral never pauses the session. OQ 3 (`:948`): record the floor in the details. R11.9 (`:856`): replace "so no tuning of OQ 3 can invalidate it" with the floor citation (fence F40, R5.9). — **done.** R5.9 states the floor: N_CONSEC_HARD is never below 2 whatever OQ 3 tunes it to (fence F40), because a guard that fires on a single row is not watching for a run — and that floor is what makes the no-pause promise that follows hold. OQ 3's details record the bound and why. R11.9 drops "no tuning of OQ 3 can invalidate it" and rests the interleaved step on the floor instead (fence F40, R5.9).
- [x] **FX8-4** (R8-F4 · Test R8-T3) — R4.22 (`:692`): state that a license valid in every other way but without spectral data passes the pre-flight gate with the shortfall shown as an indicator, never as a block (fence F36; device PRD §4 puts it under the authorization check), and cite OQ 16. Closing hand-off paragraph (`:919`): the device PRD §4/§7 note says the same — indicator, not a block, and the §7 state gains its forward action. — **done.** R4.22 states that a license valid in every other way passes the pre-flight gate, with the shortfall shown as an indicator under the authorization check rather than as anything that blocks (device PRD §4), citing fence F36 as clarified and OQ 16. The closing hand-off note now runs §2/§4/§7 and says the same — indicator at §4, forward action at §7 — and OQ 16's title and details take §4.
- [x] **FX8-5** (R8-F5 · Test R8-T2; **fence F41**) — R7.1 (`:749`): the opening sentence lists what the samples survive — a reorder, looking at the queue list, and any offer the operator cancels (the re-scan offer of R6.3/E28, the add of R9.10/E32) — and the discard list drops "Add a swatch" in favour of "an add the operator sees through". R9.10 (`:816`): cancelling the add returns to the held row with its samples intact and says nothing was let go; only an add that goes through discards them. E32 (`:905`): body becomes "You're back on ⟨code⟩ with the ⟨n⟩ samples you'd taken" (leave the count sentence out when it would be zero, per the Placeholders rule). — **done.** R7.1's opening sentence lists what survives: a reorder, looking at the queue list, and any offer the operator cancels (the re-scan offer of R6.3/E28, the add of R9.10/E32), because backing out of an offer never put a different item under the instrument (fence F41); the discard list now reads "an 'Add a swatch' the operator sees through". R9.10 says cancelling creates nothing and lets nothing go — back on the held row with its samples — and that only an add that goes through discards them; its opening clause now discards "when the add goes through". E32's body becomes "You're back on ⟨code⟩. The ⟨n⟩ samples you'd taken on it are still there.", the count sentence dropping out at zero by the Placeholders rule.
- [x] **FX8-6** (R8-F6 · Arch A8-4, Staff Q4) — R4.22 or R11.4 (`:692` / `:851`): inherited note for the device PRD §6 on the R11.3 pattern — the Demo Device's simulated state surface gains a settable "spectral data available / absent" so a session can start either way without the licensing layer the Demo Device never invokes, and E43 shows under it exactly as it would live; OQ 16 carries it (its Feeds already list R4.22). Closing hand-off paragraph (`:919`) lists the note. — **done.** Placed in R4.22 (the row that owns the test), on the R11.3 pattern: because selecting the Demo Device never goes near licensing and the absent-spectral-data outcome sits inside the layer it skips, the Demo Device's settable state surface gains a spectral-data-available-or-absent switch of its own, on the same footing as the battery level and the calibration-due signal, and a session started with it absent shows E43 exactly as a live one would. Marked as an inherited note for device PRD §6, OQ 16. The closing hand-off paragraph now says §6 gains three notes; OQ 16's §6 sentence carries it and its Feeds already list R4.22.
- [x] **FX8-7** (R8-F7 · Arch A8-3) — R11.13 (`:860`): the record is kept per session; a chain's interactions are read by following the chain's links (R7.12), so "per session and per session chain" and "covers one session at a time" no longer claim two scopes. OQ 24 (`:969`) wording matches. — **done.** R11.13: the record is kept per session and no second copy per chain — a day that crashed twice is read by following the links between its three sessions (R7.12) and reading each one's record, the way the tallies and elapsed time already are; the later "covers one session at a time" sentence is gone. OQ 24 matches.
- [x] **FX8-8** (R8-F8 · PMM PMM8-2) — E43 (`:916`) body names the real loss and the way forward, in the operator's words: the license doesn't unlock spectral measurements, so scanning carries on without them; the app keeps the colour values the instrument reports rather than the full curve behind them, so these swatches can't later be worked out under a different light; every swatch scanned now is marked so it can be told apart and re-scanned once the license includes spectral data. Drop the D50/2° sentence (true either way, so it says nothing here). — **done.** E43's body names the real loss and the way forward: the license doesn't unlock spectral measurements so scanning carries on without them; the app keeps the colour values the instrument reports rather than the full curve behind them, so these swatches can't be worked out later under a different light; every swatch scanned now is marked so they can be told apart and scanned again once the license includes spectral data. The D50/2° sentence is dropped.

## Minors

- [x] **FX8-9** (PM PM8-2, Test R8-T4) — E23 (`:896`): the dropped-samples sentence names the swatch, using a second placeholder distinct from the resume-row ⟨code⟩ (e.g. "the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ were let go"); extend the Placeholders paragraph (`:869`) to say what ⟨swatch⟩ names in E23. R7.5 keeps "and off which swatch". — **done.** E23 reads "the ⟨dropped⟩ samples you'd taken on ⟨swatch⟩ were let go", with ⟨code⟩ left to the resume row. The Placeholders paragraph says ⟨swatch⟩ appears in E23 alone and that the two sentences are about two different rows. R7.5 keeps "and off which swatch".
- [x] **FX8-10** (PMM PMM8-3) — Surfaces table Capture row (`:582`), Shows column: add "and the non-spectral indicator while a session runs without spectral data". — **done.** The Surfaces table's Capture row now shows "the always-visible simulated indicator, and the non-spectral indicator while a session runs without spectral data".
- [x] **FX8-11** (hygiene) — After all edits: every row a fix touched carries the right status; §12 Labels paragraph (`:867`) still lists every control named by a row; Traceability paragraph (`:571`) names F39–F41; mermaid still renders; no `&amp;` introduced; row counts reported (expect 144 R, 44 E, 11 M). — **done.** Rows: 144 R, 44 E, 11 M (199). Statuses: 182 🤝, 17 ⌛️; every row carries one of the two. 15 rows were edited and 19 changed status only, checked cell by cell against the previous commit — no body was touched by a status flip (one clobbered cell, M11's Method, was caught and restored). §12 Labels still writes every control a row names, E23's new action among them; the Traceability paragraph names F39–F41. Six mermaid blocks, none of them touched. No `&amp;`. 689 links checked, none broken.
- [x] **FX8-12** (residue the fix pass surfaced; fence F31 as clarified, F39) — UJ3 flowchart (`:335`, `:338`): the queue-exhausted edges read "yes, deferred rows exist" → review and "yes, none deferred" → complete; make them turn on an *unsettled* set-aside row, matching R8.1 route 1, R3.9, and the session diagram's edges (`:84`/`:85`). Validate the mermaid after the edit. The end-early edge at `:409` ("deferred rows exist") is correct under F39 and stays. — **done.** The UJ3 flowchart's two queue-exhausted edges now turn on whether a set-aside row is unsettled, in the same words as the session diagram's edges at `:84`/`:85`: `M -- "yes, a set-aside row is still unsettled" --> N` (end-of-session review) and `M -- "yes, every set-aside row is settled" --> O` (session complete). Both labels are quoted exactly as the neighbouring edges are; the wrap edge and `:409`'s end-early edge are untouched, the latter being correct under fence F39. Six mermaid blocks still parse, every multi-word edge label quoted.

## Item-for-item map (verified against each review)

PM: PM8-1→FX8-2 · PM8-2→FX8-9. Staff: S8-F1→FX8-1 · S8-F2→FX8-2 · S8-F3→FX8-3 · Q1→FX8-1 · Q2→FX8-2 · Q3→FX8-3 · Q4→FX8-6. Test: R8-T1→FX8-1 · R8-T2→FX8-5 · R8-T3→FX8-4 · R8-T4→FX8-9. Arch: A8-1→FX8-1 · A8-2→FX8-2 · A8-3→FX8-7 · A8-4→FX8-6 · missing UJ3.2 line→FX8-1 · R3.12 risk note→log only. PMM: PMM8-1→FX8-2 · PMM8-2→FX8-8 · PMM8-3→FX8-10. agy: none.
